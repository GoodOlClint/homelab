# DEPRECATED: This file is no longer used at runtime.
#
# Infisical is the sole source of runtime secrets. There is no SOPS fallback.
#
# For bootstrap secrets, see: bootstrap.sops.example.yml
# For external secrets seeding, see: bootstrap.sops.example.yml (external_secrets section)
# For disaster recovery backup: make infisical-backup
