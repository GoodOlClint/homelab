---
- name: restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true

- name: restart lancache
  community.docker.docker_compose_v2:
    project_src: /opt/lancache
    state: restarted
  listen: "restart lancache"
