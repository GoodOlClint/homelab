---
# UniFi Controller Handlers

- name: Restart UniFi
  ansible.builtin.systemd:
    name: unifi
    state: restarted
    daemon_reload: true

- name: Restart MongoDB
  ansible.builtin.systemd:
    name: mongod
    state: restarted
