{
  "settings": {
    "cert": "{{ control_fqdn }}",
    "port": 443,
    "redirPort": 80,
    "allowLoginToken": true,
    "allowedOrigin": ["{{ control_fqdn }}", "meshcentral.{{ dns_zones.vlan100.zone }}"],
    "plugins": { "enabled": false }
  },
  "domains": {
    "": {
      "title": "{{ meshcentral_title }}",
      "newAccounts": {{ meshcentral_new_accounts | to_json }},
      "amtManager": true
    }
  }
}
