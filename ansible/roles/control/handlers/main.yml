---
- name: restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: restart control compose
  community.docker.docker_compose_v2:
    project_src: /opt/control
    state: present
    recreate: always
