{{'{{'}}- /* ntfy publish URL for alert delivery — bare value for Alertmanager url_file.
     A literal comment line here would become part of the URL. */ -{{'}}'}}
{{'{{'}}- with secret "{{ infisical_agent_project_id }}" "{{ infisical_agent_env_slug }}" "/monitoring" -{{'}}'}}
{{'{{'}}- range . -{{'}}'}}
{{'{{'}}- if eq .Key "alert_ntfy_topic" -{{'}}'}}
https://ntfy.sh/{{'{{'}} .Value {{'}}'}}?template=alertmanager
{{'{{'}}- end -{{'}}'}}
{{'{{'}}- end -{{'}}'}}
{{'{{'}}- end -{{'}}'}}
