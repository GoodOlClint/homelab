{{'{{'}}- /* OpenObserve password — bare value for Prometheus remote_write password_file.
     A literal comment line here would be read as part of the password (this
     exact bug silently broke remote_write basic auth until 2026-07-27). */ -{{'}}'}}
{{'{{'}}- with secret "{{ infisical_agent_project_id }}" "{{ infisical_agent_env_slug }}" "/monitoring" -{{'}}'}}
{{'{{'}}- range . -{{'}}'}}
{{'{{'}}- if eq .Key "openobserve_root_user_pass" -{{'}}'}}
{{'{{'}} .Value {{'}}'}}
{{'{{'}}- end -{{'}}'}}
{{'{{'}}- end -{{'}}'}}
{{'{{'}}- end -{{'}}'}}
