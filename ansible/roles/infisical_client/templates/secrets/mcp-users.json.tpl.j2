{{'{{'}}- with secret "{{ infisical_agent_project_id }}" "{{ infisical_agent_env_slug }}" "/mcp" {{'}}'}}
{{'{{'}}- $s := . {{'}}'}}
{
{% for name, u in mcp_users.items() %}
{{'{{'}}- range $s {{'}}'}}{{'{{'}} if eq .Key "gateway_user_token_{{ name }}" {{'}}'}}
  {{'{{'}} .Value | printf "%q" {{'}}'}}: {"name": "{{ name }}", "tools": {{ u.tools | to_json }}, "can_approve_t3": {{ u.can_approve_t3 | default(false) | to_json }}}{{ '' if loop.last else ',' }}
{{'{{'}}- end {{'}}'}}{{'{{'}} end {{'}}'}}
{% endfor %}
}
{{'{{'}}- end {{'}}'}}
