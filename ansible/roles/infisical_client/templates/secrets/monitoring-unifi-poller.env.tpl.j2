# UniFi Poller password — rendered by Infisical Agent
{{'{{'}} with secret "{{ infisical_agent_project_id }}" "{{ infisical_agent_env_slug }}" "/monitoring" {{'}}'}}
{{'{{'}}- range . {{'}}'}}
{{'{{'}}- if eq .Key "unifi_monitoring_password" {{'}}'}}
{# UP_UNIFI_DEFAULT_PASS, not ..._PASSWORD — unpoller reads `pass`, and a wrong name #}
{# is silently ignored, leaving it authenticating with an empty password. #}
UP_UNIFI_DEFAULT_PASS={{'{{'}} .Value {{'}}'}}
{{'{{'}}- end {{'}}'}}
{{'{{'}}- end {{'}}'}}
{{'{{'}} end {{'}}'}}
