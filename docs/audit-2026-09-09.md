# Repo scan — 2026-09-09

Whole-tree review by five reviewer lenses (bugs, security, performance, simplification, modernization), every finding re-read at the cited line by the orchestrator. Raw: 65 findings. Dropped: 1 (timestamped `terraform.tfstate.*.backup` files are covered by `terraform/.gitignore` `*.backup`; none tracked). Downgraded to notes: 2. Cross-lens duplicates merged. Each row below maps to one GitHub issue (one PR closes it); the GitHub issue number follows the row id; X7 lands in #12 with the Makefile fixes.

Independent baseline gathered while the reviewers ran: 36 roles, 3 with no reference anywhere (`juju4.openobserve`, `mitre.yedit`, `plex_services` — all untracked working-tree leftovers); one test script (`scripts/test_axosyslog_routing.py`), live-only; no `.github/` directory, so nothing runs in CI; `ansible-playbook --syntax-check site.yml` passes but warns that `nvidia-licensing`, `minio`, `squid` match no host; `terraform validate` passes on all three roots with 8 bpg `proxmox_virtual_environment_download_file` deprecation warnings (removed in provider v1.0).

## Fix order

1. ~~CI workflow (#5)~~ — **done 2026-09-10** (ee7c6a0, b5cb50e): `.github/workflows/validate.yml` = `make validate`; green on main from run 2. The first landing baselined 136 pre-existing `ansible-lint` findings in an ignore file, which made the run green while printing every one as a warning annotation — reverted the same day: every finding is fixed (changed_when on every command, pipefail on every pipe, apk FQCN, file modes, deb822 for plex) and there is no ignore file; the retired-guest roles are excluded until #24 deletes them; `flux_check.py --build-only` in CI because the binding check needs the cluster; the axosyslog test needed a poll instead of a fixed wait (`use_dns(yes)` landed after it was written).
2. ~~Correctness with data-loss or outage potential~~ — **done 2026-09-10**: loader (#6, 10196ef: a 404 reads as empty, anything else fails; `pbs_client` verify fails on a real error; `vps_wireguard` reuses the host's key), Talos secrets (#7, already fixed by WP0), PBS (#8, f0ea11d — found en route: `gc-job` is not a PBS 4 command, so the live datastore had **no GC schedule** until this fix; the `datastore.cfg` fallback is deleted, `--reuse-datastore` is the rebuild path), BIND journal (#9, 766eb6a). Deviation: the VPS play still ends `changed=5` on a second run for reasons untouched here (OpenRC service status, two wait tasks, a setcap) — filed as #41.
3. ~~Security~~ — **done 2026-09-10** for the fix-order set: TSIG config 0640 + keyed AXFR + scoped forwarding (#15, 766eb6a), MeshCentral key 0600 + `no_log` (#16, f562488), reaper `-k` (#18, already fixed by WP6d), forward-auth binding (#19, already fixed by WP2), shared acme-key (#20: ADR 0050 written, 63cfd32; implementation re-homed to tranche 2). S4/S6/S7/S9/S10/S11 are tranche 2.
4. ~~Retired-guest sweep~~ — **done 2026-09-10** (723a27f, #24): the three plays, four roles, five collections, docs, the `dns_config` paragraph; S11 with it.
5. ~~Everything else by table order~~ — **tranches 2, 3 and 4 done 2026-09-10** (the tranche rows below carry the deviations). Every reviewer-filed issue is closed; the phase is drained.

## Bugs

| # | file:line | Defect | Failure scenario | Fix direction |
|---|---|---|---|---|
| B1 (#6) | `ansible/tasks/infisical_login.yml:58,92` | `failed_when: false` on the `/shared` read and the 16-folder loop; `:106` drops any errored folder | One 5xx on the `/control` read → `secrets.control` absent → `generate_secret.yml` rotates `portainer_admin_password` under the live instance (the 2026-08-22 incident by a second route) | Fail the play when any folder read errors; distinguish "empty" from "failed" |
| B2 (#8) | `ansible/roles/proxmox_backup/tasks/main.yml:472-492` | The "path not empty" fallback `copy`s the whole `datastore.cfg` from only the failed results | Two datastores, one create succeeds → the file is rewritten without it; proxy restarts; every backup to the dropped store fails | Render all `pbs_datastores`, or per-store `blockinfile` keyed by name |
| B3 (#7) | `kubernetes/talos/talos.sh:46` | `inf secrets get … 2>/dev/null \|\| true` swallows auth/network failure as "key absent" | `existing` empty → `talosctl gen secrets --force` → `inf secrets set` overwrites `/talos/talos_secrets_yaml`; the running cluster's PKI is no longer in the vault | Separate "call failed" from "key absent"; refuse to generate unless the folder read succeeded |
| B4 (#9) | `ansible/roles/bind9/tasks/main.yml:238-288` | One zone failing `rndc zonestatus` → stop named, `rm -f /var/lib/bind/*.jnl`, start | A new zone whose file is not yet created wipes the DDNS journal of every zone, rolling back all recently registered records | Scope the removal to the failed zones; require `rc == 0` and non-empty stdout |
| B5 (#10) | `ansible/roles/adguard/tasks/main.yml:60,81` | `get_url` of `releases/latest` with no checksum/stat guard; the binary `copy` has no `notify` | Every run downloads from GitHub; an upstream release lands on disk while the old process keeps serving | Pin the version, add the stat/size guard, notify the restart handler |
| B6 (#10) | `ansible/roles/adguard/tasks/main.yml:386,399,413,428` | `retries:`/`delay:` with no `until:` (the only four such tasks in the tree) | The validation runs once; a warming BIND fails the play on the first attempt | Add `until:`; move the pass condition out of `failed_when` |
| B7 (#11) | `kubernetes/ceph-csi/deploy.sh:17`, `kubernetes/zot/deploy.sh:18`, `kubernetes/infisical/deploy.sh:27` | `… \| grep -q X && echo PASS` — the `&&` left side is exempt from `set -e` | `make infisical-smoke` reports success when the operator synced no keys | `grep -q X \|\| { echo FAIL >&2; exit 1; }` (the `plex-services` smoke is the correct shape) |
| B8 (#6) | `ansible/roles/pbs_client/templates/pbs_env.j2:15`, `tasks/main.yml:113-127` | `default('')` bakes an empty token; verify is `failed_when: false` and prints "SKIPPED" | Combined with B1: every guest's PBS env rewritten empty, run green, no backups | Fail when the token is absent once the PBS role has run |
| B9 (#12) | `Makefile:382` | `$(eval VPS_IP := $(shell terraform output …))` expands before the Phase-1 apply on `:380` | First-ever `make vps-deploy`: empty IP, Phase 2 dies after the firewall was opened | Resolve inside a shell line, as `rebuild` does for `VM_IP` |
| B10 (#11) | `kubernetes/cert-manager/deploy.sh:48`, `kubernetes/talos/talos.sh:130` | Unbounded `until … ; do sleep 5; done`, stderr discarded | A malformed issuer or a node that never applies → `make talos-certs`/`talos-apply` hangs silently | Bound the loops, print the last stderr on give-up |
| B11 (#6) | `ansible/roles/vps_wireguard/tasks/main.yml:73-101` | Generation is gated on the secret being falsy, not on the host lacking a key | A swallowed `/vps` read (B1) → new keypair → pfSense holds the old peer key → tunnel dead, sshd only on tunnel IPs → `make vps-rebuild` | Gate on `/etc/wireguard/wg0.conf` absence; fail loudly if `secrets.vps` is undefined |
| B12 (#8) | `ansible/roles/proxmox_backup/tasks/main.yml:697-732` | Verify/GC/prune job creation `failed_when: false` | A bad schedule or missing datastore is indistinguishable from "already exists" | `failed_when: rc != 0 and 'already exists' not in stderr` (the iSCSI login at `:252` is the pattern) |
| B13 (#13) | `terraform/modules/proxmox-vm/containers.tf:158`; `virtual_machines.tf:140-142` | `"${ip}/${prefix}"` with no null check; a guest with no management leg still gets a management-subnet address | An LXC leg without `ip_offset` renders `"/24"` | `precondition` requiring a non-null `ip` per leg |
| B14 (#40) | `ansible/tasks/dns_records.yml:102` | `_dns_mirror` is only set inside a loop; used without `default([])` | `internal_zones` with no mirrors → undefined variable | Seed `_dns_mirror: []` |
| B15 (#14) | `ansible/roles/plex/tasks/main.yml:247,256-262` | `apt_repository` (banned for LXC/Debian 13 guests); `state: latest` gated on the package being absent | Plex is never upgraded by its role; a fresh CT template fails the repo step | `deb822_repository` + `install_python_debian`; drop the `when:` |
| B16 (#12) | `Makefile:503` | `clean-ssh` reads `.get('all')` — `proxmox.yaml`'s top key is `proxmox:`; crashes on an empty yaml | Node SSH keys are never cleaned | Walk every top-level group |

Note only: the three `deploy.sh migrate` paths run `rsync --delete` with no non-empty-source gate (the 2026-08-21 Valheim loss mechanism), but the old guests are gone and the paths are dead — delete them in the scaffolding sweep.

## Security

| # | file:line | Defect | Failure scenario | Fix direction |
|---|---|---|---|---|
| S1 (#15) | `ansible/roles/bind9/tasks/main.yml:82-87`, `templates/named.conf.local.j2:5,11,18,39,49` | `named.conf.local` is `0644` and carries all three TSIG secrets; AXFR allowed by bare secondary IP; `allow-update-forwarding { any; }` | Any local process on dns1/dns2 reads `dhcp-update-key` (`zonesub ANY` on every zone) and `acme-key` → repoint any name and mint a trusted cert for it | Move `key {}` blocks to a `0640 root:bind` include; scope transfer/forwarding to the internal CIDR |
| S2 (#16) | `ansible/roles/control/tasks/main.yml:52-61,100-112` | MeshCentral `key.pem` copied `0644`; Portainer `admin/init` POST has no `no_log` | Any local user reads the control-plane TLS key; the admin password lands in journald and ships to OpenObserve | `0600` on the key item; `no_log: true` |
| S3 (#20) | `scripts/pki_hosts.sh:26`, `named.conf.local.j2:33` | One shared `acme-key` (wildcard TXT on every zone) + a `*.<service domain>` policy | Any compromised guest mints a cert for `infisical.`/`pve.` that every host trusts | Design decision: per-host TSIG keys scoped to `_acme-challenge.<host>` or a narrower policy; record as an ADR 0041 consequence |
| S4 (#23) | `scripts/node-bootstrap.sh:2-4,26` | `terraform@pve` gets `Administrator` on `/` under a "least-privilege" header | The comment misleads reviewers; the token is root-equivalent | Scope the ACL or delete the claim |
| S5 (#18) | `kubernetes/arc/reaper.yaml:25` | `curl -sk` with the `ci@pve!ci` token | Anything winning the address on vlan40 collects a token that creates/destroys pool-`ci` guests; ADR 0042 forbids `--insecure` | Mount `homelab-root-ca`, drop `-k` |
| S6 (#17) | `pdm/tasks/main.yml:14`, `vps_wireguard:110`, `playbooks/vps-rotate-keys.yml:30`, `playbooks/backup-finalize.yml:100,110,125`, `proxmox_backup:510`, `plex_certificate:148`, `scripts/bake-answer.sh:61,74,81-90`, `kubernetes/lib.sh:31`, `kubernetes/arc/deploy.sh:17-24`, `ansible/tasks/bootstrap_infisical_setup.yml:96,143`, `squid:113` | Secrets on process argv (visible in `ps`); PDM hash uses a static salt `pdmroot`; `bake-answer.sh` chmods after writing | Any local user reads the PBS token, WG private key, PDM root password, Infisical admin password for the life of the process | `stdin:`/`--value-stdin` (the pattern in `bootstrap_pre_tasks.yml:114`); compare against the stored hash's own salt; `umask 077` |
| S7 (#21) | `ansible/tasks/bootstrap_infisical_setup.yml:224-227,249` | `ansible-admin` org-admin identity trusts `0.0.0.0/0`, `ttl: 0`, `numUsesLimit: 0` | A leaked `bootstrap.infisical_client_secret` is a permanent, anywhere-valid vault superuser | Trusted IPs = management + services prefixes |
| S8 (#19) | `kubernetes/authentik/blueprint-internal.yaml:7-20` | The forward-auth application has no policy binding (the only binding in either blueprint is `pfsense`) | Every authenticated user of the internal realm reaches Headlamp, Prometheus, Alertmanager, the arr UIs; ADR 0046 federation would hand that to every family account | Bind the application to `authentik Admins` before ADR 0046 lands |
| S9 (#22) | `scripts/security_guardrails.sh:70,79-82,10` | Domain check silently no-ops without `vlans.yaml`; any basename containing `example` bypasses the scan; tfstate regex misses `.<epoch>.backup` | A fresh clone or CI runner passes the guard with the domain half disabled and no message | Fail loudly when `vlans.yaml` is absent; anchor the exemption to `.example`; widen to `tfstate*` |
| S10 (#23) | `terraform/unifi/provider.tf:11`, `ansible/roles/monitoring_users/tasks/unifi.yml:7-94`, `synology.yml:8` | `allow_insecure = true` / `validate_certs: false` after P9 gave both hosts fleet certs | Admin credentials over unverified TLS to the system that owns the L2 fabric; ADR 0041 forbids `insecure` on a provider | Verify against the root |
| S11 (#24) | `ansible/roles/nvidia/tasks/main.yml:31,115` | `curl --insecure` / `validate_certs: false` | Role is retired; delete it with the sweep |

Notes only: ARC runner pods are `hostNetwork` + `privileged` on `talos-cp-a` (`kubernetes/arc/values-common.yaml:9,25`) — this is the ADR 0034 design, recorded as an accepted risk; the Portainer agent `TLSSkipVerify` path (`control/tasks/main.yml:148-150`) is dead because `portainer_agent_hosts` defaults to `[]`.

## Performance

| # | file:line | Defect | Failure scenario | Fix direction |
|---|---|---|---|---|
| P1 (#29) | `ansible/ansible.cfg` | No `pipelining`, default `forks = 5`, no fact cache; Phase 2 re-gathers facts on six Phase 1 hosts | Every task pays an extra SFTP round trip; `hosts: all` plays run in batches of 5 | `pipelining = True`, `forks = 20`, `gathering = smart` + jsonfile cache |
| P2 (#27) | `ansible/tasks/full_pre_tasks.yml:29`, `infisical_login.yml:15-92,28-41,70-86` | The loader is included per play (13 in `infrastructure.yml`, 4 in `services.yml`): 17 × (status + SOPS decrypt + login + 18 folder reads); six of the folders (`minio`, `docker`, `plex-services`, `pfsense`, `jellyfin`, `talos`) have no Ansible consumer; root `/` is read although policy says it is empty | ~360 vault round trips per `make ansible-all` to build the same dict; a down vault costs 17 × 30 s of retries before a clean failure (`load_secrets.yml:4-18`) | Load once in the existing localhost pre-flight play and read it from `hostvars['localhost']`; drop the dead folder entries and the root read |
| P3 (#28) | `ansible/roles/infisical_client/tasks/provision_identity.yml:18-30`, `delete_identity.yml:26-45`, `bootstrap_infisical_setup.yml:22,348`, `playbooks/refresh-identity.yml:37` | Five copies of the admin universal-auth login; `provision_identity` logs in per host, discarding the play-level token; `infisical_write_secret.yml:5-36` POSTs the folder and re-reads it per key | One extra session per guest per run; serialized on `refresh-identity` | One `ansible/tasks/infisical_admin_token.yml` with `run_once`; reuse the play token |
| P4 (#12) | `Makefile:25-29` | Five `:=` `sops -d` calls at parse time, exported to every target | `make clean-ssh` decrypts the PVE root password and hands it to kubectl/helm/talosctl | Target-specific `export` on the terraform recipes |
| P5 (#31) | `kubernetes/monitoring/deploy.sh:16` | One `CONFIG_HASH` over every config stamped into six Deployments | Editing `snmp.yml` restarts Prometheus, Grafana and the syslog collector (RWO PVCs, `Recreate`) | Hash per consumer |
| P6 (#31) | `kubernetes/update.sh:24` | All ~23 `:latest` workloads restarted at once; ~12 are `docker.io` → Zot re-checks upstream anonymously | Two runs in 6 h trip the Docker Hub quota CLAUDE.md records | Batch with a wait |
| P7 (#30) | `adguard:39`, `proxmox_backup:129`, `bind9:19`, `pxe:44`, `plex:260`, `apt_cacher_ng:6`, `unifi/install:18`, `squid`, `vps_nftables:29`, `expand_disk:8` | `update_cache: true` unconditional, no `cache_valid_time` | 10–30 s per host per run; a hard dependency on the mirror answering | Gate on the repo task's `changed` (`pdm:57`, `pbs_client:67` are the pattern) or `cache_valid_time: 3600` |
| P8 (#31) | `kubernetes/monitoring/app.yaml` heartbeat CronJob, `arc/reaper.yaml`, `plex-services/app.yaml` pg-backup | No `resources.requests` (the only four containers in the tree missing them) | The per-minute pod looks free to the scheduler | Add requests |
| P9 (#28) | `ansible/playbooks/refresh-identity.yml:55` | `limit=100&offset=0`, no pagination | Past 100 identities the orphan cleanup silently stops finding them | Page until short |

## Simplification

| # | file:line | Defect | What to delete / merge |
|---|---|---|---|
| X1 (#24) | `ansible/playbooks/services.yml:1-18,47-72,74-88`; roles `nvidia`, `nvidia_licensing`, `minio`, `squid`; `docker-config.yml:14,39-58`; `update-all.yml:24-29`; `infisical_login.yml` `minio`/`squid` entries; `requirements.yml` `devopsarr.*` ×4 + `community.postgresql`; `README.md:23,223-228,323,354-357`; CLAUDE.md tag list + folder rows; `dns_config/tasks/main.yml:23-27` stale paragraph | Three plays target guests absent from `vm-configs.tf`; the roles, collections, docs and comments that only they used | Delete together; `services.yml` becomes the plex play. Untracked leftovers `ansible/roles/{plex_services,juju4.openobserve,mitre.yedit}` go from the working tree |
| X2 (#25) | `ansible/tasks/deploy_compose.yml`; `infrastructure.yml:28-58` + `network-data/public_policy.yaml` + `scripts/validate_public_policy.py` + `Makefile:455` + `security_guardrails.sh:118` + `terraform/modules/network/*.tf.placeholder`; `scripts/{organize_infisical_folders.sh,bridge-restore.sh,migrate-terraform-state.sh,infisical_cleanup_root.sh,mdns-*}` + `Makefile:432`; the three `deploy.sh migrate` functions | Zero callers (deploy_compose, documented as canonical in CLAUDE.md Resolved #6); `use_rendered_policy` is set nowhere and `make pr6-render` does not exist; one-shot scripts whose events are complete | Delete; strike the CLAUDE.md sentence |
| X3 (#26) | `terraform/modules/proxmox-vm/packer.tf`, `variables.tf:277-293`, `virtual_machines.tf:147,170,215,284`, `terraform/variables.tf:163-170,185-195,245-253`, `main.tf:5,57`, `outputs.tf:54-80,90-95` | `use_packer_template` is a constant `false`; `iscsi-*` storage defaults and `guest_access_plane = "management"` describe the destroyed fleet; five outputs and `manage_sdn` have no reader | Delete the packer path and outputs; flip the defaults to `ceph-rbd`/`services`; refresh `vars.auto.tfvars.example` |
| X4 (#36) | `ansible/roles/plex_certificate/tasks/main.yml` vs `cert_client/tasks/main.yml`; `plex_certificate/handlers/main.yml:2-4` | Same certbot role with a different DNS plugin; ADR 0045 plans a third copy; one handler nothing notifies | Parameterize `cert_client` (plugin, credentials template, ACME server); `plex_certificate` keeps only the PFX tasks |
| X5 (#37) | `kubernetes/plex-services/app.yaml:444-482`, `kubernetes/authentik/app.yaml:37-75` | The PBS `pg-backup` CronJob copy-pasted | One `kubernetes/pg-backup.yaml` with `${NS} ${PG_USER} ${PG_SECRET} ${BACKUP_ID} ${SCHEDULE}` |
| X6 (#38) | `pbs_client:24-56`, `proxmox_backup:47-113`, `pdm:29-48`, `pxe:5-23`, `proxmox_host/tasks/repos.yml:36-54` | Four idioms for the Proxmox apt key (three keyring paths, `.list` vs deb822) | One `ansible/tasks/proxmox_apt_repo.yml` |
| X7 (#12) | `Makefile` (19 copies of the playbook prefix); 12 `kubernetes/*/deploy.sh` (`REGISTRY=` preamble) | Mechanical duplication | `ANSIBLE_PLAYBOOK :=` variable; `lib.sh` exports `REGISTRY` |
| X8 (#35) | `AGENTS.md` | A 364-line fork of CLAUDE.md with 2 mentions of Talos/Kubernetes, `scripts/seed_uptime_kuma.py` (nonexistent), `pbs_fingerprint` (retired) | One-line pointer to CLAUDE.md |
| X9 (no PR) | `ansible/group_vars/all.yml` (gitignored) | 34 unreferenced keys incl. `lancache_nfs_src`, `pbs_insecure`, `home_assistant_*`, `vm_network_interfaces` | Local cleanup, no PR |

## Modernization

| # | file:line | Defect | Consequence | Fix direction |
|---|---|---|---|---|
| M1 (#5) | none | No CI; no `terraform fmt/validate`, `ansible-lint`, or test invocation in the Makefile | Every rule in CLAUDE.md is enforced by memory | One workflow: fmt-check + validate ×3 roots + ansible-lint + syntax-check + the axosyslog test; `actions/checkout@v6` |
| M2 (#32) | every `kubernetes/*/deploy.sh` except `authentik/deploy.sh:15,48` | `helm_apply` with no `--version` (cert-manager, traefik, metallb, zot, external-dns, infisical, headlamp, metrics ×3, ceph-csi, arc) | A recovery re-run can install a new chart major; renovate cannot see it; contradicts the `make talos-update` contract | `CHART_VERSION` per tree + a renovate regex manager |
| M3 (#33) | `kubernetes/zot/values.yaml` (no `loadBalancerIPs`), `ansible/tasks/dns_records.yml:12-16`, `traefik/deploy.sh:11`, `monitoring/deploy.sh:11`, `games/deploy.sh:10`, `metallb/deploy.sh:12` | Zot's LB address is whatever MetalLB hands out while DNS hardcodes 64; offsets 64–67 are literals in five files | A pool reallocation points `registry.` (baked into `machine.registries`) at the wrong host → cluster-wide pull failure | Pin Zot like the other three; offsets into `vlans.yaml`, read by `subnet_ip` and `dns_records` |
| M4 (#34) | `kubernetes/zot/deploy.sh:39-42` | `kubectl create secret` outside the documented allowlist (htpasswd + OIDC secret) | An unrecorded exception to the ADR 0035 rule; the push credential does not rotate with the vault | OIDC half via `InfisicalSecret`; record the htpasswd half as an exception |
| M5 (#34) | `kubernetes/monitoring/deploy.sh:13` | `sed` over `terraform/vars.auto.tfvars` for `PVE_API_HOST` | A reformat silently empties the Prometheus PVE target; nothing alerts | Add the VIP to `inv_env` |
| M6 (#39) | `ansible/roles/apt_proxy/templates/apt-proxy-detect.sh.j2:1-12`, `terraform/modules/proxmox-vm/templates/user-data.yaml.tmpl:26-42` | Byte-identical probe in two layers, nothing pins them | Divergence persists until the next fleet-wide VM roll | A render-and-diff check in `make` |
| M7 (#26) | `terraform/talos.tf:56` and 7 more | bpg `proxmox_virtual_environment_download_file` deprecated, removed in v1.0 | A provider bump breaks the plan | `proxmox_download_file` |

## Tranche plan (2026-09-10)

Open reviewer-filed issues at phase start: 36. Closed as already fixed by WP0–WP8: #7 (B3, B10), #11 (B7, B10), #19 (S8), #32 (M2), #34 (M4, M5). Drain rule: the phase does not close while the open count exceeds 36; after the stale closures it is 31. **Closed 2026-09-10: open reviewer-filed count 0** (tranche 4 closed the last four).

- **Tranche 1 — fix-order steps 1–3 — DONE 2026-09-10:** #5 CI workflow; #6 (B1 + B8 + B11, the silent-regeneration chain); #8 (B2 + B12); #9 (B4); #15 (S1, with #9 — same role); #16 (S2); #18 (S5, found already fixed); #20 (S3, the ADR only — ADR 0050; implementation → tranche 2). `PARALLEL: no` — solo loop, direct commits (the operator's chosen mode for the IaC work; no PRs opened).
- **Tranche 2 — security tail + retired-guest sweep — DONE 2026-09-10:** #24 (723a27f); #20 (5dfff08, ADR 0050 Accepted after the live pin: cross-host `nsupdate` REFUSED from proxmox-backup, forced renewals on ms-01b/PBS/PDM pass); #17 (14176fd); #21 (12a7441); #22 (6604456); #23 (7edcee6 + the k8s_seed follow-up); #10 (a758922); #14 (f275ed4); #13 (c59d643); #12 (d95b993); #40 (3e0d8b4); #41 (05f44ee). Solo loop, direct commits. **Deviations:** the three hand-enrolled ACME clients (pfSense, DSM, Home Assistant) still hold the retired shared key until the operator re-keys them, and `/infrastructure/acme_tsig_key_secret` is deleted by hand after that (operator-hand-steps.md); #21 could not land as written: the IP allowlist is an Infisical paid-plan feature (the live PATCH answered 400 `plan restriction`), so the identity keeps `0.0.0.0/0` and the accepted control is network reach (12a7441 reverted by the follow-up commit; recorded in CLAUDE.md); the two Infisical CLI user logins in `bootstrap_infisical_setup.yml` and pvesm's `--password` stay on argv (no stdin path — SRP login and a tty read), recorded on #17; the UniFi user half of `monitoring_users` and the (long commented-out) synology include were not exercised live; the adguard bump-and-restart half was not exercised (the fleet already runs the newest release). **Found en route:** the VPS telegraf had been `crashed` with no outputs (Alpine's `/etc/telegraf.conf.d` vs the role's `/etc/telegraf/telegraf.d`) — every run restarted it into the same crash; an OpenRC status is the word `started` to `ansible.builtin.service`; Homebrew bash 5.3 blocks on a heredoc after an env-prefixed command; a target-specific `:=` in make expands per listed target at parse time (150 sops calls).
- **Tranche 3 — scaffolding + dedupe — DONE 2026-09-10:** #36 (closed by verification, 7178060: `plex_certificate` already rides `cert_client_issuer: letsencrypt`, 0 changed, forced renewal proven; the "new sibling" line was in ADR 0044, not 0045); #37 + #31 (f9fcf1b: one `kubernetes/pg-backup/` tree parameterized by Flux `postBuild.substitute`, requests on the four containers, `flux_check` fails a request-less container, `make k8s-update` serialized; the CONFIG_HASH half was already per-consumer name-hashing); #25 (238336f); #26 (c7bab0a); #33 (2a3ac77); #39 (82c7835); #38 (83b6ddd); #35 (8e9c658). Solo loop, direct commits. **Deviations / decisions:** `public_policy.yaml` was deleted rather than kept yaml-only (nothing read it; the README table is the intent record); `docker-config.yml` folded into `make ansible infisical`; `guest_access_plane` kept as the rollback lever, default flipped to `services`; #26 went past its list and renamed every deprecated bpg type (`sdn_*`, `haresource` too) with `moved` blocks — a refresh-only apply recorded the 20 moves, `make plan` = No changes; `proxmox_host/tasks/repos.yml` stays outside the shared apt task (package-owned keyring — deleting a package conffile on PBS was a mistake made and undone en route); the frozen `authentik` namespace copy of the CronJob goes with the namespace. **Found en route:** this cluster's kustomize-controller v1.9 runs post-build substitution in strict mode (an unset `${VAR}` fails the Kustomization rather than blanking it); `make flux-reconcile` needed the kubeconfig in-recipe; the bindings ConfigMap needed `apply: true` to prune a removed key (the pre-existing `LB_IP` took one hand patch).
- **Tranche 4 — Ansible performance — DONE 2026-09-10:** #29 (1b64db7: `pipelining`, `forks = 20`, `gathering = smart` + a jsonfile cache under `.ansible-facts/`, dropped by `clean-ssh` so a rebuilt guest never reads hour-old facts; `make ansible-all` 395 s → 300 s, `Gathering Facts` 13 → 1 task, Phase 2 re-gathers nothing); #27 (7139f8f: the first play through `full_pre_tasks.yml` loads the vault and publishes `secrets` + the login to localhost, every later play copies it from `hostvars['localhost']`, the pre-flight play is tagged `always`; root `/` and the folders no Ansible task reads — `docker`, `plex-services`, `pfsense`, `talos` — are gone from the loop; `Login to Infisical` 14 → 1, read tasks 42 → 2, 300 s → 240 s); #28 (79b497d: `tasks/infisical_admin_token.yml` reuses the loader's session and only logs in from a play without it; `infisical_write_secret.yml` decides create-vs-update from the loaded fact and `generate_secret.yml` stores before it merges; `tasks/infisical_identity_page.yml` pages `/api/v1/identities` until a short page, pinned offline by `scripts/test_identity_paging.py` in `make validate`; live `TAGS=cleanup` deleted 12 orphaned identities of retired guests); #30 (2ae0f4f: `cache_valid_time: 3600` on the five ungated apt tasks, telegraf's per-run refresh gated on its source tasks). Solo loop, direct commits. **Deviations:** `jellyfin` stays in the loader loop — `k8s_apps` generates into it, and an unlisted folder regenerates every run; the VPS `apk` refreshes stay (no cache validity in apk); the bootstrap setup keeps its two SRP CLI logins on argv (#17) — its `vault.login` moved ahead of folder creation and its token is reused; the `sops` decrypt still runs per play (local, sub-second). **Found en route:** a task-level tag such as `dns` leaks into every play whose roles carry it (`dns_config`) while the loader's dynamic include stays skipped, so `make ansible-all TAGS=dns` dies `'dns_zones' is undefined` in the infisical play — use play tags (`phase1`, `adguard`, …); `pkgcache.bin` is rewritten by any apt run, only `/var/lib/apt/periodic/update-success-stamp` (or, on the LXCs, the lists dir mtime) records an `apt-get update`, which is also what `cache_valid_time` reads.
- **Separate gate, not a tranche:** `talos.sh` retirement (brownfield gate + worklab scratch cluster first).
