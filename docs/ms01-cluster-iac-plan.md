# MS-01 Cluster Migration — IaC Change Plan

**Status:** awaiting operator approval (brownfield gate step 4). **Date:** 2026-07-10.

Repoints this repo from the single-node `pve` hypervisor to a 3-node PVE 9 cluster (`crete`, `crete2`, rebuilt `pve`) with Ceph, brings the Proxmox hosts themselves under Terraform from first boot, and rebuilds the service fleet greenfield as static-IP LXCs / docker-on-LXC / VMs per the placement doc.

**Design inputs (canon for the ops side):** `~/okf/playbooks/ms01-cluster-migration.md` (runbook), `~/okf/brainstorm/ms01-cluster-network.md` (network design), `~/okf/brainstorm/workload-placement-vm-lxc-docker.md` (placement), `docs/audit-2026-07-07.md` (IaC gaps), [rebuild-as-routine-design.md](rebuild-as-routine-design.md) (end-state per-service state/rebuild matrix + image policy — ADRs 0015/0016).

## Decisions locked (interview 2026-07-10 + design docs)

| Decision | Choice |
|---|---|
| Host OS install | Wipe + committed `proxmox-auto-install` answer file per node; unattended PVE 9 install; Terraform takes over at first boot |
| Migration mechanism | **Greenfield rebuild everything**, carried across the cutover by a **restore-bridge** ([ADR 0024](decisions/0024-cutover-rides-a-restore-bridge-restore-the-old-fleet-onto-the-2-node-pve-9-cluster-rebuild-pve-early.md)): the old fleet is PBS-restored onto the 2-node cluster as the production bridge so pve rebuilds early; greenfield replacement then proceeds per service. Infisical re-seeded from the SOPS DR export remains the DR fallback; UniFi re-imported from cloud backup (accept re-adoption blip) |
| PBS placement | VM on the cluster, datastore on the Synology NAS (portable datastore = the asset) |
| Infisical isolation | Stays a VM (crown jewel), `protected = true`, Ceph-backed |
| Storage | Ceph `size=3, min_size=2` on local NVMe day one; cluster network on **switched 25G** — one CX-4 Lx port per node to the Pro-Aggregation's SFP28 ports, VLAN 21 access/jumbo (ADR 0014 — supersedes the switchless FRR mesh); Synology = media + PBS datastore + ISO/templates only |
| GPU | vGPU retired entirely; LLM VM on `pve` with full passthrough (driver in guest); Plex uses MS-01 iGPU QuickSync via LXC `/dev/dri` bind-mount; `nvidia-licensing` VM retired |
| Endpoint | keepalived VRRP VIP on VLAN 30 (with host mgmt; DNS name in the gitignored bindings) is the Terraform/Ansible endpoint (ADR-0008; was VLAN 40) |
| Management pane | PDM VM for human single-pane (cluster + standalone worklab); worklab stays standalone through the migration (amended: it later folds in as a non-voting compute-only member — see Longer-term) |
| LXC rule | Static IPs only — **never DHCP an LXC**. Verified 2026-07-10: bpg reads DHCP LXC IPs since v0.88, so this is convention (deterministic plan-time inventory), not workaround |
| DNS availability | Redundancy over migration: 2× AdGuard LXCs + BIND9 primary/secondary on different nodes, both resolvers handed out (LXC restart-migration verified: no live migration, blip is stop+start on Ceph) |
| UniFi | New aggregation switch configured via Terraform from first adoption (`modules/unifi-network/`, provider already in project); pfSense IaC deferred to future work (ADR 0005) |
| Repo visibility | Stays public; private IPs/domain scrubbed from tracked files going forward + pre-commit guard; history exposure accepted (ADR 0004 era values rotate anyway as the fleet rebuilds) |
| DNS-first access | Every guest/node/VIP gets a DNS record generated from the IaC inventory; names are the access layer, static IPs stay as IaC plumbing (`ansible_host` remains IP) (ADR 0006) |
| Certs | Hybrid: Infisical PKI (internal CA, ACME issuance) for the fleet with Ansible-distributed root; LE wildcard per zone for browser-facing UIs so unmanaged devices need no root install. step-ca is the fallback if self-hosted PKI is license-gated (ADR 0007) |
| Guest networking (2026-07-28) | **Single-homed on services VLAN 40** (ADR 0017): no guest VLAN 10 legs (VLAN 10 becomes the network plane — zero guests, zero client-reachable services); storage reaches LXCs via host bind mounts (`shared` mount points, fleet media UID/GID convention); PBS is the only VM keeping a VLAN 20 leg. Ceph cluster VLAN renumbered 33→21; VLAN 50 + 140 deleted; **squid retired**; AdGuard/BIND stay on 40 |
| Infisical hardening | Already gets the base hardening role; tighten during rebuild: UFW + PVE firewall to mgmt/services VLANs only, SSH from mgmt only, unattended-upgrades; consider cluster-wide Ceph OSD at-rest encryption |

## Terraform / Ansible boundary

Terraform (bpg `~> 0.111`, bumped from `~> 0.78`) manages everything the PVE API exposes; Ansible covers the bootstrap the API can't do.

| Layer | Tool | Mechanism |
|---|---|---|
| PVE install | answer file | `answer-<node>.toml` committed; `proxmox-auto-install-assistant` bakes the ISO (checklist in repo) |
| First-boot API token for Terraform | bootstrap script | one small first-boot hook (or `make node-bootstrap <node>`) creates the `terraform@pve` user + token; the only hand-off step |
| Host networking (bond0 LACP on X710s, VLAN-aware vmbr0, VLAN interfaces, MTU 9000 on storage VLAN) | **Terraform** | `network_linux_bond` / `network_linux_bridge` / `network_linux_vlan`, applied over the stable 2.5G mgmt link so the API path is never the link being reconfigured |
| Cluster create/join (**community.proxmox API modules** — ADR 0023, not `pvecm`-over-SSH), Ceph bootstrap (`pveceph install`, MON/MGR/OSD), mgmt re-home + ring/ceph interfaces, keepalived VIP | **Ansible** (new `proxmox_host` role) | create/join is API-driven; the rest is not exposed usefully via the API/provider |
| SDN zones/VNETs (all nodes), cluster options, HA groups/resources, ACME, users/tokens | **Terraform** | existing network module extended. Ceph pool + storage definitions are **Ansible-owned** (B3 boundary — bpg has no storage-definition resource; `proxmox_host` ceph.yml creates pool/CephFS + `pvesm` defs, fleet root consumes the IDs as strings) |
| VMs + LXCs | **Terraform** | existing `proxmox-vm` module + new container support |

New Terraform root **`terraform/hosts/`** with its own state for the host/cluster plane (networking, cluster options, SDN — the Ceph pool moved to the WP2 Ansible role, B3 boundary). Rationale: it must exist before the VM fleet, changes rarely, and a host-networking apply that drops connectivity must not hold the VM project's state hostage. The main `terraform/` project keeps the fleet. (ADR-0002.)

## Repo work packages

### WP0 — Day-0 safety net (ops, no code)
- vzdump every production pve VM → Synology NFS; test-restore one on an MS-01 before wiping anything.
- `make infisical-backup` → fresh SOPS DR export; verify `bootstrap.sops.yml` decrypts; **verify every key in the gap table of [infisical-external-credentials.md](infisical-external-credentials.md) is present in the export** (docker cloudflared token, github-runner app creds, homepage tokens).
- Confirm UniFi cloud backup is current; download a local copy of the `.unf` too.
- Capture `qm config <id>` for every VM + `pvesh get /cluster/resources` into `docs/pre-migration-state/` (reference, gitignore if it contains secrets — it shouldn't).
- **DoD:** one vzdump restore verified on MS-01 scratch; SOPS export decrypts; `.unf` in hand.

### WP1 — `terraform/hosts/` root + auto-install
- `answer-crete.toml` / `answer-crete2.toml` / `answer-pve.toml`: disk layout (ZFS boot mirror where applicable), initial mgmt IP on the i226-V / VLAN 30, root key.
- Host networking per node: `bond0` (X710 ×2, 802.3ad layer3+4) → VLAN-aware `vmbr0`; `terraform/hosts` puts only the **VLAN 20 storage IP** (MTU 9000) on the bridge. **i226-V** carries VLAN 30 native (install/mgmt bootstrap) + **VLAN 31 tagged (corosync ring0)**; **i226-LM** is the install NIC (native 30 — AMT rides the same native VLAN; AMT cannot tag, see [pxe plan](pxe-netboot-plan.md)) + **VLAN 31 tagged (corosync ring0)**; **i226-V** carries **VLAN 32 tagged (ring1)** (NIC roles flipped 2026-08-16 for AMT); corosync VLANs 31/32 are dedicated, switch-local on the 2.5G switch (no gateway, not trunked upstream — ADR-0008); one 25G port left for the switched Ceph VLAN 21 link (WP2, ADR 0014). Host mgmt (VLAN 30, web UI/API) + the keepalived VIP are **WP2** — Ansible re-homes mgmt from the i226-V install link to the bond (VLAN 30) and stands up the VIP there (a second VLAN 30 IP on the bond can't be set by `terraform/hosts`, which runs over that link — ADR-0002/0008). AMT is ME-side config, documented not managed. pve variant: 82599ES bond, single I225-V.
- **NIC naming: install the 25G cards BEFORE the answer-file install.** The PVE 9 installer auto-pins every present NIC by MAC via `/usr/local/lib/systemd/network/50-pmx-nicN.link` (`MACAddress=` match) — so a fresh install gives stable `nicN` names immune to later PCIe renumbering, *including the ConnectX ports* as long as the card is seated at install time. Reference the pinned `nicN` names in the answer-file/`interfaces`, not `enpXsY`. (Verified 2026-07: crete2, fresh-installed, was MAC-pinned and survived its card install untouched; crete, upgraded-from-PVE-8, kept unpinned `enpXsY` and lost all networking when its card renumbered the bus — the config still pointed at the old `enp90s0`. Upgraded nodes not being rebuilt need manual MAC `.link` pinning; pve was pinned by hand to `mgmt0/sfp0/sfp1`.)
- **Same rule protects GPU passthrough.** Adding the ConnectX card to pve renumbered its IOMMU groups, breaking the RTX 5000 resource mapping (`iommugroup` 20→21) and stopping the vGPU guests until the mapping was corrected. IOMMU groups, like NIC names and PCI addresses, are stable across reboots but shift when PCIe hardware changes — so seat every card (NIC + GPU) *before* configuring the LLM-VM passthrough. Full passthrough binds by PCI path (`0000:01:00.0`); the optional `iommugroup` pin in the PVE resource mapping can be omitted for extra robustness.
- Cluster options, ACME, `terraform@pve` ACLs once the cluster exists.
- **DoD:** fresh answer-file install of crete → `terraform apply` in `hosts/` converges with zero manual host edits; re-apply is a no-op; host reboot comes up with all planes (bond, VLANs, jumbo `ping -M do -s 8972` to the NAS).

### WP2 — Ansible `proxmox_host` role
- Idempotent cluster create/join via `community.proxmox.proxmox_cluster` over the PVE API (ADR 0023 — `pvecm add --use_ssh` proved unreliable under Ansible in W4), temporary QDevice while 2-node (drop when pve joins).
- `pveceph install` + MON/MGR/OSD creation (idempotent: check before create), keepalived VIP with `pveproxy` track_script + unicast peers.
- **Ceph network split (decide at bootstrap — ADR-0009):** `cluster` network (OSD replication) on the **switched 25G Ceph VLAN 21** (one SFP28 port per node on the Pro-Aggregation — ADR 0014, replaces the FRR mesh); `public` network (client access) on the **10G fabric** so a non-25G node (worklab, later) can be a Ceph *client*. Retrofitting this onto a live cluster is painful — set it at `pveceph init` time.
- New `proxmox` inventory group: `crete`, `crete2`, `pve` (replaces the single-host `inventory/proxmox.yaml`); `update-all.yml` and `monitoring_users` (currently hardcoding a single `proxmox_host` IP in `group_vars/all.yml:34`) go per-node.
- **NUT shutdown clients (landed 2026-08-13, pre-cutover):** `nut_client` role + `nut-clients.yml` (`make nut-clients`), deployed to the live pve + worklab against the pfSense NUT server ([pfsense-nut.md](pfsense-nut.md)). The `nut_clients` inventory group takes the cluster nodes as children (see `proxmox.example.yml`), so crete/crete2 inherit at cutover — run `make nut-clients` after WP2, no new code.
- **netconsole on every node → axosyslog UDP 5515 → OpenObserve `netconsole` stream.** Kernel printk shipped from kernel context over UDP, needing neither the filesystem nor a userspace daemon, so it is the *only* log path that survives a node losing its root disk. Motivating incident: worklab's NVMe dropped off the bus 2026-07-27 and ran ~3 days as a page-cache zombie; the kernel messages naming the cause had to be written to the disk that had just died, so root cause is still unknown and undiagnosable after the fact. Every node in a Ceph cluster carries that same failure mode with a bigger blast radius.
  - **Nodes only — an LXC cannot have its own netconsole.** netconsole is a kernel facility and containers share the host kernel, so the node's netconsole already covers every LXC on it. Since WP4 moves most of the fleet to LXC, expect this to come up; the answer is that guest-level netconsole is neither possible (LXC) nor worth it (full VMs — a guest losing its disk is already visible from the host and PBS).
  - Load late, not via `modules-load.d`: the module needs its egress interface to exist, and `vmbr0` does not at early boot. A systemd unit ordered `After=network-online.target` is correct; the tradeoff is that early-boot messages are missed, which is acceptable for runtime-failure capture.
  - **`console_loglevel` must be raised or the whole thing is theatre.** netconsole is a console driver and only transmits what passes console-level filtering; PVE ships 1–3, so NVMe/EXT4 errors at KERN_ERR (3) never leave the box. The role sets `kernel.printk = 5 4 1 7` via `/etc/sysctl.d/` — level 5 passes `err`+`warn` (the NVMe/EXT4 failure classes) and filters `notice`+`info`. Do not raise it to 7: `console_loglevel` is shared with the VT, so a late `notice` like `sd … Attached SCSI disk` overwrites the getty login prompt and leaves the console stuck on a boot message (observed on worklab 2026-07-30 after briefly running at 7). Applied ad-hoc to pve + worklab pending the role.
  - **A quiet `netconsole` stream is the healthy state — do not alert on staleness.** netconsole carries kernel printk only, so an idle host legitimately sends nothing for days; treating silence as an outage would page constantly. Alert on the *senders* (a Kuma/blackbox check per node) or on a periodic synthetic `/dev/kmsg` heartbeat, never on stream freshness. Corollary: a **manual `rmmod`/`modprobe` cycle** replays the printk ring buffer to the newly-registered console — 2026-07-30, +1034 messages on kernel 7.0.14-4-pve (worklab) but only +3 on 6.8.12-36-pve (pve). A **real boot does not replay**: measured on worklab the same day, a reboot yielded 40 messages with the earliest at 12.3 s, i.e. capture starts when `network-online.target` fires and nothing before it is recorded. Early-boot failures are therefore out of scope for this lane — that is the accepted cost of loading late, and getting it would mean an initramfs-stage netconsole with static addressing. A burst after a manual reload is correct behaviour, not an incident.
  - Off-subnet targets need the **gateway MAC** baked into the module argument — netconsole/netpoll does no ARP resolution. Derive it in the role rather than hardcoding, and treat it as a re-render trigger: a gateway MAC change silently kills transmission with no error anywhere.
  - **Dedicated port, not shared with 5514.** Ingest side landed with this plan entry (`axosyslog.conf.j2` + compose port). The first attempt routed netconsole off the shared 5514 port via parse failure and was wrong: `syslog-parser()` does not reject unframed printk, it parses it as RFC 3164 and eats the first token — verified 2026-07-30, `nvme nvme0: Removing after probe failure` landed in the `syslog` stream as `nvme0: Removing after probe failure`. Silent corruption of exactly the field that names the failing subsystem. The port is the only reliable discriminator. Regression-checked by `scripts/test_axosyslog_routing.py`.
- **Host userspace syslog → OpenObserve (`rsyslog`) — the counterpart to netconsole.** netconsole carries kernel printk only; the daemon logs that actually name a `pvestatd`/corosync/ceph failure ride userspace syslog. **PVE 9 (Debian 13) ships journald-only — no rsyslog installed by default — so the role must `apt install rsyslog` before it can forward** (confirmed 2026-08-10: worklab, a PVE 9 node, had no `rsyslogd` at all; the every-node MS-01 rebuild is PVE 9 and needs the same install). The role then deploys `99-openobserve.conf` forwarding `*.*` over **TCP/RFC 5424 to the services-VLAN axosyslog on 5514** (the `syslog` stream, same path as the VM-fleet `rsyslog_client`). Target is a site binding (`host_bindings.cluster.openobserve_syslog_target`) — the OO VM address is a guest binding, unknowable from `vlans.yaml` in this pre-guest plane — and the whole block is gated on it, so an unset site skips cleanly rather than failing the cluster build. Motivating gap: the pre-rebuild pve host had forwarded to a stale dead services-VLAN address (a long-retired guest IP) for months and nobody noticed, because only netconsole (kernel-only) was still reaching OO — so the 2026-08 `pvestatd` SEGV that greyed the UI was invisible centrally and took an SSH expedition to find. Landed in `proxmox_host/tasks/logging.yml` 2026-08-10. Node-naming caveat: worklab's hostname is `pve` (collides with the primary), so its live forward overrides HOSTNAME→`worklab`; the MS-01 nodes install with unique fqdns (`crete`/`crete2`/`pve`) so they need no override.
- **DoD:** role run twice = zero changes; `pvecm status` quorate; `ceph -s` HEALTH_OK; VIP answers on VLAN 30 and survives killing pveproxy on its holder; `echo test > /dev/kmsg` on each node lands in the OpenObserve `netconsole` stream **verbatim** (leading token intact), tagged with that node's IP, and still lands after a reboot; **`rsyslog` is installed on each node and a `logger` line lands in the OpenObserve `syslog` stream tagged with that node's hostname.**

### WP3 — Main project cluster rework ✅ code landed 2026-07-28 (DoD gates at cutover)
- Provider bump `~> 0.78` → `~> 0.111`; endpoint → the VIP; per-node `node {}` SSH blocks for file uploads.
- **Landed 2026-07-28:** pinned images (ADR 0016, `ignore_changes` shields dropped), LXC container support + shared interface/MAC builder, detached data volumes via never-started holder CT (ADR 0015), per-guest `node_name` + `ha` (haresource), SDN zones span `proxmox_nodes`, inventory carries `guest_type`/`pve_node`, vGPU plumbing deleted. Deferred to cutover as **tfvars-only changes**: endpoint → VIP, `primary_disk_storage` → Ceph RBD pool id, `virtual_environment_storage` → CephFS datastore id, `proxmox_nodes` → the three nodes. Per-node `ssh { node {} }` blocks skipped — bpg resolves node addresses via the API, which works over the VIP; add only if file uploads fail at cutover. Note: bpg has **no RBD/CephFS storage-definition resource** — storage defs come from `pveceph pool create --add_storages` / the WP2 role, and the fleet consumes storage IDs as strings.
- **Remaining live checks (cutover DoD):** clean plan against the 3-node cluster; test LXC + VM per node on Ceph; data-volume guest rebuild preserves contents; cross-VMID CT mount accepted by the API (ADR 0015 fallback: `pvesh`-alloc script).
- `vm_configurations` schema gains `node_name` (placement) + `ha` (bool); HA resources for Ceph-backed guests; LLM VM pinned to pve, excluded from HA.
- Storage: default disk storage → Ceph RBD pool (`proxmox_ceph_pool` + storage def); cloud-init snippets → a small CephFS datastore (shared, so snippet upload is node-agnostic).
- SDN: `nodes = [all three]` (fixes `modules/network/sdn.tf:13`); bridge references updated to the new `vmbr0` model.
- Delete `pci.tf`, `gpu_mapping`, `needs_gpu`, mediated-device plumbing.
- LXC support: `proxmox_virtual_environment_container` alongside VMs in the module (static IP via `initialization.ip_config`, `nesting`/`keyctl` flags for docker hosts, `/dev/dri` device passthrough for Plex); inventory output gains guest type + node.
- **Detached data volumes (ADR 0015):** module support for a Ceph RBD data volume whose lifecycle is independent of the guest (rebuild = destroy guest, keep volume, reattach). Gate item: verify bpg can attach a pre-existing volume to an LXC `mount_point` and a VM disk; fallback is an API/`pvesm`-created volume referenced by ID.
- **Pinned images (ADR 0016):** image resources pinned by version URL + checksum on shared storage, replacing the `latest` download; drop the cloud-init `ignore_changes` drift shields.
- **DoD:** `terraform plan` clean against the live 3-node cluster; a test LXC + test VM deploy on each node, land on Ceph, and appear in `vms.yaml` with correct static IPs; a test guest with a data volume rebuilds with the volume's contents intact.

### WP4 — Fleet rebuild definitions
**Deliberately NOT pre-written:** swapping `vm-configs.tf` to the cluster fleet before the cluster exists would make any apply against live pve propose destroying production guests (today's replace-all drift is bad enough). The new fleet definitions are written at cutover, when the endpoint already points at the cluster.
Placement per the okf doc: **VMs** — infisical (protected), pfsense-test, github-runner, proxmox-backup, unifi, LLM (new). **LXCs** — plex (iGPU), adguard ×2, dns ×2 (BIND primary/secondary), lancache, homepage, mcp, minio, doge. **docker-on-LXC** (nesting) — plex-services, openobserve, docker-legacy. **Retired** — nvidia-licensing, **squid** (its openclaw interception VLAN 140 is deleted — ADR 0017).
- Network contract (ADR 0017): every guest `vlans = ["vlan40"]` (PBS adds vlan20); host role mounts NAS exports on all nodes and LXCs get `shared` bind mount points; pfSense manual steps — admin-port rules on 40, remove VLAN 50/140 interfaces.
- **Default-route trap on PBS's two-legged config — fixed pre-emptively in W6, do not regress.** With `management_vlan` becoming vlan40, PBS's `["vlan40","vlan20"]` leaves vlan20 as the only non-management VLAN, and VLAN 20 has no router (ARP at `.1` is INCOMPLETE). The module now skips gateway-less VLANs when choosing the default-route leg, driven by an explicit `gateway: null` on `storage` in `vlans.yaml`. **That line lives in a gitignored file** — if `vlans.yaml` is ever regenerated or restored from elsewhere, re-add it or PBS boots with no default route, installs nothing, and terraform hangs waiting for the guest agent ([W6 results](pre-migration-state/worklab-campaign-w6-results.md) finding 2).
- **Image rolls (both proven on worklab, W6):**
  - **PBS 4 on Debian 13** (ADR 0025): set the PBS guest's `image = "debian13"` in `var.cloud_images` — the repo suite needs no action, it derives from the guest (below). Proven: PBS 4.2.5 green + idempotent on a Debian 13 cloud image. PVE's iso store rejects `.qcow2`, so the pin renames `file_name` to `.img`.
  - **Fleet noble → resolute (Ubuntu 26.04) — DONE 2026-08-21:** `var.cloud_image` pinned to resolute 20260731 and `var.lxc_template` to `ubuntu-26.04-standard_26.04-1` (sha512 computed from a direct download — aplinfo exposes none); homepage 211 and infisical 205 rebuilt on it the same day (`make rebuild`, then the infisical re-seed chain). Original note: roll `var.cloud_image`, and `lxc_template` too — `ubuntu-26.04-standard` exists upstream, so the container half is not blocked. No suite flip is needed: resolute ships `libfuse3-4` only and the bookworm client needs `libfuse3-3`, which would have broken every guest's in-guest backup — so the suite is now derived per host instead of being a value someone has to flip at the right moment. Docker on resolute is clean (29.7.2 / Compose v5.4.0).
  - **The suites are DERIVED, not flipped (changed in W6 review round 3b — do not reintroduce a manual flip).** `pbs_repo_suite` and `pbs_client_repo_suite` evaluate from each guest's own `ansible_distribution` facts. A global scalar was wrong in both directions during a roll: `make rebuild <guest>` moves guests one at a time, so the fleet is MIXED throughout, and `bookworm` breaks the first resolute guest while `trixie` breaks every host still on noble — fleet-wide, since `pbs_client` runs in `infrastructure.yml` plus eight service plays. Verified across all four shapes: Ubuntu 24.04 → bookworm; Ubuntu 26.04 VM, Ubuntu 26.04 LXC and Debian 13 → trixie.
  - The LXC template rolls too and was proven on worklab: only the container replaced, the ADR 0015 data volume and ADR 0017 bind mount came back intact.
- **CLOSED 2026-08-21 by [ADR 0029](decisions/0029-adguard-is-an-lxc-pair-behind-a-keepalived-vip-container-resolv-conf-is-terraform-owned-adguard-only-except-on-the-resolver-cts.md)** — CT nameserver list is per-guest in `containers.tf`: VIP only, plus the public failover on `resolver = true` guests. Original problem statement kept below. ~~**OPEN (ADR 0012 gap on containers) — decide before the LXC fleet is built.**~~ `dns_config` is a VM-only mechanism: its drop-in targets the netplan-generated `10-netplan-<iface>` unit, which containers do not have, and its `netplan apply` handler cannot run in one. It is now guarded to skip on LXC, so plays no longer fail — but **nothing then removes the public bootstrap resolver from a container**, because an LXC's `/etc/resolv.conf` is written by PVE from `containers.tf`'s `dns { servers = var.dns_servers }`, i.e. the full AdGuard-plus-failover list. With most of WP4's fleet on LXC this reinstates exactly the poisoning ADR 0012 prevents. Complication: AdGuard itself is an LXC, so containers cannot simply be given an AdGuard-only list without a bootstrap story ([W6 results](pre-migration-state/worklab-campaign-w6-results.md) finding 11).
  **Deliberately deferred to WP4 (operator, 2026-08-12) — but two facts are already settled, do not re-derive them:** (a) **PVE rewrites a container's `/etc/resolv.conf` on every start** (measured), so an Ansible-written resolv.conf is reboot-transient — the same trap ADR 0012 rejects for `resolvectl`. The only durable control point is the CT `nameserver` setting in `containers.tf`, i.e. Terraform. (b) The live options are therefore: a CT-only resolver list (`lxc_dns_servers`, AdGuard-only, VMs keep the failover for cloud-init), dropping the public failover fleet-wide, or a two-phase build that narrows the CT nameserver after DNS is up. Each needs an answer for how the AdGuard CTs themselves resolve before AdGuard exists.
- **Ansible inventory must carry a per-shape `ansible_user`** — VMs use the cloud-init account, LXCs are root-only by module design (W6 finding 13).
- DNS redundancy (ADR 0003): the two AdGuard instances get identical IaC config; BIND secondary via zone transfer; instances pinned to different nodes via `node_name`; pfSense DHCP hands out both resolver IPs (manual step this round, documented).
- DNS-first (ADR 0006): `dns` role templates BIND zone records from the Terraform inventory + node/VIP list — every guest gets a name automatically; AdGuard forwards internal zones to BIND; homepage/monitoring/inter-service configs use names, not IPs.
- Infisical hardening tightening: UFW + PVE firewall scoped to mgmt/services VLANs, SSH from mgmt only, unattended-upgrades enabled.
- Ansible roles port as-is (SSH is SSH); expected touch-ups: anything assuming a full VM (qemu-guest-agent tasks, kernel/sysctl tasks like the SABnzbd NFS tuning move to the host or get `when: not lxc` guards), plex role gains the `/dev/dri` render group bits.
- Stateful-data checklist (see below) drives per-service restore steps; the **end-state** home for each service's data is the matrix in [rebuild-as-routine-design.md](rebuild-as-routine-design.md) — cutover copy-outs land directly onto the ADR 0015 data volumes.
- End-state items from the matrix: per-service volume definitions; pg_dump timer on plex-services (writes to its volume so PBS snapshots hold a consistent dump); AdGuard admin password seeded into Infisical **before** the first AdGuard build; OpenObserve/Prometheus/Kuma data dirs on the openobserve volume (parquet + metadata.sqlite together).
- **DoD:** `make apply` from a clean clone (+ SOPS key) converges the whole fleet; second run zero changes; per-service smoke checks pass (DNS resolves, Plex plays with QuickSync transcode, arr stack healthy, monitoring ingesting); `make rebuild` of each Class-V service preserves its state, and rebuilding either instance of a DNS pair causes zero resolution failures observed from a client.

### WP5 — UniFi network module (ADR 0005)
- `modules/unifi-network/` in the main project: `unifi_network` resources from the gitignored `network-data/vlans.yaml` (creates the UniFi-only L2 VLANs — corosync 31/32 + ceph 21; dual-managed VLANs referenced via data sources), port profiles (node-bond trunks, NAS LAG, corosync + ceph SFP28 access — ADR 0014), port overrides + LACP aggregation for the new switch, device-level jumbo. Port-to-device assignments in gitignored `network-data/local/unifi-ports.yaml`.
- **Logging settings via `unifi_setting.syslog`** — the controller exposes remote syslog and netconsole as two *separate* destinations, and they must stay separate: `enabled`/`ip`/`port` → axosyslog **5514** (parsed into the `syslog` stream), `netconsole_enabled`/`netconsole_host`/`netconsole_port` → axosyslog **5515** (raw, into the `netconsole` stream). Pointing UniFi's ordinary syslog at 5515 would strand it in the unparsed stream with no field extraction. Provider 0.55 omits unset `port`/`netconsole_port` rather than serializing `0`, which the controller rejects with `400 api.err.InvalidPayload` — set both explicitly regardless, since the default is stale-prone. This is the only netconsole source in the fleet that is not a PVE node: UniFi APs/switches/gateways run Linux and the controller pushes the netconsole config down to them.
- Scope: the new aggregation switch + migration-touched ports only; full-fabric import is future work. Lives in a **separate `terraform/unifi/` root** (`make unifi-plan`/`unifi-apply`) because provider `~> 0.55` connects to the controller at plan time — it must not gate routine fleet plans (ADR-0002 rationale). Prerequisite: seed `unifi_admin_password` in `bootstrap.sops.yml` (currently unfilled).
- **DoD:** switch adopted → `terraform apply` produces the complete working switch config with zero hand edits in the controller; re-apply is a no-op; node bonds negotiate LACP; jumbo validated end-to-end.

### WP6 — Public-repo scrub + guard ✅ (landed 2026-07-27)
- Repo is public; 15 tracked files carried private IPs, 6 the internal domain (scan 2026-07-10) — violating the policy-vs-binding rule. Operator decision: keep public, scrub going forward, accept history exposure.
- Done: concrete IPs/domains moved out of role defaults, templates, and docs into derived vars (`network_data.*` facts, `internal_network_cidr`, `vps_wg_tunnel.*`) or neutral placeholders; `archive/` and `scripts/nvidia-vgpu-project-prompt.md` deleted.
- Guard: `scripts/security_guardrails.sh` (existing pre-commit hook) now scans **added lines** for RFC 1918 addresses + the internal domain (read at runtime from gitignored `vlans.yaml`). Allowlisted: `*example*` files and the generic supernet literals (10.0.0.0/8, <private ip>/12, 192.168.0.0/16).
- Scope call (confirmed): hostnames and VLAN *numbers* stay tracked (structural); IPs, subnets, and domain names are bindings. Example files keep their illustrative 172.16.x values under the allowlist — tighten to RFC 5737 later if the operator wants.
- **DoD met 2026-07-27:** leak scan zero hits over tracked files; hook verified rejecting a staged file containing an internal IP and passing the clean stage.

### WP8 — PKI + certs (ADR 0007) — cluster CA swap ✅ (landed 2026-08-24, [ADR 0039](decisions/0039-wp8-an-infisical-internal-root-signs-cert-manager-s-homelab-ca-intermediate-acme-http-01-stays-for-per-host-certs-the-pbs-postgres-dump-is-the-ca-s-dr-path.md)); per-host ACME / root distribution / LE wildcard still open
- **Gate first:** verify Infisical PKI (internal CA + ACME) is available on the self-hosted tier and that `make infisical-backup` captures PKI objects; if gated → step-ca LXC, same shape.
- **Gate verdict (probed 2026-08-24, VM 205 `infisical/infisical:latest` = `INFISICAL_PLATFORM_VERSION=v0.162.22`, image built 2026-08-20, self-hosted with no license key — `GET /api/v1/organizations/<org>/plan` → `tier: -1`, `slug: null`):**
  - **PKI: YES, not gated.** PKI is the separate *Certificate Manager* project type (`POST /api/v2/workspace {type: cert-manager}` succeeded from the fleet machine identity; the `pki/*` routes answer `Invalid Certificate Manager project` against the `secret-manager` project, not 403/404). Plan flags: `pkiAcme: true`, `pkiEst: false`, `pkiScep: false`, `caCrl: false`, `maxInternalCas: null` (unbounded). The OpenAPI spec exposes `/api/v1/cert-manager/ca/internal` (root/intermediate, `keySource: infisical|hsm`, EC/RSA/PQC algorithms), `/ca/internal/{caId}/sign-intermediate` (signs an external CSR — the CA key never leaves Infisical), `/ca/internal/{caId}/csr`, `certificate-policies`, `certificate-profiles`, and an ACME server at `/api/v1/cert-manager/acme/profiles/{profileId}/directory` with EAB. **ACME is HTTP-01 only** (docs: DNS-01 "planned"), so it cannot issue the Traefik wildcard — the cluster's CA hand-off rides `sign-intermediate`, ACME is for per-host certs later (nodes, guests). The step-ca fallback is not needed.
  - **Backup coverage: NO today, on two counts.** (1) `make infisical-backup` exports secret folders only (`scripts/infisical_backup.sh` FOLDERS list) — PKI objects live in Infisical's Postgres, KMS-encrypted under `ENCRYPTION_KEY`, and internal-CA private keys are non-exportable by design, so the SOPS export can never carry them. (2) The lane that *does* cover them — the nightly `infisical_pbs_backup.sh` cron (pg_dump + `ENCRYPTION_KEY`/`AUTH_SECRET` → PBS ns `databases`) — has **never succeeded**: `/var/log/infisical_pbs_backup.log` holds only `Failed to create PBS namespace` since 2026-08-22, because `/etc/proxmox-backup/pbs-client.env` was rendered 2026-08-23 02:29 with **empty** `PBS_PASSWORD`/`PBS_FINGERPRINT` baked fallbacks (the `TAGS=pbs_client` no-facts trap in CLAUDE.md) and 205 has no Infisical agent to fall back on (the vault does not run `infisical_client`). `/shared/pbs_fingerprint` matches the live PBS 201 cert, so `make ansible infisical TAGS=phase1` re-renders it; the DoD for coverage is a fresh `infisical` snapshot in ns `databases` listed by PBS, then a restore rehearsal of the dump on worklab. The ADR 0007 requirement ("CA key material must be covered by `make infisical-backup`") is therefore amended: the PBS DB dump is the CA's DR path; the SOPS export stays secrets-only.
  - Also found: `kubernetes/lib.sh` `inf_get` works under bash (the 2026-08-24 "CLI error" was zsh — `BASH_SOURCE` unset → wrong `ROOT`); the repo-root `.infisical.json` carries a stale `workspaceId` (`15d72dca…`, deleted project) that the CLI reads when `--projectId` is absent. The probe also created and deleted an empty-named `cert-manager` project (`f980773d…`).
- **Change plan (interview 2026-08-24 → [ADR 0039](decisions/0039-wp8-an-infisical-internal-root-signs-cert-manager-s-homelab-ca-intermediate-acme-http-01-stays-for-per-host-certs-the-pbs-postgres-dump-is-the-ca-s-dr-path.md)):** the cluster keeps its `ca` ClusterIssuer; only where `homelab-ca`'s certificate comes from changes.
  - *Scope:* `kubernetes/cert-manager/deploy.sh` + `issuer.yaml` (the `selfsigned` issuer and the self-signed CA `Certificate` go away), one `inf_token` helper in `kubernetes/lib.sh`. Nothing else in the tree changes — `traefik/certificate.yaml`, `zot/certificate.yaml`, `talos.sh`'s trust patch and the Makefile targets are untouched.
  - *Infisical side (created by `deploy.sh` through the API with the fleet machine identity, idempotent by name):* Certificate Manager project `homelab-pki` → internal root CA **`Homelab Root CA`**, `EC_secp384r1`, 20 years, `maxPathLength: 1`, `keySource: infisical`. No certificate profile / ACME enrollment yet — that is the per-host phase.
  - *Cluster side:* if the `homelab-ca` secret is absent or its `tls.crt` is self-signed, `deploy.sh` generates an EC P-256 key + CSR (`CN=homelab-ca`, openssl on the workstation), POSTs `/api/v1/cert-manager/ca/internal/{caId}/sign-intermediate` (5 years, `maxPathLength: 0`), and writes `tls.key` / `tls.crt` (intermediate) / `ca.crt` (root) into the secret; the `homelab-ca` ClusterIssuer picks it up unchanged. `kubernetes/.secrets/homelab-ca.crt` becomes the **root**. The intermediate key is disposable state — on cluster loss the script re-signs a fresh one and every client keeps trusting the root — so it is not exported anywhere.
  - *Re-issue:* delete `traefik/wildcard-tls` and `zot/zot-tls` (cert-manager re-issues from the new chain in seconds), `rollout restart` zot + traefik; then `make talos-trust` (nodes trust the root — a ~2 min Zot-pull gap while the node configs roll, running pods unaffected) and `make registry-smoke`. Operator follow-ups: `~/.docker/certs.d/registry.<domain>/ca.crt` and the macOS keychain get the new root.
  - *Sequence:* (0) fix the DR lane first — `make ansible infisical TAGS=phase1`, run `infisical_pbs_backup.sh` once, prove a fresh `infisical` snapshot in PBS ns `databases`; (1) commit the script/manifest change; (2) build + verify; (3) ADR Accepted, plan row, CLAUDE.md CA sentence.
  - *DoD:* `kubectl get clusterissuer homelab-ca` Ready with the `selfsigned` issuer gone; `openssl s_client -connect <.65>:443 -servername homepage.<domain>` with `-CAfile kubernetes/.secrets/homelab-ca.crt` returns `Verify return code: 0` and shows `i:CN=homelab-ca` / root `CN=Homelab Root CA` (today: code 21, self-signed); `make talos-trust` + `make registry-smoke` PASS; `make plan` + `make talos-plan` clean; PBS lists a `databases/infisical` snapshot newer than the fix.
  - **Built 2026-08-24:** Certificate Manager project `homelab-pki`, root CA id `7091d003…` (`O=homelab, CN=Homelab Root CA`, secp384r1, expires 2046-08-19, pathlen 1); intermediate `CN=homelab-ca` (prime256v1, expires 2031-08-23, pathlen 0) in the cluster secret; `wildcard-tls` + `zot-tls` re-issued; `openssl s_client` to `.65` and to `registry.<domain>` → `Verify return code: 0 (ok)` with the exported root (was 21 self-signed); `make talos-trust` applied on all three nodes (the Traefik replica that rolled inside the gap sat in `ImagePullBackOff` until trust landed — deleted once, re-pulled); `make registry-smoke` PASS; `make plan` = the same 2 Cloudflare IPv6 updates as before, `make talos-plan` clean. Step 0: `load_secrets.yml` skip made reachability-gated, `make ansible infisical TAGS=phase1` re-rendered `pbs-client.env` (`PBS Connection: SUCCESS`), manual run produced `databases/infisical` snapshot `2026-08-24T15:25:14Z` (4.3 MB). Also fixed en route: the cert-manager chart's kube-system leader-election objects vs `helm_apply -n` (`global.leaderElection.namespace`; the orphaned kube-system Role/RoleBindings deleted by hand). Operator follow-ups: install the new root in `~/.docker/certs.d/registry.<domain>/ca.crt` + the macOS keychain (drop the old self-signed one); restore-rehearse the Infisical dump on worklab.
  - *Deferred (not this session):* per-host ACME (certificate profile + EAB → PVE native ACME via bpg, `cert_client` role), the fleet root-distribution role, the LE wildcard generalization, and ADR 0022's ref-templating (now unblocked once the swap lands).
- Infisical PKI: internal CA + cert profiles; PVE node certs via native ACME pointed at the internal directory (bpg ACME resources in `terraform/hosts/`); guest certs via a `cert_client` role (lego/certbot, ACME against Infisical).
- Root CA distribution: common role installs the internal root into every managed guest/node trust store.
- LE wildcard per internal zone: generalize `plex_certificate` (DNS-01, existing Cloudflare token), renew centrally, deliver via the Infisical-agent file pattern to browser-facing services (homepage, plex, arr UIs).
- **DoD:** `curl https://<any-fleet-name>` verifies from a managed guest with no `-k`; browser-facing UIs show trusted certs on an unmanaged device (no root installed); renewal dry-runs pass for both planes.

### WP7 — Docs + guardrails
- README: new prerequisites (answer-file/ISO step, `terraform/hosts/` root, node bootstrap), updated make targets.
- CLAUDE.md: cluster architecture, LXC conventions, **add to What Never To Do:** "Never DHCP an LXC"; "Never reconfigure host networking over the interface carrying the PVE API session".
- Retire `scripts/nvidia-vgpu-project-prompt.md` (obsolete with vGPU gone); keep the packer golden-image plan (orthogonal, still valid).

## Sequencing (interleaved with hardware/ops)

| When | Work |
|---|---|
| Day 0 (today) | WP0 backups (incl. credential-gap verification). Rack + adopt the new SFP+ switch (USW-Pro-Aggregation — purchased, awaiting install), then **WP5 UniFi apply** configures it (VLANs incl. Ceph 21, port profiles, LACP, jumbo) before cabling. Install 25G NICs + cable each node's 25G port to an SFP28 switch port (ADR 0014) + 10G links on the MS-01s (their VMs are disposable — power off freely). Write WP1+WP2 code against the docs. |
| Day 1 | Wipe crete/crete2 → answer-file install → `hosts/` apply → `proxmox_host` role → 2-node cluster (+ temp QDevice). Verify DoD gates. |
| Day 1–2 | **Restore-bridge ([ADR 0024](decisions/0024-cutover-rides-a-restore-bridge-restore-the-old-fleet-onto-the-2-node-pve-9-cluster-rebuild-pve-early.md), replaces the greenfield-backbone-first order):** final PBS backup pass on pve, then restore the old fleet as-is onto MS-01 local ZFS (retiring VMs excluded — lancache etc.), stopping each old VM as its restored copy comes up. Verify the restored fleet runs. Prereqs: vlan10/40 live on the MS-01 trunks (WP5); actual-usage capacity check vs MS-01 free ZFS. |
| Day 2 | **DONE 2026-08-20** — msi (ex-pve) PXE-installed, joined as node 3, QDevice dropped, Ceph bootstrapped HEALTH_OK (4 OSDs across 3 hosts, size=3), VIP live. msi runs turbo-capped on the RMA-pending 14900K. Original notes: Nothing left needed on pve → wipe pve (25G NIC **already seated** — verified 2026-08-11; GPU stays), answer-file install, join cluster, drop QDevice, Ceph bootstrap + pool, validate the 25G ceph VLAN/`HEALTH_OK`. Keepalived VIP up; repoint endpoint at it. pve ring quirk: ONE copper NIC (I225-V `mgmt0`) → ring0 on it (48-port, VLAN 31 tagged over native 30); **ring1 = VLAN 32 subif on the bond** (Pro-Agg trunk carries it) — rings deliberately cross different switches. |
| Day 2–3 | **Disk move DONE 2026-08-20** — all 16 restored guests live-moved bridge → `ceph-rbd` (`qm move-disk` + `qm set --ide2 ceph-rbd:cloudinit`, ~6 min total, no guest outage), `bridge` storage + pools destroyed, Crucial `-part4` added as OSDs (8 total, HEALTH_OK; partitions had to be retyped `BF01`→`8300` first — pveceph treats a ZFS-typed partition as in use). Remaining: greenfield-replace at leisure per the runbook below (ADR 0028): `make bootstrap` (adguard + infisical), `make infisical-seed`, dns LXC, unifi VM + cloud re-import, rest of the fleet in final form (WP3/WP4). Stateful restores per checklist. |
| Day 3+ | LLM VM (vfio passthrough, pinned), HA resources, PDM VM, monitoring per-node, PKI + certs (WP8), repo scrub + guard (WP6), docs (WP7), decommission sweep. |
| Post-migration program (decided 2026-08-22) | See [Post-migration change plan](#post-migration-change-plan-decided-2026-08-22): ADR 0030 management plane, ADR 0031 Kubernetes services plane, ADR 0032 CI sandbox. |

## Post-migration change plan (decided 2026-08-22)

Status snapshot and the unhomed-services inventory that produced this: [migration-remaining-2026-08-22.md](migration-remaining-2026-08-22.md). Decisions: [ADR 0030](decisions/0030-three-management-planes-vlan10-out-of-band-vlan30-hypervisor-with-pdm-pbs-apt-cache-pxe-vlan40-services.md), [ADR 0031](decisions/0031-a-three-node-talos-kubernetes-cluster-becomes-the-services-plane-and-the-bootstrap-tier-stays-on-proxmox.md), [ADR 0032](decisions/0032-ci-runners-are-ephemeral-and-build-their-own-guests-inside-a-pve-pool-on-an-isolated-vlan-and-the-mac-studio-is-a-separate-trust-tier.md). Ordering is strict; each phase is its own approval gate and its own set of small commits.

| Phase | Work | DoD |
|---|---|---|
| P0 — close out the Proxmox replaces | `backup_jobs` + `make backup-jobs` **done 2026-08-22**; pfSense `config.xml` DR documented ([pfsense-config-backup.md](pfsense-config-backup.md)). **No more greenfield-replaces for k8s-bound guests** (operator, 2026-08-22): github-runner 113 and mcp 115 stay as they are until they move to Talos. `backup_jobs` re-authored + applied; Libation into plex-services 206 — **built 2026-08-22**, operator `login-external` pending ([runbook](libation-audiobook-sync-plan.md#built-2026-08-22-p0-of-the-post-migration-plan)); drop `pfsense-test` + `doge` from `vm-configs.tf`; pfSense `config.xml` export cron documented | nightly PBS snapshot present for every live VMID; second `make build plex-services` = 0 changed |
| P1 — management planes (ADR 0030) | **COMPLETE 2026-08-23 except the SB conversion (hand).** `control` LXC 212 on vlan10 DONE 2026-08-22 (MeshCentral + Portainer server on data volume idx 5; agents on plex-services/docker/openobserve/homepage registered; `portainer_api_key` now role-written into `/homepage` and the widget works; rewrites `control`/`portainer`/`meshcentral.core` added live; homepage dropped Portainer). Operator: create the first MeshCentral account (becomes site admin), then set `meshcentral_new_accounts: false` and `make ansible control`; **apt-cache 216 + pxe 217 built on vlan30 2026-08-22** (`.30.16`/`.30.17`, `backup_jobs` repointed; `vlans.yaml` `infrastructure.bridge` is now `vmbr0` = tagged port, and the module falls back to a guest's first leg for `ansible_host` when it has neither a services nor a management leg). **Operator:** repoint pfSense netboot to 217 per [pfsense-netboot.md](pfsense-netboot.md) (Next Server, option 250, the TFTP/HTTP rule), then stop 116/117 `onboot 0`. **PDM VM 220 built on vlan30 2026-08-22** (`.30.20`, `https://pdm.core.<domain>:8443`, login `root@pam` with Infisical `/control/pdm_root_password`; `pdm` role installs `proxmox-datacenter-manager` + `-ui` with recommends OFF — the recommended `-meta` drags in the Proxmox kernel and `grub-pc`, whose postinst fails on a cloud image). **Operator:** add the remotes in the PDM UI (cluster via the VIP, worklab standalone) — the wizard creates the PVE token from `root@pam` itself, which keeps the bootstrap-tier root password out of the role. **PBS 201 re-home to vlan30 DONE 2026-08-23** (operator: rebuild + finalize; rewrite repointed; `pbs-self` job had to be re-created — PVE drops a job with its last VMID; fleet `pbs_client` pass run). Sequence for the record: `make rebuild proxmox-backup` (single-writer LUN: the rebuild destroys the old VM before the new one logs in) → `make backup-finalize` (cluster entry → `.30.101`, new fingerprint) → `make adguard-rewrite DOMAIN=proxmox-backup.core.<domain> ANSWER=<.30.101>` (now repoints, deleting the stale answer) → `make ansible VM=<every fleet guest>` for the baked `pbs_client` fallback (`TAGS=pbs_client` is broken — no facts). vlan40 → vlan30 is already open (verified 8443/3142 from plex-services), so no new pfSense rule is needed for `:8007`; PDM VM on vlan30; pfSense plane rules (`40 → 30:8007`, named `:8006` rules); node install ports → native 10 / tagged 30, one node at a time with the SB conversion. **ms-01a port flip DONE 2026-08-23**: AMT-a re-IP'd to `.10.55` by hand, Pro-Max p34 moved to the new `tf-node-oob` profile (native Management, all tagged — the shared `tf-node-install` must stay native 30 because worklab's untagged mgmt rides p46 on it); ring0 unaffected, no host `interfaces` change needed since WP2 already left the LM as a tagged-31 carrier. **ms-01b DONE the same day** (AMT-b `.10.65`, p38 → `node_oob`); that apply also landed the Synology LAG on Pro-Agg p1+p2 the bindings had declared since 2026-08-18 (state-only drift — the controller already showed it), DSM-side bond config is the operator's follow-up. `make unifi-plan` converges to no changes. Follow-ups: MeshCentral/MeshCommander device addresses, retire the `10 → 30:16993/16995` pfSense rule, and a PXE reinstall now needs the Management-scope netboot options ([pfsense-netboot.md](pfsense-netboot.md)) | Portainer shows every docker host's containers; MeshCentral reaches AMT on both MS-01s; no vlan30/40 guest can reach vlan10 (probe from plex-services); `pbs_client` backup from a vlan40 guest succeeds; PDM shows the cluster + worklab |
| P2 — CI sandbox on Proxmox (ADR 0032) | **IaC staged 2026-08-22:** VLAN 60 `ci` in `vlans.yaml`, `terraform/hosts/iam.tf` (pool/user/token/ACLs), `proxmox_host` `ci-reaper` timer, `make sdn-apply`; hand steps in [pfsense-ci-vlan.md](pfsense-ci-vlan.md). **Applied 2026-08-22:** `Ci` VNET live on all nodes, `ci-reaper` timer active on the three nodes. IAM applied + UniFi/pfSense done + end-to-end sandbox test passed 2026-08-22 (token scope, DHCP on VLAN 60, create/destroy). **Operator:** `cd terraform/hosts && terraform output -raw ci_api_token` into the PSProxmoxVE/PBS-collection repo secrets; tighten the CI rules after the capture. **P2 otherwise complete.** | the scoped token can create/destroy in the pool and gets 403 on any other path; a CI guest cannot reach any RFC 1918 address but the internet; reaper removes a guest older than N h |
| P3 — Talos cluster (ADR 0031) | **P3a DONE 2026-08-23** ([ADR 0033](decisions/0033-talos-p3a-rides-talosctl-from-a-kubernetes-tree-with-the-factory-image-pinned-in-cloud-images-the-worklab-member-via-a-second-bpg-provider-alias-flannel-and-cluster-secrets-in-infisical.md), [plan](talos-p3a-plan.md)): `talos-cp-a/b/w` 230/231/232 pinned, no HA, v1.13.9 / k8s 1.36.2, API VIP services offset 60, etcd 3 members, ceph-csi RBD default StorageClass (smoke PASS), `kubernetes/` canonical pipeline. worklab member is the RMA-window seat (ADR 0031) — replaced by a member on msi after the swap (`talosctl etcd remove-member` + one map entry in `talos.tf`). **P3b DONE 2026-08-23** ([ADR 0034](decisions/0034-p3b-metallb-l2-on-a-reserved-vlan40-slice-zot-behind-an-internal-cert-manager-ca-arc-scale-sets-as-hostnetwork-dind-pods-pinned-to-one-node-and-a-cronjob-reaper.md), [plan](talos-p3b-plan.md)): MetalLB L2 on services offsets 64–79; cert-manager `homelab-ca` (placeholder for Infisical PKI — swap before ADR 0022's ref-templating); Zot `registry.<domain>` on `.64` with a 50 GB ceph-rbd PVC, nodes trust the CA; ARC scale sets `psproxmoxve`/`pbs-collection` (hostNetwork dind on talos-cp-a, RWO cache PVC, ci-reaper CronJob); PSProxmoxVE run [32645649835](https://github.com/GoodOlClint/PSProxmoxVE/actions/runs/32645649835) green on ARC with 5081/5082/5091/5092 created + reaped in pool `ci` (PSProxmoxVE PR #84 pending merge); github-runner 113 stopped, `onboot 0`, out of `vm-configs.tf`/backup job, role + play deleted. pfSense CI rules: [pfsense-ci-vlan.md](pfsense-ci-vlan.md). **Next: P4** | PSProxmoxVE integration job green on an ephemeral pod, nested PVE created in pool `ci` and reaped; `kubectl get nodes` stays Ready with msi powered off |
| P4 — stack migrations, one at a time | **P4a homepage DONE 2026-08-23** ([ADR 0035](decisions/0035-p4a-traefik-ingress-on-one-lb-ip-with-a-wildcard-internal-cert-the-infisical-kubernetes-operator-is-the-secret-path-and-homepage-is-the-first-zot-templated-ref.md), [plan](talos-p4a-homepage-plan.md)): Traefik on services offset 65 with a wildcard `homelab-ca` cert as the default TLS store; Infisical Kubernetes operator = the k8s secret path (one bootstrap `infisical-universal-auth` secret, `InfisicalSecret` CRDs per folder); homepage Deployment at `homepage.<domain>` + `home.<services zone>` via AdGuard rewrites, image through Zot (first ADR 0022 ref); LXC 211 stopped + `onboot 0`, out of `vm-configs.tf`/backup job, role + play + agent template deleted. Open: `monitoring@pve` does not exist on the new cluster, so the PVE widget 401s until `monitoring_users` runs post-cutover. **P4b openobserve DONE 2026-08-23** ([ADR 0036](decisions/0036-p4b-the-monitoring-stack-moves-to-the-cluster-as-one-namespace-axosyslog-keeps-a-stable-address-on-a-metallb-lb-with-local-traffic-policy-state-rides-per-service-ceph-rbd-pvcs-rsynced-from-203.md), [plan](talos-p4b-openobserve-plan.md)): `kubernetes/monitoring/` — 12 Deployments, per-service ceph-rbd PVCs (OO 120 Gi for inodes), one `InfisicalSecret` on `/monitoring`, axosyslog on a MetalLB LB at services offset 66 (`externalTrafficPolicy: Local`, netconsole source IPs preserved), UIs at `grafana|openobserve|prometheus|alertmanager|uptime-kuma.<domain>`; 42 GB of history rsynced from 203 (both alerting lanes re-proven on ntfy); fleet rsyslog/docker-logging/nodes repointed via `openobserve_listen_host` = prefix + offset 66; `monitoring@pve` recreated (`make monitoring-users`), `proxmox` scrape UP again; LXC 203 stopped + `onboot 0`, out of state/`vm-configs.tf`/backup job; `monitoring` + `openobserve_dashboards` roles, play, tag, agent templates deleted. Operator steps: pfSense remote log server + UniFi remote syslog/netconsole → offset 66 (UI), `make ansible control` for the control guest's Docker syslog driver. Open: worklab was powered off for a cable move mid-copy (18:32→18:52) and the stack landed on `talos-cp-w` when it returned; 25 zero-length `.ttv` + 10,386 zero-length parquet files from 2026-08-19 on the source make wide OO queries spanning that hour fail. **P4c plex-services DONE 2026-08-24** ([ADR 0037](decisions/0037-p4c-plex-services-moves-to-the-cluster-media-rides-a-kubelet-mounted-nfs-pv-postgres-keeps-a-pbs-dump-lane-via-a-locally-built-client-image.md), [plan](talos-p4c-plex-services-plan.md)): `kubernetes/plex-services/` — 12 Deployments, Services keep the compose names (Cloudflare tunnel routes unchanged), media via a **kubelet-mounted NFS PV** of the Synology plex export (ADR 0017 amended for the services plane — smoke-gated before migration), SABnzbd scratch a bounded 60 Gi ceph-rbd PVC, postgres dump lane re-homed as a nightly CronJob pushing pxar to PBS ns `databases` via the in-tree `proxmox-backup-client` image (`make plex-pbs-image`, the registry's first local push); 3.8 GB of state rsynced from 206, arr libraries + Libation Audible login intact, a SAB test download landed on the NAS with 2003:2000 ownership; 8 UIs at `sonarr|radarr|lidarr|prowlarr|bazarr|sabnzbd|tautulli|seerr.<domain>`; homepage widgets + the 5 Kuma rows on in-cluster svc DNS, all green; LXC 206 stopped + `onboot 0`, out of state/`vm-configs.tf`/backup job, role + play + tag + agent templates deleted. Gotchas: non-root pods need `fsGroup: 2000`; postgres mounts `subPath data/data` at PGDATA (migrated parent dir unsearchable for uid 999); SAB `host_whitelist` needed the ingress hostname + svc-DNS forms (live-config API edit). **mcp 115 retired 2026-08-24 without a successor** — the operator confirmed the MCP stack was never built out (an empty restored VM); stopped + `onboot 0`, dropped from `vm-configs.tf`/backup job, `mcp` role + play + tag deleted; the standing "3 to add" fleet-plan drift went with it. **MeshCentral does not move — ADR 0030 stands (operator, 2026-08-24):** the earlier "MeshCentral → games host" entry in this row was a plan-doc error; MeshCentral stays on the vlan10 OOB `control` guest 212, no ADR change. **P4d games host DONE 2026-08-24** ([ADR 0038](decisions/0038-p4d-the-games-host-moves-to-the-cluster-valheim-on-a-metallb-udp-lb-with-local-policy-the-playfab-status-sidecar-keyed-by-server-name-in-the-same-pod-kiwix-zims-via-an-nfs-pv-meshcentral-stays-on-control.md), [plan](talos-p4d-games-plan.md)): `kubernetes/games/` — one `valheim` pod (server + the `valheim-status` PlayFab sidecar keyed by server name + busybox httpd, liveness probe instead of autoheal) with UDP 2456–2458 on a MetalLB LB at services offset 67 (`externalTrafficPolicy: Local`), Kiwix over a read-only NFS PV of the docker export at `kiwix.<domain>`; player-gated migration (`players: 0` read from 204's sidecar at 14:46:50Z, clean stop saved `HomeWorld2.db` at 14:46:52Z, 3.8 GB rsynced) — the new pod loaded that save (636,595 zdos), registered with PlayFab (join code 418571, lobby 14:50:43Z, entity ID unchanged); Kuma row 23 green at the in-cluster sidecar (it had been DOWN against `:8080` since cutover); pfSense WG_VPS forward UDP 2456–2458 → `.67` added by the operator (it had been absent since cutover — players were joining via the PlayFab relay only); VPS DNAT unchanged. LXC 204 stopped + `onboot 0`, out of state/`vm-configs.tf`/backup job; `docker`, `authentik`, `portainer_agent` roles, play, tag, `docker.yml`, 3 agent templates deleted. Also found: `secrets.yaml` was gitignored, so the P4a–P4c `InfisicalSecret` manifests had never been committed (fixed). The join path itself (PlayFab relay + direct-IP forward) is the operator's test — no client-side check is possible synthetically. **P4 complete — every docker-LXC stack is on the cluster.** Next: P5 per the change plan (or the Parked items) | per stack: service verified from a client, old guest stopped not destroyed, matrix row flipped |
| P5 — DNS, certs, SSO, Jellyfin | **APPROVED 2026-08-24** — [ADR 0040](decisions/0040-p5-real-dns-on-a-flat-public-domain-zone-bind-fed-by-nsupdate-external-dns-adguard-forwards-rewrites-retired-let-s-encrypt-dns-01-wildcards-as-traefik-s-default-cert-two-authentik-realms-split-by-audience-jellyfin-over-the-vps-relay-with-ldap-auth.md), [plan](p5-dns-sso-jellyfin-plan.md): P5a flat zone on BIND (nsupdate + external-dns, AdGuard forwards, rewrites retired — implements ADR 0006), P5b LE DNS-01 wildcard as Traefik's default cert (ADR 0007), P5c two authentik realms, P5d Jellyfin over the VPS relay with LDAP, P5e retire the `.internal` suffix (`domain_suffix`). **P5a DONE 2026-08-25:** `<service domain>` is a flat zone on the BIND pair (initial-only shell, records = `nsupdate` from `make dns-records` / the `dns` play + external-dns for Ingress hosts), reverse zones for VLAN 30/40 added, AdGuard forwards the zone and the rewrite machinery is deleted (variables, `make adguard-rewrite`, every `deploy.sh` caller; 11 stale rewrites removed by API), Ingress hosts flipped to the service domain with a wildcard 302 for the legacy names, `vpn` mirrored from the DDNS-owned public record. **P5b DONE 2026-08-25:** Traefik's default cert is a Let's Encrypt DNS-01 wildcard (`*.<service domain>` + `*.<media domain>`, cert-manager `letsencrypt` ClusterIssuer, Cloudflare token via an `InfisicalSecret` on `/infrastructure` — the token re-homed from `/plex`); `legacy-tls` from `homelab-ca` sits beside it for the legacy SNI; cert-manager's DNS-01 self-check pinned to public resolvers (scoped ADR 0012 exception); every Kuma `ignore_tls` gone, rows on the service domain (`CHECK=1` = 0); media-domain literals replaced by the `media_domain` binding everywhere (Terraform zone by name, `jellyfin.<media domain>` records staged un-applied for P5d). Open: the operator Mac's `en0` resolver list carries the ISP's IPv6 resolvers ahead of AdGuard. Next: P5c | per-phase DoD table in the plan |
| Parked | Uptime Kuma rows: the Ansible collection for Uptime Kuma is ready (operator, 2026-08-24) — the next Kuma migration/seed pass should drive the API through it instead of the sqlite-edit pod in `deploy.sh kuma`; HA resources; LLM VM (after RMA); WP8 per-host phase (certificate profile + ACME EAB → PVE ACME via bpg, `cert_client`, root-distribution role, LE wildcard generalization — the cluster CA swap is DONE 2026-08-24, ADR 0039); Infisical DB-dump restore rehearsal on worklab; worklab fold-in | — |

## Greenfield-replace runbook (Day 3+, [ADR 0028](decisions/0028-greenfield-replace-builds-final-form-guests-beside-the-restored-copies-under-vmid-old-100-after-pruning-the-fleet-state-of-every-proxmox-resource.md))

Order: **homepage first** (stateless, cosmetic outage — the proving guest, DONE 2026-08-20 as VMID 211), then **infisical (DONE 2026-08-21 as VMID 205** — fresh `make infisical-backup`, stop 105, `make build infisical` (the Infisical-only play skips the secrets gate), `bootstrap.yml --tags infisical-setup` re-creates org/project/identity in SOPS, `make infisical-seed` 59/59 verified, `make refresh-identity FORCE=true` across the six agent hosts; no AdGuard rewrite exists for infisical, the fleet reaches it by the unchanged `.40.105` URL**) → **dns DONE 2026-08-21 as the LXC pair dns1 (218, ms-01a, primary) + dns2 (219, ms-01b, AXFR secondary), VIP = `dns_server.bind_ipv4` held by the shared `ansible/tasks/keepalived.yml`** (VMIDs are 200+ip_offset 18/19 — 13/14 would have collided with github-runner/squid's old+100 slots; flow: stage `bind9_vip_state: stopped` via `group_vars/local/all.yml` → `make build dns` → serials equal + secondary transferred → `qm set 109 --onboot 0 && qm shutdown 109` → `systemctl start keepalived` on both from the Mac → VIP up in ~4 s, AdGuard forwarding untouched; `pct stop` of the primary moved the VIP in 3.3 s with zero failed probes at 0.5 s; DDNS A+PTR verified via the VIP and forwarded through the secondary after moving primary zone files to `/var/lib/bind` — AppArmor had been denying journals in `/etc/bind`, so 109 never held a DDNS record) → unifi → rest. **unifi (DONE 2026-08-21 as VMID 200)**: decided to keep it on vlan10 at the same `mgmt_ip_offset 10` address — the controller-wide inform-host override, pfSense DHCP option 43 (`01:04:…`) and `unifi_controller_url` all key on it, so a services-VLAN move would cost every device a re-inform plus two hand-managed edits for no benefit (vlan10 has an SDN VNET; ADR 0017 exception stands). Same address ⇒ `qm set 100 --onboot 0 && qm shutdown 100` on ms-01a FIRST, then `make build unifi` (targeted plan verified: 3 adds, all unifi), then the operator restores `~/Downloads/unifi_os_backup_1787338270574_*.unifi` through the setup wizard at `https://<unifi vlan10 addr>:11443` (UOS restore is UI-only); pre-change device list: U7 Pro, Pro-Aggregation, Pro-Max-48 ONLINE (Pro 24, Aggregation, Flex 2.5G already OFFLINE). `make build unifi` applied 3 adds/0 change/0 destroy, ansible 0 failed, UOS 5.0.6 healthy on the resolute image, wizard answers `isSetup:false` at `:11443`; `qm list` ×3 diff = only 100 running→stopped (onboot 0, operator-run) + 200 added. Operator restored the UOS backup through the wizard; all three live devices came back **without re-adoption** (same address + inform override). No `unifi.<zone>` rewrite had ever existed — added `unifi` to `adguard_rewrite_vms` and landed it live with the new `make adguard-rewrite`. `make unifi-plan` shows one pre-existing drift unrelated to the restore: the Synology 10G pair on the Pro-Agg (ports 1+2) is a LAG in the gitignored ports file but two plain ports on the controller (`synology 10G b (spare)`) — reconcile by hand before the next `unifi-apply`. `backup_jobs` re-authored to the new VMIDs (200/251/252/205/218/219/211 + 117) — NOT yet applied (`jobs.cfg` is empty on the cluster; `pbs` + `nas-nfs` storage entries exist): `cd terraform && terraform apply -target=proxmox_backup_job.jobs` under the Makefile env. Monitoring redeployed on openobserve the same day — both DNS VIP probes UP, prometheus template now loops `groups['adguard']`/`groups['dns']`. **adguard DONE 2026-08-21 as the LXC pair adguard1 (251, ms-01a) + adguard2 (252, ms-01b), VIP held by keepalived** ([ADR 0029](decisions/0029-adguard-is-an-lxc-pair-behind-a-keepalived-vip-container-resolv-conf-is-terraform-owned-adguard-only-except-on-the-resolver-cts.md); flow: `make build adguard` → verify both instances + VIP → `qm set 102 --onboot 0 && qm shutdown 102` → VIP stays, Mac resolves; pfSense untouched because the VIP is the address it already hands out; scratch 202 destroyed; ms-01b's stale blocklisted cephfs mount from the 2026-08-20 SDN incident had to be remounted first). **plex DONE 2026-08-21 as LXC 208 on ms-01b (iGPU)** — the first live ADR 0015/0020 data volume: `data_volumes.plex = { index = 1, size_gb = 150 }` created holder VM 900 via the new `make data-volumes` (holder-only apply + blkid-guarded `mkfs.ext4` + chown 100000:100000 on the cluster plane — and `rmdir lost+found`, because a host-root `lost+found` is unmapped in the CT and Plex's postinst `chown -R` dies on it; the first build hit exactly that and needed a `dpkg --purge --force-all` + reinstall in the CT); media reaches the CT as a read-only bind mount of the node's `/mnt/nas/plex/data/media` (`cluster.nas_mounts` in `host-bindings.yaml`, `make proxmox-hosts TAGS=mounts`, second run 0 changed — `/volume1/media` is exported to one address, the plex export carries the same tree); `/dev/dri/renderD128` (gid 991 = the template's `render`) + `card1` (gid 44) passed through, `media_idmap = true`. Role: fixed `plex` uid 999/gid 988 (the 108 values, so the volume is portable across rebuilds), no in-guest NFS, no nvidia import, no auto PBS restore on fresh install (the Library rides the volume; restore scripts stay deployed for DR), `python3-packaging` for the pip module on 26.04. Migration: `make build plex` at temporary `ip_offset 158` → `rsync -aHAX --numeric-ids --delete` 108 → 208 over vlan40 with Plex stopped on 208 only (65 GB / 278k files, ~12 min at ~100 MB/s via `ssh -A` + `sudo -E rsync`) → `systemctl stop plexmediaserver` on 108 (0 sessions) → delta pass (380 MB) → `xmlstarlet ed -d //Preferences/@HardwareDevicePath` (108 had the NVIDIA PCI path pinned) → start. Verified before the address flip: all six sections match (1272/3/4/257/104/59), watched sample intact, `/transcode/sessions` shows `transcodeHwDecoding="vaapi" transcodeHwEncoding="vaapi" transcodeHwFullPipeline="1"` (iHD on renderD128) — the API transcode start needs the X-Plex headers, not query params (400 otherwise). Then operator `qm set 108 --onboot 0 && qm shutdown 108` → `ip_offset 108`, `make build plex` as an in-place `net0` update. Found on the way: `infisical_client` flip-flopped `/etc/infisical/secrets` 0700↔0755 every run fleet-wide (fixed: the parent-dir task skips the secrets dir), and `make inventory`'s `clean-ssh` drops host keys, so scripted `ssh` to a restored copy afterwards needs `StrictHostKeyChecking=accept-new`. **plex-services DONE 2026-08-21 as docker-LXC 206 on ms-01a** (the second live data volume: `data_volumes.plex_services = { index = 2, size_gb = 40 }` → `make data-volumes` appended `scsi1` to holder 900 and formatted it; mounted at `/opt/plex-services`, so arr configs, the postgres data dir (now a bind of `postgres/data`, no named volume) and the new `plex-services-pgdump.timer` output (`backups/pg_dumpall-<date>.sql.gz`, daily 02:00, 30-day prune — replaces the postgres PBS cron, and every PBS CT snapshot now carries a consistent dump) all ride it; media is the node's `/mnt/nas/plex/data` bind-mounted **rw** at `/mnt/data` (`/volume1/media` is exported to one address, but the stack only ever used `/volume1/plex/data`, same tree), `media_idmap = true` keeps uids 2001–2010 1:1 on NFS; SABnzbd par2 scratch is a rootfs dir (the 100 GB `extra_disk` had 129 MB on it); `keyctl = true`. Role: in-guest NFS packages/sysctl/named-volume-recreate and the first-deploy remote postgres restore are gone; `python3-docker` added — the 26.04 LXC template lacks `requests`, so `community.docker` modules fail where the cloud image worked. Migration: `make build plex-services` at temporary `ip_offset 156` (apply = 1 add, the container only; one ansible failure on the missing python lib, converged on rerun) → pre-change sample on 106 (`pg_dumpall` + config tarball parked on 106's scratch disk; row counts Series 278 / Episodes 22622 / EpisodeFiles 18015 / Movies 1287 / MovieFiles 1271 / Artists 49 / Albums 482 / Indexers 2 / Bazarr 278+1271 / Seerr media 1541) → `docker compose down` on both → `rsync -aHAX --numeric-ids --delete` of `/opt/plex-services/` (minus compose file) and the `plex-services_postgres` docker volume → `postgres/data` (3.1 GB + 0.6 GB) → `up -d`: 11/11 healthy, every count identical, SAB client / Prowlarr indexers / root folder / Plex notification intact via the APIs; the next ansible run pushed the restored API keys back to Infisical (the fresh build had seeded new ones) and the run after was 0 changed. Then operator `qm set 106 --onboot 0 && qm shutdown 106` → `ip_offset 106`, `make build plex-services` as an in-place `net0` update (1 change). The 0-changed bar exposed three flip-flops the play had carried on 106 for months: SABnzbd's `inet_exposure=4` never stuck (SAB 5 refuses exposure ≥3 without a UI password, which the role clears — task dropped) and its whitelist came back as a list compared to a string; Tautulli's `ini_file` loop used `no_extra_spaces` against a file Tautulli rewrites as `key = value`, and Tautulli rewrites `pms_token`/`http_base_url` itself (now a seed-on-fresh-install gated on the existing `pms_ip` probe); `infisical_client`'s parent-dir task pinned `root 0755` on `/opt/plex-services/recyclarr` which the service role owns as `2010:2000 0750` (now existence-only). **openobserve DONE 2026-08-21 as docker-LXC 203 on ms-01b** (third data volume: `data_volumes.openobserve = { index = 3, size_gb = 100 }` — measured 40 GB OO stream+db, 2.2 GB Prometheus TSDB, ~150 MB Grafana+Kuma — `make data-volumes` appended `scsi2` to holder 900 and formatted it; mounted at `/var/lib/monitoring`, one subdir per store: `openobserve` (parquet + `metadata.sqlite` together), `prometheus` and `alertmanager` (were named docker volumes, now bind paths owned 65534), `grafana` (472), `uptime-kuma`; the rootfs is 20 GB of compose + config). Role: one `monitoring_data_dir` default, the data-dir loop sets owners, the top-level `volumes:` block is gone; `docker-config.yml` unchanged. Migration: `make build openobserve` at temporary `ip_offset 153` (apply = 1 add, ansible 0 failed first run — the role already carried `python3-docker`) → pre-change baseline on 103 (1353 streams, `syslog` count over a July range = 1088, Kuma 22 monitors / 47.7k heartbeats from 08-10, Prometheus blocks from 08-07, schema = 104 migrations ending `m20260622_000001_add_org_id_to_short_urls`) → live `rsync -aHAX --numeric-ids --delete` of the OO tree with both stacks up (~40 min; the parquet tree is small-file bound, ~10 MB/s — and a nohup'd rsync loses the forwarded agent when the `ssh -A` session exits, so every rsync must run in a foreground session, and a nested `ssh root@ct` from the VM for `compose down` fails the same way: stop/start the CT stack from the workstation) → `docker compose down` on both → delta pass (OO 0 files, Kuma/Grafana/Prometheus ~47 MB, 2.5 min) → `up -d` on 203: `DB_SCHEMA_VERSION match`, same 1353 streams, July query = 1088, Kuma/Prometheus history intact, 38 targets up. Then operator `qm set 103 --onboot 0 && qm shutdown 103` → `ip_offset 103`, `make build openobserve` in place (net0 only; ansible 7 changed = address-derived compose/prometheus/rsyslog rewrites; next run 0 changed). Fleet senders key on `hostvars['openobserve'].ansible_host`, so the flip needed no fleet redeploy — 22 hosts shipping syslog within 5 min. `scripts/seed_uptime_kuma.py` DB path moved to the volume. **proxmox-backup DONE 2026-08-21 as VM 201 on ms-01b, PBS 4.2.5 on the pinned Debian 13 genericcloud image (ADR 0025)** — no data to copy: the datastore is the Synology iSCSI LUN, single-writer, so the flow was `qm set 101 --onboot 0 && qm shutdown 101` FIRST, then `make build proxmox-backup` (targeted plan = 3 adds, all proxmox-backup; the debian13 image was already on cephfs from pxe) at the same `ip_offset 101` and the vlan10+vlan40+vlan20 legs. Role: the datastore task now stats `<path>/.chunks` and passes `--reuse-datastore true` (PBS ≥ 3.3) so a rebuild re-attaches the LUN's datastore instead of refusing the non-empty path; the LUN mount gained `nofail`; the blkid guard kept mkfs off the formatted LUN; repo suite derived `trixie`, deb822 + keyring path, ansible 0 failed first run. Pre-rebuild baseline from 101 (PBS 3.4.9 on noble): 18 root `vm/` groups + 7 namespaced groups, fingerprint `91:37…14:b6`; on 201 the same 25 groups list intact, new fingerprint `88:46…36:a6` written to Infisical `/shared/pbs_fingerprint` with a new `pbs_backup_token` and `/monitoring/pbs_api_token` (pbs-exporter on 203 restarted by its agent hook and scrapes 201). `make backup-finalize` recreated the cluster `pbs` entry — first at the vlan40 address, because `ansible_host` is services-plane post-cutover and the play only translated the mgmt prefix; now translates services→storage too, so the entry is back on the vlan20 address and `pvesm status` shows `pbs active` on all three nodes. Fleet `pbs_env`: the Infisical agent re-rendered `pbs.env` on every agent host within a minute; `make ansible-all TAGS=pbs_client` does NOT work (play-level tags skip fact gathering → `ansible_distribution` undefined), so the baked fallback was refreshed with `make ansible VM=infisical,homepage,plex,plex-services`; `proxmox-backup-client list` succeeds from 211/208/206. `backup_jobs` applied (`jobs.cfg` = `nightly-fleet` + `pbs-self`, the latter re-pointed 101→201 in the gitignored tfvars) and a manual per-node `vzdump <local job vmids> --storage pbs --job-id nightly-fleet` landed ct/203, ct/206 and the rest (the holder VM 900 rides the job deliberately — its raw disks ARE the durable state). docker 104's agent is dead on the old Infisical project ID (stale since the 205 rebuild) — it is the pending docker→204 replace, not a PBS issue; infisical 205's `pbs-client.env` bakes an empty fingerprint because its play skips the secrets gate (it relies on nothing — no in-guest backup runs there). **docker DONE 2026-08-21 as docker-LXC 204 on ms-01a — the last docker-LXC replace** (fourth data volume: `data_volumes.docker = { index = 4, size_gb = 20 }` → `make data-volumes` appended `scsi3` to holder 900; mounted at `/opt/docker`, Valheim `config` (0.5 GB, world saves) + `data` (3.3 GB, server + in-image backups) ride it as bind paths; the kiwix ZIM library (538 GB, ro) and the dogecoin chain (222 GB, rw) stay on the NAS as node bind mounts of a new `cluster.nas_mounts` entry `/mnt/nas/docker` ← `/volume1/docker` (`make proxmox-hosts TAGS=mounts`, all three nodes). **BOINC decommissioned** (operator, 2026-08-21): service, `boinc_config` volume and `boinc_*` vars removed — restorable from git history at `58a0eb1` (last commit carrying it); the nvidia role import, container toolkit and CUDA validation went with it (no GPU on 204), as did the in-guest NFS named volumes and `docker_nfs_src`; `python3-docker` added. Two things the role could not carry: `valheim-server:playfab-test` is a local image (operator-built 2026-08-10, no registry) — moved by `docker save | gzip` on 104 → `docker load` on 204 (369 MB); and the dogecoin image's entrypoint `chown -R`s `/data` as root before dropping to uid 999, which the CT's mapped root (host 100000) cannot do on NFS — the tree was `chown -R 100999` from the node after 104 stopped and the compose service now runs `user: "999:999"` (skips the root branch). Migration: `make build docker` at temporary `ip_offset 154` (apply = 1 add; first ansible run failed on the unpullable local image, converged after the load) → rsync of the two Valheim trees from 104's docker volume mountpoints with valheim stopped on 204 only → world `HomeWorld2` loaded on 204 → logged player check (`status.json` player_count 0, 0 connects in 15 min, 23:46Z) → `docker compose down` on 104 (clean SIGINT saved the world at 23:46Z). **Incident:** the "final delta" rsync then ran against 104's *named-volume mountpoints*, which `compose down` had just unmounted — an empty source plus `--delete` emptied 204's trees and the restarted server generated a fresh 87 KB world. Nothing was lost: the NAS export is the volume's backing store, so the trees were re-synced from ms-01a's `/mnt/nas/docker/valheim/` (1631 files, name+size listing identical under `LC_ALL=C`), the fresh world discarded. Lesson recorded in CLAUDE.md. Then `qm set 104 --onboot 0 && qm shutdown 104` → `ip_offset 104`, `make build docker` in place (net0 only, ansible 1 changed = the `user:` line; next run 0 changed). `status.json` `error == null` needed one more fix: the image's `valheim-status` pins the PlayFab entity id to `/var/run/valheim/playfab-entity-id.txt`, and that id changed on every container restart here (three restarts, three ids), so the filter found no lobby after the first restart — re-pinned by hand + `supervisorctl restart valheim-status-updater`. **Open:** any future restart of the valheim container (autoheal, `UPDATE_CRON`) likely re-stales the pin and autoheal then restart-loops; that is the operator's image, not the role. `nightly-fleet` re-pointed 104→204 and applied (`jobs.cfg` verified). Agent on 204 renders `docker.env`/`docker-valheim.env` (no `valheim_discord_webhook` in Infisical `/docker`, so no join-code post). Valheim players: two `HomeWorld2` re-registrations happened during the cutover, each with a new join code — the current one is in `status.json`. **Day-after (2026-08-22):** 204 at 8 GB thrashed at `memory.high` (16 M throttle events, node load 136, Valheim offline) — NAS-bind page cache counts against the CT; now 16 GB / 4 cores. **dogecoin node decommissioned** (operator) — service, bind mount and vars removed, chain data left on `/volume1/docker/dogecoin`. Removing the `mp2` bind made bpg **replace** the CT (mount-point removal forces recreation; the data volume is attach-only and survived, the rootfs — and the loaded local image — did not; 104's `/tmp` tar was gone after its own reboot). Valheim now runs upstream `ghcr.io/community-valheim-tools/valheim-server:latest` without the PlayFab healthcheck/autoheal label (upstream `status.json` cannot see a crossplay lobby, so the check would loop). **TODO: open a PR upstream with the fork's PlayFab status.json work (`~/Source/valheim-server-docker`), then restore the healthcheck + autoheal label and the image ref.** Earlier deferral note: **adguard was deferred** to its end-state shape — an LXC pair, one per MS-01 (design matrix) — because the fleet resolver is mission-critical AND the operator's own resolver: stopping 102 for the swap took down DNS on the Mac, so `terraform init` itself failed mid-rebuild (2026-08-20). A scratch final-form adguard VM (202, `.40.54`) stays up as the rehearsal artifact; the restored 102 at `.40.53` remains live. Retiring guests (107 nvidia-licensing, 110 lancache, 112 minio) are not rebuilt.

**One-time (with the first guest):**
1. Archive `terraform/terraform.tfstate` → `docs/pre-migration-state/terraform.tfstate.pre-cutover-<date>` (gitignored); `terraform state rm` every `proxmox_*` resource. Vultr/Cloudflare stay.
2. `vars.auto.tfvars`: endpoint → VIP, `virtual_environment_node` → ms-01a, `virtual_environment_storage` → `cephfs`, `primary/secondary_disk_storage` → `ceph-rbd`, `proxmox_nodes` → the three nodes, `guest_access_plane = "services"`. `vlans.yaml` `sdn_zones.*.bridge` → `vmbr0` (Storage zone MTU 9000 — the bond is jumbo).
3. `backup_jobs` re-authored for new VMIDs as replacements land (follow-on; old jobs died with the old cluster).

**Per guest:**
1. `vm-configs.tf`: `vm_id` = old+100, `vlans = ["vlan40"]` (PBS keeps vlan20), data volume per the matrix; keep `ip_offset`.
2. `make build <guest>` (targeted — NEVER a bare `terraform apply`/`make apply` while a restored copy runs). Stop the restored copy FIRST when the guest keeps its `ip_offset` (the two cannot share the address); a guest whose address the operator's own workstation depends on (the resolver) needs a partner instance serving before its copy is stopped — that is the LXC-pair design, not a scratch-offset dance.
3. Seed/restore state per the Stateful-data checklist; verify the service from a client.
4. Restored copy: `qm set <old> --onboot 0` + `qm shutdown <old>`. Never destroy here — decommission sweep after a week. Repoint the live AdGuard rewrite for the guest (`<guest>.core` → services address) on 102 until adguard itself is replaced.
5. `qm list` on all three nodes: no other VMID changed state.

## Stateful-data checklist (decide per service before its rebuild)

Decisions below are the **cutover** carries; the **end-state** home of each service's data (data volume / re-seed / restore-on-provision / accepted loss) is the per-service matrix in [rebuild-as-routine-design.md](rebuild-as-routine-design.md) (ADR 0015).

| Service | State at risk | Proposal |
|---|---|---|
| Infisical | all runtime secrets | re-seed from SOPS DR export (decided) |
| UniFi | controller config/adoption | cloud backup re-import (decided; expect re-adoption blip) |
| Plex | library metadata, watch history | copy `Library` dir out of the old VM before wipe; restore into the LXC — cheap vs a full re-scan + lost watch state |
| plex-services | postgres (arr DBs), configs | DONE 2026-08-21: stack stopped, configs + postgres data dir rsynced onto the data volume (pg_dumpall parked on 106 as the fallback); pg_dump timer now lands a daily dump on the volume |
| minio | bucket contents | inventory what's in it first; likely copy out via `mc mirror` |
| openobserve | metrics/log history | DONE 2026-08-21: PRESERVED — both stacks stopped, OO parquet+sqlite, Prometheus TSDB, Alertmanager, Grafana and Kuma DBs rsynced onto the data volume (design matrix supersedes the old "drop") |
| lancache | cache contents | drop (it's a cache) |
| doge | chain data | copy datadir if re-sync time matters, else re-sync |
| Tautulli/home­page/etc. | small app DBs/configs | config is IaC; small DBs copied out with their service if wanted |

## Test bar / verification

- Every Terraform change: `terraform validate` + clean `plan`; applies are idempotent (second apply = no changes).
- Every Ansible role: run-twice-zero-changes on a fresh guest (repo standard).
- Phase gates are the DoDs above — no phase starts until the prior gate's evidence is captured (command output in the session / `docs/pre-migration-state/`).
- Rollback: until pve is wiped, full rollback = power the old VMs back on (they're stopped, not destroyed, during cutover). After pve is wiped, rollback = vzdump restore onto the cluster (verified in WP0).

## Future work (explicitly out of scope this migration)

- **pfSense IaC** (ADR 0005): evaluate pfsensible Ansible collection vs a REST-API Terraform provider (needs the pfSense-API package); targets firewall rules, DHCP/resolver options, WireGuard peers. The redundant-DNS DHCP change and any migration-driven rule edits are documented manual steps until then.
- **Full UniFi fabric under Terraform**: import the Pro-24, Flex, APs, and remaining port profiles (WP6 covers only the new switch + touched ports).
- **Re-publicizing history**: operator accepted history exposure; if that changes, a git-filter-repo rewrite is the path.
- **Fold worklab into the cluster** (ADR-0009, amends 0001): at the worklab rebuild, join the NUC 15 Pro as a **non-voting (`quorum_votes: 0`), compute-only** member — corosync on its native I226-V 2.5G (never the Thunderbolt 10G), no Ceph OSDs (Ceph *client* via the WP2 public network), and homelab HA resources restricted to `crete/crete2/pve` so nothing homelab fails over onto it. **Prereq (Codex review 2026-08-11): `proxmox_host` needs an explicit Ceph-server eligibility gate first** — today ceph.yml runs MON create on every group member once the group is ≥3 nodes, which is correct for the three storage nodes and wrong for a compute-only member (OSD avoidance alone rides the empty `osd_disks` binding). Driver: pooled capacity + one IaC endpoint for scaling multi-version work labs. Needs, above the cluster: an IaC lab-environment module per product version, SDN-isolated VNets per lab, and templates/linked clones.

## Open items (not blocking approval)

- **BOINC:** keep CPU-only or drop — decide before WP4 lands the docker-legacy stack.
- **Corosync ring1:** ring1 on **VLAN 32** (i226-LM, tagged alongside AMT), configured in WP2 (cheap); drop if flaky. ring0 is VLAN 31 on i226-V (ADR-0008).
- **MS-01 PCIe lane-sharing:** verify the x4 slot doesn't starve an M.2/OSD when the 25G NIC is installed (check during Day-1 bring-up, before Ceph).
- **Packer golden images:** unaffected; revisit after migration.
- **Reverse DNS (PTR) zones — set up once DNS is fully functional (operator, 2026-08-12).** No PTR records exist today (`dig -x` on any fleet address returns nothing), which is why the `netconsole` OpenObserve stream labels hosts by bare IP. netconsole's wire format has no hostname field at all (measured 2026-08-12; see the netconsole userdata entry in `CLAUDE.md`'s What Never To Do), so the collector is the only place a name can come from, and with `use_dns(no)` set globally in `axosyslog.conf.j2` there is nothing to resolve against. Once bind9 serves PTR zones for the fleet subnets, flipping `use-dns(yes)` on the `s_netconsole` source alone (a per-source option, so the 5514 streams keep their sender-supplied hostnames) turns `host` into a name with no sender-side change. PTR is strictly better than the host's own hostname here: PVE node renames are unsupported once guests exist, so the two live nodes both report `pve`, while PTR names are per-address and stay distinct.
- **DNS registration end-to-end (operator, 2026-08-21): forward + reverse records and automatic registration through DHCP must be verified working for everything.** Server side is proven as of the dns pair build: RFC 2136 A+PTR updates land on the primary via the VIP, forward through the secondary, and replicate (the `/etc/bind` AppArmor journal denial that silently broke it on 109 is fixed). What is NOT done: pfSense's DHCP scopes have `ddnsdomain`/`ddnsdomainprimary`/`ddnsdomainkey*` **empty on every VLAN** (`/conf/config.xml`, read 2026-08-21) — nothing has ever registered a lease. Operator action per scope (hand-managed, ADR 0005): DDNS domain = that VLAN's zone, primary = `dns_server.bind_ipv4` (the VIP), key name `dhcp-update-key`, hmac-sha256, secret = Infisical `/infrastructure/bind_tsig_key_secret`; reverse zones already exist per `zone_vlans` VLAN. Then verify a fresh lease yields both `dig <host>.<zone>` and `dig -x`, and that AdGuard's per-zone forwards (`[/zone/]bind_ip` + the in-addr.arpa forwards) return them. Static-IP fleet guests still get only the AdGuard rewrite (`<vm>.<zone>`), not a BIND record — decide whether Terraform/Ansible should also `nsupdate` them (closes the PTR item above for the fleet).
- **Fleet apt source is flaky — DECIDED 2026-08-11: [ADR 0021](decisions/0021-fleet-packages-ride-an-apt-cacher-ng-pull-through-cache-with-per-client-fallback-lancache-retires.md)** — apt-cacher-ng pull-through cache with per-client `Proxy-Auto-Detect` fallback (first boot survives cache-absent); Lancache retires. Implementation is follow-on work scoped in the ADR's Consequences. Original problem statement and measurements preserved below for reference. Cloud-init leaves every guest on `archive.ubuntu.com`, whose IPv4 path from here is erratic: measured 2026-08-04 from unifi, three consecutive 6 MB range fetches gave 0 B/s / 4.5 MB/s / 0 B/s, against 8.9 / 9.2 / 3.9 MB/s to `mirror.math.princeton.edu`. Not VLAN-specific (a vlan40-egress guest measured slower than vlan10-only unifi) and not MTU (PMTU 1462 discovered correctly). Guests are IPv4-only on mgmt, so unlike a dual-stack client they cannot fall back to Canonical's much faster IPv6. Symptom is a silent multi-minute stall in `make update` whenever a kernel is in the set — apt has no minimum-rate abort, so a 19 KB/s transfer looks identical to a hang.
  - **Client half WIRED 2026-08-24:** `ansible/roles/apt_proxy` renders `/usr/local/bin/apt-proxy-detect` + `apt.conf.d/01proxy` (`Proxy-Auto-Detect`, https always DIRECT) on every node, worklab and guest via `make apt-proxy` (both inventories) and from `proxmox_host` on the WP2 path; `apt_proxy_host` derives from `hostvars['apt-cache']` (empty → files removed, apt direct). Verified fleet-wide: every host resolves the cache, `apt-get update` rc=0, second run 0 changed. Cloud-init delivery deliberately NOT done — a snippet change flips every VM to `must be replaced` (ADR 0016); carry it with the next planned image roll. Original note: the template was rendered by no task and worklab ran on a stale hand-written hard proxy from the 2026-08-12 W4 harness until then.
  - **Rebuild-speed angle (operator, 2026-08-11):** the same WAN-pull cost hits the whole Day-1/rebuild flow, not just `make update` — every guest's cloud-init apt phase AND every node's `pveceph install` pulls packages independently. W4 evidence: nested `pveceph install` across 3 nodes was the single slowest phase of the dress rehearsal (multi-minute, serialized over the no-subscription repo). Across ~3 nodes + ~15 guests the fleet rebuild re-downloads the same package set a dozen-plus times over the flaky path. Two shapes to weigh when we evaluate post-dress-rehearsal: (a) a **pull-through apt cache** (apt-cacher-ng) — purpose-built for `.deb`, cleanest for both guest apt and node `pveceph install`/PVE repos; (b) **adapt the Lancache VM to apt/PVE repos**. Note (operator, 2026-08-11): **Lancache is a retirement candidate** — it was being trialed for Windows Update caching, performed poorly at that, and will be retired UNLESS an apt/other-source use keeps it alive. So this is not "reuse a free existing service" but "give Lancache a reason to exist." Technical fit is plausible: Lancache is DNS-driven sniproxy caching of HTTP GETs, and both `archive.ubuntu.com` (apt) and `download.proxmox.com` (PVE no-subscription) serve over **HTTP**, so adding those to Lancache `cachedomains` + pointing AdGuard at Lancache for those hosts would cache `.deb`/PVE packages fleet-wide with no per-guest proxy config. That single adaptation would both solve rebuild speed AND justify keeping Lancache. Decision fork: adapt-Lancache (keeps one VM, one caching mechanism for games+apt+PVE) vs retire-Lancache + apt-cacher-ng (purpose-built, but a new service). Evaluate both against "does it survive a from-scratch cutover when the cache itself is being rebuilt" — a rebuild-time dependency on a fleet-internal cache is a bootstrap-ordering trap (the cache VM must come up, and its DNS redirection be live, before the guests that depend on it — or first-boot apt must tolerate cache-absent and fall back to the WAN).

## CLAUDE.md conflicts surfaced

- The repo overview + conventions describe a single-node pve world; WP5 amends them (same edit window as implementation, per house rules).
- "No migration tasks" rule **holds**: roles stay greenfield; all migration mechanics live in this plan + the okf runbook + Make targets, never in role logic.
