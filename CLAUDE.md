# Claude Code Guidelines — Homelab Ansible/Terraform Repo

## Repo Overview

This repo automates a Proxmox-based homelab with Terraform (VM provisioning, SDN, Vultr VPS, Cloudflare DNS) and Ansible (software configuration, Docker stacks, monitoring, secrets management). Key VMs: AdGuard (DNS filtering), BIND9 (authoritative DNS), Infisical (secrets vault), OpenObserve (monitoring stack), Proxmox Backup Server, UniFi, Plex, plex-services (arr stack), Docker (legacy services), Homepage, Lancache, NVIDIA licensing. A Vultr VPS acts as a WireGuard relay for external access.

**Architectural decisions** live in `docs/decisions/` (ADRs) — read them before proposing architecture changes. In flight: the MS-01 3-node cluster migration ([plan](docs/ms01-cluster-iac-plan.md), ADRs 0001–0009 + 0014) — 3-node PVE 9 + Ceph, Terraform-managed hosts, static-IP LXCs, greenfield rebuild. Cluster create/join is API-module orchestrated, strictly serial with membership gates ([ADR 0023](docs/decisions/0023-proxmox-cluster-create-and-join-use-the-community-proxmox-api-module-not-pvecm-over-ssh.md)); cutover runs a restore-bridge — old fleet restored from PBS onto the 2-node cluster so pve is wiped/rejoined early, greenfield replacement follows at leisure ([ADR 0024](docs/decisions/0024-cutover-rides-a-restore-bridge-restore-the-old-fleet-onto-the-2-node-pve-9-cluster-rebuild-pve-early.md); mixed 8/9 clustering rejected as unsupported). Post-bridge, guests are **greenfield-replaced one at a time** beside the restored copies — fleet state pruned of every `proxmox_*` resource, new VMID = old+100, restored copy stopped never destroyed, targeted `make build` only ([ADR 0028](docs/decisions/0028-greenfield-replace-builds-final-form-guests-beside-the-restored-copies-under-vmid-old-100-after-pruning-the-fleet-state-of-every-proxmox-resource.md), [runbook](docs/ms01-cluster-iac-plan.md#greenfield-replace-runbook-day-3-adr-0028)). Ceph cluster network is switched 25G on the Pro-Aggregation's SFP28 ports, VLAN 21 (ADR 0014, renumbered from 33 — the FRR mesh is superseded). Corosync VLANs 31/32 live on the shared 48-port access switch, not a dedicated one ([ADR 0019](docs/decisions/0019-corosync-moves-onto-the-shared-48-port-access-switch-trading-physical-isolation-for-consolidation.md), superseding [ADR 0008](docs/decisions/0008-corosync-on-a-dedicated-switch-local-vlan-vlan-30-stays-infrastructure.md)'s dedicated-switch model — VLAN isolation itself is unchanged, only the physical switch). End-state rebuild model ([design](docs/rebuild-as-routine-design.md)): durable state on detached Ceph data volumes, guest rootfs disposable ([ADR 0015](docs/decisions/0015-durable-state-rides-detached-ceph-data-volumes-the-guest-rootfs-is-disposable.md); holder is a single never-started **VM** whose raw disks get a one-time format+chown before first consumer start — appendable in-place, never a CT, [ADR 0020](docs/decisions/0020-detached-data-volumes-ride-a-single-never-started-holder-vm-not-a-holder-ct.md)); guest OS images pinned + deliberately rolled ([ADR 0016](docs/decisions/0016-guest-os-images-are-pinned-and-deliberately-rolled-container-tags-stay-latest.md)); guests single-homed on the services VLAN with storage via host bind mounts, squid retired ([ADR 0017](docs/decisions/0017-guests-are-single-homed-on-the-services-vlan-storage-reaches-containers-via-host-bind-mounts.md)). The dual-homed vlan10/vlan40 conventions below (SSH via vlan10, prefix translation) describe the live fleet and are amended at cutover (WP7).

**Post-migration program (decided 2026-08-22):** three management planes — **vlan10 out-of-band** (KVM/AMT/UniFi/switches + a `control` VM with MeshCentral and the Portainer server; laptop-only ingress), **vlan30 hypervisor** (nodes, VIP, PDM, PBS, apt-cache, pxe; named guest rules only), **vlan40 services** (Talos + the client-reachable guests); AMT ports go native-10 with the host mgmt tagged 30; this amends ADR 0017's zero-guests-on-VLAN-10 ([ADR 0030](docs/decisions/0030-three-management-planes-vlan10-out-of-band-vlan30-hypervisor-with-pdm-pbs-apt-cache-pxe-vlan40-services.md)). A **3-node Talos Kubernetes cluster is the services plane** (registry + ARC runners first, then the docker-LXC stacks one at a time); the bootstrap tier — DNS, Infisical, PBS, apt-cache, pxe, unifi, plex, LLM, PDM — stays on Proxmox forever; **no further greenfield-replaces for k8s-bound guests** (github-runner 113 → ARC, ADR 0034; mcp 115 was never built out and was retired 2026-08-24 without a k8s successor); control-plane VMs pinned one per node, no HA, third etcd member on worklab until the msi RMA ([ADR 0031](docs/decisions/0031-a-three-node-talos-kubernetes-cluster-becomes-the-services-plane-and-the-bootstrap-tier-stays-on-proxmox.md)). **CI** is ephemeral ARC pods that build their own guests inside PVE pool `ci` (scoped token, VMIDs 5000–5999, isolated `ci` VLAN, reaper); the Mac Studio runner is a separate persistent trust tier ([ADR 0032](docs/decisions/0032-ci-runners-are-ephemeral-and-build-their-own-guests-inside-a-pve-pool-on-an-isolated-vlan-and-the-mac-studio-is-a-separate-trust-tier.md)). Phased change plan: [plan doc](docs/ms01-cluster-iac-plan.md#post-migration-change-plan-decided-2026-08-22).

**Talos services plane — P3a built 2026-08-23 ([ADR 0033](docs/decisions/0033-talos-p3a-rides-talosctl-from-a-kubernetes-tree-with-the-factory-image-pinned-in-cloud-images-the-worklab-member-via-a-second-bpg-provider-alias-flannel-and-cluster-secrets-in-infisical.md), [plan](docs/talos-p3a-plan.md)):** `talos-cp-a/b/w` (VMIDs 230/231/232, pinned to ms-01a / ms-01b / worklab, **never an HA resource**) are plain resources in `terraform/talos.tf` — not the VM module — with worklab reached through the `proxmox.worklab` provider alias (`bootstrap.worklab_password`). Each node has a services leg (kubelet/etcd/API, Talos L2 VIP at services offset 60) and a Ceph-public VLAN 20 leg for CSI only. Image = factory v1.13.9 nocloud qcow2 pinned in `cloud_images.talos`; running nodes roll with `talosctl upgrade`, not a rebuild. See Canonical pipelines.

## Canonical pipelines

- **Kubernetes / Talos:** `kubernetes/` is the only surface — `kubernetes/talos/talos.sh` (configs rendered from the `talos_nodes` terraform output + Infisical `/talos` secrets; `make talos-secrets|apply|bootstrap`) and `kubernetes/ceph-csi/deploy.sh` (`make talos-csi|smoke`; `helm template | kubectl apply`, never `helm upgrade` — helm's client cannot complete the TLS handshake to this apiserver). **Ansible never touches a Talos node**, and no second tool may render machine configs. `kubeconfig`/`talosconfig` live in `kubernetes/talos/.secrets/` (gitignored; `make talos-secrets` re-pulls). **P3b add-ons ([ADR 0034](docs/decisions/0034-p3b-metallb-l2-on-a-reserved-vlan40-slice-zot-behind-an-internal-cert-manager-ca-arc-scale-sets-as-hostnetwork-dind-pods-pinned-to-one-node-and-a-cronjob-reaper.md)):** every `kubernetes/<component>/deploy.sh` sources `kubernetes/lib.sh` (`helm_apply` = template|apply, `subnet_ip`, `inf_get`). MetalLB L2 owns services offsets **64–79** (`make talos-lb`; speakers need `ignoreExcludeLB` because every node is a control plane). cert-manager's `homelab-ca` ClusterIssuer is a **`ca` issuer over an intermediate signed by the Infisical internal root `Homelab Root CA`** ([ADR 0039](docs/decisions/0039-wp8-an-infisical-internal-root-signs-cert-manager-s-homelab-ca-intermediate-acme-http-01-stays-for-per-host-certs-the-pbs-postgres-dump-is-the-ca-s-dr-path.md), WP8): `make talos-certs` creates the Certificate Manager project `homelab-pki` + the root through the API (idempotent by name, fleet machine identity via `lib.sh` `inf_token`), signs a fresh intermediate with `sign-intermediate` whenever the cluster's `tls.crt` does not verify against the root, and exports the **root** to `kubernetes/.secrets/homelab-ca.crt` — that file is what the Talos nodes trust through `machine.registries` (`make talos-trust`), the workstation's docker `certs.d`, and the future fleet root-distribution role; the intermediate is disposable (re-signed on cluster loss, never exported). Infisical's ACME server is HTTP-01 only, so it is reserved for per-host certs; the cluster never uses it. The CA's DR path is the nightly `infisical_pbs_backup.sh` Postgres dump to PBS ns `databases` — verify by snapshot recency, `make infisical-backup` cannot carry PKI keys. Zot is `registry.<service domain>` on its own LB IP (`nodes.json .domain`; the A record comes from `make dns-records`; the name is pinned into `machine.registries`, so a rename is `make talos-registry` → restart the statefulset (Zot does not reload a renewed TLS secret) → `make talos-trust` → redeploy every image-ref consumer), upstream image ref, blobs on a ceph-rbd PVC; it mirrors docker.io, ghcr.io, quay.io, public.ecr.aws and registry.k8s.io. ARC scale sets (`psproxmoxve`, `pbs-collection`) are **hostNetwork dind pods pinned to talos-cp-a** sharing one RWO cache PVC; `runs-on:` must be the scale-set *name* (label lists never match). The GitHub App creds and the `ci@pve!ci` token enter the cluster as one-off secrets created by `make talos-arc` (P3b-only path). The ci-reaper CronJob destroys pool-`ci` guests older than 6 h. pfSense CI VLAN rules are hand-managed: [docs/pfsense-ci-vlan.md](docs/pfsense-ci-vlan.md). **P4a ([ADR 0035](docs/decisions/0035-p4a-traefik-ingress-on-one-lb-ip-with-a-wildcard-internal-cert-the-infisical-kubernetes-operator-is-the-secret-path-and-homepage-is-the-first-zot-templated-ref.md)):** HTTP apps publish a plain `Ingress` (`ingressClassName: traefik`, no `tls:` block) on `<name>.<service domain>` (`DOMAIN="$SERVICE_DOMAIN"` from `lib.sh` `inv_env`) — Traefik sits on services offset **65** with the Let's Encrypt wildcard (ADR 0040 P5b) as its default TLS store, and **external-dns publishes every Ingress host into BIND** (`make talos-dns`, ADR 0040). Nothing is an AdGuard rewrite any more and no legacy-suffix name exists (P5e). **Runtime secrets reach a pod only through an `InfisicalSecret` in the component's tree** (one per source folder, `template.data` renames keys to the env names the workload expects, `envFrom`, `secrets.infisical.com/auto-reload`); the single `kubectl create secret` in the cluster is `infisical-universal-auth` (`make talos-infisical`) — never add one-off create paths (ARC's two are grandfathered). Image refs are `registry.<service domain>/<upstream>/<repo>` with the prefix derived from `nodes.json` at deploy time (`REGISTRY`), never written into a tracked manifest. App configs carry `${VAR}` placeholders filled by `envsubst` from `lib.sh` `inv_env` (fleet `service_ip`s from `vms.yaml`, AdGuard VIP, `HOST_ALIAS`) — a `kubernetes/<app>/` tree never contains a private address. `helm_apply` passes `-n` to `kubectl apply` because some charts emit no `metadata.namespace`. **P4b ([ADR 0036](docs/decisions/0036-p4b-the-monitoring-stack-moves-to-the-cluster-as-one-namespace-axosyslog-keeps-a-stable-address-on-a-metallb-lb-with-local-traffic-policy-state-rides-per-service-ceph-rbd-pvcs-rsynced-from-203.md)):** the monitoring stack is `kubernetes/monitoring/` (`make talos-monitoring`; 12 Deployments, per-service ceph-rbd PVCs, one `InfisicalSecret` on `/monitoring`, Prometheus telegraf targets rendered from `vms.yaml` by `render.py`); **axosyslog is a MetalLB `LoadBalancer` at services offset 66 with `externalTrafficPolicy: Local`** (the netconsole stream's `host` is the sender's source IP, so Cluster policy would SNAT every node to the same address) — `openobserve_listen_host` in `all.yml` is the services prefix + `monitoring_syslog_offset`, never a `hostvars` lookup; `host-bindings.yaml` `cluster.openobserve_syslog_target` points there (`make proxmox-hosts TAGS=logging`); pfSense's remote log server and UniFi's remote syslog + netconsole are hand-set in their UIs to the same address (the `ansible` user's sudo on pfSense is password-gated, so `pfsensible.core.pfsense_log_settings` cannot run unattended). UIs are `grafana|openobserve|prometheus|alertmanager|uptime-kuma.<service domain>` Ingresses. The OpenObserve image stays pinned (`v0.92.0-rc2`, schema 46; renovate watches `app.yaml`). `make monitoring-migrate FROM=<ip>` is the one-off history copy; `make monitoring-users` recreates `monitoring@pve` + its token (play tag + `proxmox.yaml`, UniFi half skipped). ConfigMaps go through server-side apply (the dashboards exceed the last-applied annotation cap). **P4c ([ADR 0037](docs/decisions/0037-p4c-plex-services-moves-to-the-cluster-media-rides-a-kubelet-mounted-nfs-pv-postgres-keeps-a-pbs-dump-lane-via-a-locally-built-client-image.md)):** the arr stack is `kubernetes/plex-services/` (`make talos-plex-services`; 12 Deployments, Services keep the compose names so the Cloudflare tunnel routes `tautulli:8181`/`seerr:5055` resolve unchanged); the NAS media tree is a **kubelet-mounted NFS PV** (`/volume1/plex`, server via `${SYNOLOGY_STORAGE}`, `subPath: data` at `/data`) — the ADR 0017 node-level-mount shape with kubelet doing the bind; SABnzbd scratch is a bounded ceph-rbd PVC, never NFS; postgres keeps its PBS lane as a nightly CronJob (`pg_dumpall` → `backups` PVC → pxar push, ns `databases`) whose `proxmox-backup-client` image is the registry's one local push (`make plex-pbs-image`, rebuild when the PBS major rolls); `PBS_REPOSITORY` is composed by `deploy.sh` from the inventory. UIs are `sonarr|radarr|lidarr|prowlarr|bazarr|sabnzbd|tautulli|seerr.<service domain>` Ingresses; homepage widgets and the Kuma rows use in-cluster svc DNS. `make plex-services-migrate FROM=<ip>` is the one-off state copy; the Kuma rows are in `ansible/playbooks/uptime-kuma.yml`. Non-root pods (seerr/recyclarr/libation) need `fsGroup: 2000`; postgres mounts `subPath: data/data` at `/var/lib/postgresql/data` because the migrated parent dir is uid 2012 and unsearchable for uid 999. **P4d ([ADR 0038](docs/decisions/0038-p4d-the-games-host-moves-to-the-cluster-valheim-on-a-metallb-udp-lb-with-local-policy-the-playfab-status-sidecar-keyed-by-server-name-in-the-same-pod-kiwix-zims-via-an-nfs-pv-meshcentral-stays-on-control.md)):** the games host is `kubernetes/games/` (`make talos-games`): one `valheim` pod = the crossplay server + the `valheim-status` PlayFab sidecar (`valheim-playfab-status.mjs --name`, keyed by server name — never re-pin an entity ID, it rotates on every recreate) + a busybox httpd for `status.json`; **UDP 2456–2458 are a MetalLB `LoadBalancer` at services offset 67 with `externalTrafficPolicy: Local`** (the pfSense WG_VPS forward targets it; the VPS DNAT targets pfSense and never changes); the server's liveness probe (stale or `online: false` for 10 min) replaces autoheal; Kiwix reads the NAS `docker` export through a read-only NFS PV at `kiwix.<domain>`. MetalLB offsets in use: 64 Zot, 65 Traefik, 66 syslog, 67 Valheim — next free 68. The namespace is `pod-security.kubernetes.io/enforce=privileged` (SYS_NICE). `make games-migrate FROM=<ip>` is the one-off copy and **refuses to stop the old server while its `status.json` reports players > 0**; the Kuma row is in `ansible/playbooks/uptime-kuma.yml`. MeshCentral stays on `control` (ADR 0030 stands — the old plan row entry was an error).

**P5 — DNS, certs, SSO, Jellyfin (APPROVED 2026-08-24, [ADR 0040](docs/decisions/0040-p5-real-dns-on-a-flat-public-domain-zone-bind-fed-by-nsupdate-external-dns-adguard-forwards-rewrites-retired-let-s-encrypt-dns-01-wildcards-as-traefik-s-default-cert-two-authentik-realms-split-by-audience-jellyfin-over-the-vps-relay-with-ldap-auth.md), [plan](docs/p5-dns-sso-jellyfin-plan.md)):** the internal namespace becomes a flat zone on an operator-owned public domain (`vlans.yaml` `service_domain`, a binding), BIND authoritative with every record a dynamic update (Ansible `nsupdate` for the fleet, external-dns RFC 2136 for the cluster), AdGuard forwards and its rewrites retire (implements ADR 0006); Traefik's default cert becomes a Let's Encrypt DNS-01 wildcard via Cloudflare (ADR 0007's browser-facing half; `homelab-ca` stays for the registry/fleet); two authentik instances split by audience (`auth.<media domain>` via the tunnel for Jellyfin/Seerr, `auth.<service domain>` LAN-only for OIDC + Traefik forward-auth); Jellyfin over the Plex-style VPS relay on 443 with LDAP auth. Additive rollout; the `.internal` suffix (`domain_suffix`) retires last. **P5a landed 2026-08-25:** `vlans.yaml` carries `service_domain`, `media_domain` and `internal_zones` (a dict `zone → [public names mirrored inward]`; the WAN is DDNS-owned by pfSense's Cloudflare client, so `vpn` is looked up on the public copy at run time, never a static binding, and Terraform does not own it). BIND serves each `internal_zones` key as a flat zone beside the per-VLAN ones (`bind9_forward_zones`; `zone_vlans` gained `infrastructure` + `services` so every fleet address has a reverse zone); `ansible/tasks/dns_records.yml` (`make dns-records`, also the tail of the `dns` play) pushes guests/nodes (A + PTR), the `adguard`/`dns`/`pve` VIPs, the MetalLB names (`registry` 64, `ingress` 65, `syslog` 66, `valheim` 67) and the mirrored public records over `community.general.nsupdate` — nothing prunes, retired names come out by hand (`nsupdate`); `kubernetes/external-dns/` owns the Ingress hosts (TSIG via an `InfisicalSecret` on `/infrastructure`, `txtOwnerId: talos`, `policy: sync`, AXFR by key against the BIND VIP). AdGuard forwards each internal zone to BIND (`adguard_zone_forwards`, converged on the live instance through `/control/dns_config` because the config template is initial-only); no rewrite is left (P5e: keepalived's `check_dns.sh` on an AdGuard instance tracks the instance's own hostname, which AdGuard serves from `/etc/hosts` — the resolver VIP never depends on BIND). `SERVICE_DOMAIN`/`MEDIA_DOMAIN`/`INTERNAL_ZONES` come from `inv_env`, `nodes.json .domain` is the service domain (registry + trust). The media zone is not served internally yet (P5d adds it to `internal_zones` with its mirror list). Optional hand step: a pfSense RFC 2136 client for `vpn.<service domain>` → the BIND VIP keeps the mirror live between Ansible runs. **P5b landed 2026-08-25:** Traefik's default cert is a **Let's Encrypt DNS-01 wildcard** (`kubernetes/traefik/certificate.yaml` `wildcard-tls`: `*.<service domain>` + `*.<media domain>`, `issuerRef letsencrypt`; `ISSUER=letsencrypt-staging make talos-ingress` rehearses against the staging CA); `kubernetes/cert-manager/issuer.yaml` carries the `letsencrypt` + `letsencrypt-staging` ClusterIssuers with the Cloudflare token from Infisical **`/infrastructure/cloudflare_dns_api_token`** (an `InfisicalSecret` in `kubernetes/cert-manager/secrets.yaml`; the `/plex` copy is gone and `plex_certificate` reads `secrets.infrastructure.*` — so **`make talos-infisical` precedes `make talos-ingress` on a fresh cluster**). cert-manager's DNS-01 self-check is pinned to `1.1.1.1`/`1.0.0.1` (`dns01RecursiveNameserversOnly`) because the cluster's AdGuard → BIND chain answers the service zone itself and never sees the `_acme-challenge` TXT (scoped ADR 0012 exception, recorded there). `homelab-ca` stays the issuer for the registry and everything non-browser (the `legacy-tls` SNI fallback went with P5e). `acme_email` in `all.yml` is the one LE account address (`inv_env` `ACME_EMAIL`, `plex_certificate`). Kuma carries **zero `ignore_tls`** rows: Traefik-fronted monitors are `<name>.<service domain>`, Plex probes `https://plex.<media domain>:32400/identity` (the `plex_certificate` LE cert; secure connections are *Required* on the server, so plain HTTP from the network is an empty reply), PBS is a TCP 8007 port check. Media-domain literals are gone from tracked files — `${MEDIA_DOMAIN}` in app configs (homepage bare `envsubst`; monitoring's explicit `SUBST` list), `module.network.media_domain` in Terraform (`data.cloudflare_zone.media` by name replaced `var.cloudflare_zone_id`; `jellyfin.<media domain>` A/AAAA staged **un-applied** for P5d, and the same plan shows the VPS/Plex AAAA contents drifted since the 2026-08-22 rebuild). Known: the operator Mac's `en0` resolver carries the ISP's IPv6 resolvers ahead of AdGuard, so `<name>.<service domain>` NXDOMAINs there while `dig @<AdGuard VIP>` answers — a client RA/RDNSS matter ([docs/ipv6.md](docs/ipv6.md)), not a repo defect. **P5c landed 2026-08-25:** `kubernetes/authentik/` is one tree, two realms (`make talos-authentik`, `REALM=`): namespaces `authentik` (`auth.<service domain>`, Ingress) and `authentik-ext` (`auth.<media domain>`, Service only — the tunnel route is a dashboard step); each realm = chart server + worker (2026.8, **no Redis — authentik dropped it**), an in-tree Postgres 17 Deployment on ceph-rbd, a nightly `pg_dumpall` → PBS ns `databases` CronJob (backup-ids `authentik-postgres` / `authentik-ext-postgres`), and an `InfisicalSecret` on its folder whose keys `deploy.sh` generates through the Infisical API on first run (hex only, so nothing downstream quotes). **All configuration is a blueprint** (`blueprint-<realm>.yaml` → ConfigMap → `ak apply_blueprint` in the worker at deploy time): internal = a `forward_domain` proxy provider on the **embedded outpost** (cookie on `<service domain>`, `config.authentik_host` set — unset, every 302 targets `http://localhost`) + OIDC providers for grafana/portainer/meshcentral/pdm/pve/pbs (`grant_types` must be explicit in 2026.x, the blueprint default is `[]` = "Invalid grant_type"); external = groups `family`/`admins`, `ldapsvc` service account with the `search_full_directory` object permission, the LDAP provider + an authentik-managed outpost Deployment through the `Local Kubernetes Cluster` service connection (declared in the blueprint so ordering never races discovery), invitation-only `family-enrollment` and link-based `family-recovery` flows (no SMTP in the fleet). Consumers: Traefik runs `providers.kubernetesCRD.allowCrossNamespace`, an Ingress opts in with `traefik.ingress.kubernetes.io/router.middlewares: authentik-forward-auth@kubernetescrd` (plex-services minus a split-out `seerr` Ingress, homepage, kiwix); **Uptime Kuma stays outside forward-auth** (`make uptime-kuma` is a socket.io client from the workstation; the 302 kills the handshake); Grafana is native OIDC (`GF_AUTH_DISABLE_LOGIN_FORM`, `authentik Admins` → GrafanaAdmin, basic auth kept for the homepage widget, secret via a second `InfisicalSecret` in `monitoring` on `/authentik`); Portainer (settings API, `AuthenticationMethod 3`) and MeshCentral (`authStrategies.oidc` in `config.json`) are wired by the `control` role from `secrets.authentik.*`; PVE/PBS/PDM realms are hand steps in [docs/authentik-setup.md](docs/authentik-setup.md). Machine paths (Prowlarr→arr, homepage widgets, Kuma rows, tunnel routes) use svc DNS and never see forward-auth. The Infisical operator restarts every auto-reload Deployment once at its first 5-minute resync after a fresh deploy — expected, not a loop. The `/docker` `authentik_*` keys and `/homepage/authentik_token` are deleted. **P5d landed 2026-08-25:** `kubernetes/jellyfin/` (`make talos-jellyfin`) is Jellyfin at `jellyfin.<media domain>` — ceph-rbd `config` + `cache` PVCs, the Synology plex export as its **own read-only NFS PV/PVC** (claims cannot cross namespaces), a plain Ingress (no forward-auth: TV apps), image via Zot. Auth is the **LDAP-Auth plugin against `ak-outpost-ldap.authentik-ext.svc:389`** (bind `cn=ldapsvc,ou=users,dc=ldap,dc=goauthentik,dc=io`, members of `family`/`admins`, `admins` → Jellyfin admin, username attribute `cn` — authentik's `uid` is a hash; `CreateUsersFromLdap`, password changes stay in authentik). `deploy.sh` drives the whole thing through the API from inside the pod (`kubectl exec … curl localhost:8096`): first-run wizard (`GET /Startup/User` before the `POST`, which 404s otherwise), plugin install + restart, plugin config merged only when it differs, Movies/TV/Music libraries with `refreshLibrary=false` + one `POST /Library/Refresh` — a library POST blocks for the whole scan while one runs, and `/Plugins` lists Ids without dashes. Secrets: `/jellyfin` (new folder, `admin_password` generated by deploy.sh; `lib.sh` now carries `inf`/`ensure_folder`/`ensure_secret`) and `/authentik-ext` `ldap_bind_password`, both as `InfisicalSecret`s whose managed Secrets deploy.sh reads — the pod takes nothing from the environment. **The media zone is served internally**: `internal_zones` carries `<media domain>` with the mirror list (every public name except `jellyfin`, which external-dns owns), external-dns filters both zones. External path = Cloudflare unproxied `jellyfin` A/AAAA (`cloudflare-dns.tf`) → Vultr firewall + `vps_nftables` DNAT TCP 443 → pfSense tunnel IP → **pfSense WG_VPS pass + port-forward 443 → Traefik LB .65 (hand step, [docs/pfsense-wireguard-vps-peer.md](docs/pfsense-wireguard-vps-peer.md))** → Traefik with the LE `*.<media domain>` cert; Jellyfin never touches the Cloudflare tunnel. Kuma row `Jellyfin` (`/health`), homepage tile; Seerr → Jellyfin auth is an operator UI step ([docs/authentik-setup.md](docs/authentik-setup.md)). **P5e landed 2026-08-25 — the `.internal` suffix is retired:** BIND serves only the `internal_zones` forward zones plus the per-VLAN reverse zones (`dns_zones` carries no `zone` any more — `vlan_id`/`dns_ip`/`bind_ip` only); AdGuard's role converges the **whole** upstream list (`adguard_upstreams` = public resolvers + reverse-zone forwards + flat-zone forwards) and deletes every rewrite through the API on each run, so the pair's rewrite count is 0 by construction; the AdGuard VIP track name is the instance's own hostname (`hostsfile_enabled` serves it locally), the BIND VIP tracks `ns.<service domain>`; the registry is `registry.<service domain>` (`nodes.json .domain` = `module.network.service_domain`, cert re-issued, nodes re-trusted, every image-ref consumer redeployed); `legacy-tls`, the 302 Ingress/Middleware and `SERVICES_ZONE` are gone; `dns_config` writes `Domains=<service domain>` on every VM; nodes' `pvesh … --search` and the LXC `/etc/hosts` FQDNs are on the service domain (`proxmox_host_dns_search`; `repos.yml` re-runs when the search domain drifts). **`domain_suffix` survives in `vlans.yaml` for one consumer only: the VM fleet's cloud-init `fqdn`** — a changed user-data snippet shows every VM as `must be replaced` (ADR 0016), so it moves to the service domain with the next deliberate VM roll, not before; the guardrail still blocks it. The AdGuard play cannot run `--limit` to one instance (the pair shares its seeded secrets through `hostvars`); run both. pfSense's per-VLAN DHCP `domain`/DDNS zone is now `<service domain>` in the role but the role is hand-run (password-gated sudo) — until the operator runs it, DHCP clients still receive the old search domain.

**Bare-metal node installs** boot over **PXE/iPXE from the `pxe` fleet guest** with per-node answer files served over HTTP keyed by install-NIC MAC; Secure Boot stays off on PVE nodes; AMT/JetKVM are power/console only (*Reset to PXE*). The AMT IDE-R ISO path (`make node-iso`) is the network-down fallback ([ADR 0026](docs/decisions/0026-bare-metal-nodes-install-over-pxe-from-a-fleet-guest-answer-files-served-over-http-secure-boot-stays-on.md), [plan](docs/pxe-netboot-plan.md)).

**Alerting** runs in two lanes with one shared ntfy channel ([ADR 0011](docs/decisions/0011-two-alerting-lanes-uptime-kuma-for-reachability-alertmanager-for-metrics.md)): Uptime Kuma owns internal reachability (on the cluster at `uptime-kuma.<domain>`; **monitors + the ntfy channel are declared in `ansible/playbooks/uptime-kuma.yml` and applied by `make uptime-kuma` through the `goodolclint.uptime_kuma` collection** — targets derive from the inventory, admin login `goodolclint` / Infisical `/monitoring/uptime_kuma_admin_password`, `CHECK=1` for check mode; never edit rows in the UI or the sqlite file, re-run the playbook; ADR 0011 amended 2026-08-24, reference in [docs/uptime-kuma-monitors.md](docs/uptime-kuma-monitors.md)); Prometheus + Alertmanager own everything metric-shaped. UptimeRobot stays the external outside-in prober. The ntfy topic is the credential and lives at Infisical `/monitoring/alert_ntfy_topic`.

**DNS** is split-horizon behind AdGuard and must never see a public resolver ahead of it ([ADR 0012](docs/decisions/0012-internal-dns-must-not-be-poisoned-by-public-resolvers-adguard-first-base-durable-networkd-drop-ins.md)): `vlans.yaml` `dns_servers` is AdGuard-first (1.1.1.1 only as greenfield-bootstrap failover), and the `dns_config` role deploys durable systemd-networkd drop-ins (empty `DNS=` reset) — not resolvectl (reboot-transient) and not netplan overrides (netplan merges nameserver lists and cannot remove entries). Rollout vehicle: `ansible/playbooks/update-dns.yml`. **Containers** get their resolver list from Terraform, not `dns_config` (PVE rewrites a CT's resolv.conf on every start): `containers.tf` emits the VIP only, plus the public failover on guests flagged `resolver = true` ([ADR 0029](docs/decisions/0029-adguard-is-an-lxc-pair-behind-a-keepalived-vip-container-resolv-conf-is-terraform-owned-adguard-only-except-on-the-resolver-cts.md)). **AdGuard is an LXC pair** (`adguard1`/`adguard2`, VMIDs 251/252, one per MS-01) behind a keepalived VIP = `vlans.yaml` `dns_server.dns_ipv4`/`dns_ipv6`; admin creds are generated into Infisical `/homepage`; `make adguard-pause MINUTES=n` pauses filtering on both; never rebuild both instances at once. **BIND is the same shape** (`dns1` primary / `dns2` AXFR secondary, VMIDs 218/219, VIP = `dns_server.bind_ipv4` — the address AdGuard forwards internal zones to and pfSense DDNS targets): zone files live under `/var/lib/bind` on the primary, DDNS hitting the VIP while it sits on the secondary is forwarded to the primary (`allow-update-forwarding`); keepalived is one shared task file, `ansible/tasks/keepalived.yml` (`vip_*` vars), consumed by both roles.

**IPv6** is per-VLAN on both address families — GUA `track6` with prefix ID = VLAN ID, ULA derived in Terraform for stable internal service addressing ([ADR 0010](docs/decisions/0010-per-vlan-ipv6-addressing-gua-tracks-the-vlan-id-ula-carries-stable-internal-services.md)). The pfSense half is hand-managed per ADR 0005 and documented in [docs/ipv6.md](docs/ipv6.md). Management, infrastructure and openclaw VLANs are deliberately IPv4-only — do not "fix" them. A static `ipv6_offset` must never imply `accept_ra: false`; RA is the only source of the default route.

**VPS relay tunnel** peers over the reserved IPv4 (rebuild-stable) and its MTU is measured, not computed ([ADR 0013](docs/decisions/0013-vps-tunnel-peers-over-reserved-ipv4-mtu-is-measured-per-address-family.md)): `wg_mtu` is 1400 with the IPv4 endpoint (60-byte encapsulation; path measured clean to a 1500-byte outer). If the endpoint ever returns to IPv6 the number is 1360, not 1400 — v6 encapsulation costs 80 bytes and the v6 path silently drops WG UDP above a 1456-byte outer. pfSense `tun_wg1` must match in BOTH the tunnel config and the interface override, and the peer public key must match the Infisical `/vps` pair (see [docs/pfsense-wireguard-vps-peer.md](docs/pfsense-wireguard-vps-peer.md)).

**Audiobook ingest** is **not built in the fleet** — parked until WP7 by decision, with an interim unmanaged Libation install on the operator's Mac carrying the archive in the meantime (OpenAudible's license expired 2026-08-02); that bridge retires at cutover and its config/migration path is in the plan doc ([ADR 0018](docs/decisions/0018-audible-library-ingest-rides-libation-in-the-plex-services-stack-one-chapterized-m4b-per-book-staged-before-publish.md), [plan](docs/libation-audiobook-sync-plan.md)): Libation joins the `plex-services` compose stack as one more service, producing one chapterized M4B per book (never per-chapter splits), staged on local scratch and published to `/data/media/Audiobooks` so Plex never scans a partial file. It lands on the ADR 0017 bind-mount model with `/config` on the ADR 0015 data volume — do not build it against the `plex_data` NFS volume. The image pin rides ADR 0016's existing demonstrated-break exception; it is not a new pinning policy.

**Fleet package caching** ([ADR 0021](docs/decisions/0021-fleet-packages-ride-an-apt-cacher-ng-pull-through-cache-with-per-client-fallback-lancache-retires.md), decided 2026-08-11): apt-cacher-ng pull-through cache fronting `archive.ubuntu.com` + `download.proxmox.com`, reached via per-client `Proxy-Auto-Detect` with `DIRECT` fallback — the cache is an accelerator, never a bootstrap dependency; first boot on a cold cutover survives cache-absent. **Client half wired 2026-08-24** (`ansible/roles/apt_proxy`, `make apt-proxy`, included by `proxmox_host`); the cacheable https repos (influx, plex, infisical CLI) are `http://` at the source and remapped on the cache. Never write a hard `Acquire::http::Proxy` anywhere — worklab carried a stale hand-written one for 12 days after the cache re-homed to vlan30 and every apt run hung on it. **Cloud-init does not carry the probe** — a changed user-data snippet shows every VM as `must be replaced` (ADR 0016), so first-boot packages still pull from the WAN until each guest's next deliberate roll; land it in `user-data.yaml.tmpl` when a fleet-wide image roll is planned anyway. **Lancache retires** (VM 110, role, NFS share, AdGuard/homepage references — removal is follow-on work scoped in the ADR). Never DNS-rewrite public repo hostnames to a fleet-internal cache. Container images get the same treatment at WP4 via a fleet-local pull-through registry with templated `image:` refs ([ADR 0022](docs/decisions/0022-container-images-pull-through-a-fleet-local-registry-via-templated-refs-the-registry-rebuilds-itself-from-upstream.md) — Zot provisionally, Harbor evaluation open; the registry guest's own refs stay upstream so it always rebuilds itself from the WAN).

**PBS version** ([ADR 0025](docs/decisions/0025-pbs-4-rides-a-pinned-debian-13-cloud-image-not-the-pbs-iso.md), decided 2026-08-12; **landed 2026-08-21 as VM 201, PBS 4.2.5 on Debian 13** — the role reuses the LUN's existing datastore via `--reuse-datastore`, and `make backup-finalize` re-points the cluster entry after every PBS rebuild): the fleet stayed on **PBS 3.x through cutover** — PVE 9 → PBS 3 is supported, and ADR 0024's restore-bridge makes PBS the single load-bearing component of the migration. PBS 4 is Debian-13-only and the guest is Ubuntu noble, so the move is a **guest rebuild, not an `apt dist-upgrade`**: post-cutover (WP4) PBS lands on a pinned **Debian 13 generic cloud image**, keeping cloud-init and `modules/proxmox-vm`, with the role's repo suite and key swapped `bookworm` → `trixie`. Do not propose the PBS installer ISO (correct for the PVE nodes, wrong for this guest — costs cloud-init to buy a Proxmox kernel and ZFS the VM doesn't use), and do not propose a second PBS for host-failure redundancy (no HA/clustering in 4.x; Ceph HA plus the existing `pbs-self` → `nas-nfs` job already cover it — the open gap is a second *copy* of the data via pull-sync or the 4.x S3 backend). The iSCSI datastore LUN on the Synology survives the rebuild untouched.

**Key directories:**
- `terraform/` — Single consolidated Terraform project with `modules/proxmox-vm/` and `modules/network/`
- `ansible/roles/` — Single flat directory (~30 roles)
- `ansible/playbooks/` — All playbooks (site.yml imports infrastructure.yml + services.yml)
- `ansible/tasks/` — Shared task files (secrets loading, generation, pre_tasks)
- `ansible/group_vars/` — Global vars (`all.yml`), bootstrap secrets (`bootstrap.sops.yml`)
- `ansible/inventory/` — Terraform-generated `vms.yaml` + static `proxmox.yaml`, `pfsense.yaml`, `vps.yaml`
- `network-data/` — Canonical `vlans.yaml` (gitignored), `public_policy.yaml` (tracked)
- `scripts/` — Infisical migration/backup, security guardrails, validation

## Before You Start Any Task

1. **Read before writing.** Always read the files you intend to modify and their related files (defaults, templates, tasks) before making changes. Understand the existing patterns.
2. **Check the inventory.** VM names, IPs, and group membership are in `ansible/inventory/vms.yaml` (Terraform-generated). Do not hardcode IPs.
3. **Check `group_vars/all.yml`** for existing variables before adding new ones to role defaults — `all.yml` overrides role defaults and causes silent variable collisions.
4. **Check `host_vars/`** for per-VM configuration.
5. **Check MEMORY.md** for known issues, gotchas, and architectural decisions before proposing changes.

## Greenfield Philosophy

- **No migration tasks.** Do not write version-check conditionals, one-time migration logic, or upgrade paths. Every role should work correctly on a fresh VM and be idempotent on re-run.
- **No version pinning without reason.** Use `latest` tags for Docker images unless there's a specific compatibility constraint. Guest **OS images** are the exception with a reason: pinned by version + checksum and deliberately rolled ([ADR 0016](docs/decisions/0016-guest-os-images-are-pinned-and-deliberately-rolled-container-tags-stay-latest.md)) — `latest` OS images made plan output unreadable and `make apply` a fleet-replacement foot-gun.
- **Idempotent everything.** Every task must be safe to run repeatedly. Use `creates:`, `when:`, registered variables, and `changed_when:` to prevent unnecessary changes.
- **REPLACE_ME sentinel.** Secrets that haven't been generated yet use the string `REPLACE_ME`. The `generate_secret.yml` task checks for this value to decide whether to generate.

## Secrets Handling

### Two-Tier Architecture
- **Tier 1 (Bootstrap):** `ansible/group_vars/bootstrap.sops.yml` — SOPS-encrypted. Contains only provider credentials (Proxmox, Vultr, Cloudflare), Infisical auth (client_id, client_secret, admin creds), and external secrets (user-provided API keys, passwords). Referenced as `bootstrap.key_name` or `bootstrap_config.key_name`.
- **Tier 2 (Runtime):** Infisical VM — stores ALL runtime secrets. Organized into folders:
  `/shared`, `/monitoring`, `/plex`, `/plex-services`, `/homepage`, `/docker`, `/minio`, `/vps`, `/pfsense`, `/pbs`, `/infrastructure`, `/github-runner`, `/control`
  Root `/` is **empty** — all secrets live in named subfolders.
- **No SOPS fallback.** If Infisical is unreachable, the deploy fails with a clear error. `secrets.sops.yml` is a DR artifact only (produced by `make infisical-backup`), never used at runtime.

### The `secrets` Fact
All secrets are loaded into a single `secrets` fact that matches the `secrets.sops.yml` structure:
- Root and `/shared` Infisical paths → flat keys: `secrets.key_name`
- Nested paths → nested dicts: `secrets.monitoring.key_name`, `secrets.plex_services.key_name`
- Key names are lowercased with hyphens converted to underscores

### Secret Delivery to Containers
All runtime secrets flow through **Infisical Agent env_files**. The agent polls Infisical, renders Go templates to `.env` files on disk, and containers read them via `env_file:` directives.

- Agent env_file path: `/etc/infisical/secrets/<name>.env`
- Go templates: `ansible/roles/infisical_client/templates/secrets/*.tpl.j2`
- Special case: Recyclarr uses `secrets.yml` with `!secret` YAML tags instead of env vars (on the cluster it is an item of the `plex-services-secrets` managed Secret, mounted at `/config/secrets.yml`).
- Agent exec hooks: When a rendered file changes, the agent runs the configured `post_command` to restart the affected container (see `infisical_agent_templates` in playbook vars).
- **No baked secrets.** No `{{ secrets.xxx }}` appears in any docker-compose template.

### `generate_secret.yml` Calling Convention
Required variables:
```yaml
- ansible.builtin.include_tasks: "{{ playbook_dir }}/../tasks/generate_secret.yml"
  vars:
    secret_key: "my_password"                # lowercase key name (REQUIRED)
    secret_gen_cmd: "openssl rand -hex 32"   # shell command that prints value (REQUIRED)
    secret_comment: "Description for Infisical" # human-readable comment (REQUIRED)
    secret_infisical_path: "/monitoring"      # Infisical folder (REQUIRED — no default)
    secret_dict_path: "monitoring"            # nested dict key in secrets fact (REQUIRED)
    # Optional:
    secret_run_on: "{{ inventory_hostname }}" # host to run command on (default: "localhost")
```

### Rules
- **Never put secrets in `vars.auto.tfvars`** — enforced by pre-commit hook.
- **Never commit unencrypted secrets** — SOPS files only, gitleaks pre-commit hook scans for leaks.
- **Always use `{{ secrets.key_name }}`** — never reference bootstrap-tier secrets in templates (except the Infisical role itself which needs bootstrap creds before Infisical exists).
- **Resolve `secret_comment` to a fact** before passing to `infisical_write_secret.yml` to avoid recursive template loops. The `generate_secret.yml` task handles this automatically.
- **Use `printf '%s' value`** not `echo -n value` in generation commands — some shells output `-n` literally.
- **All secret key names must be lowercase with underscores** — no UPPERCASE keys in Infisical. Convention: `{component}_{secret_type}` (e.g., `pbs_admin_password`, `grafana_admin_password`).

## Infisical Folder Ownership

Each Infisical folder is owned by the role that generates/provisions its secrets. Consumers read from the source folder — no copies.

| Folder | Owner Role | Agent Readers | Auto-Generated | External (User-Provided) |
|--------|-----------|---------------|----------------|-------------------------|
| `/shared` | proxmox_backup | All VMs (via pbs_client) | pbs_fingerprint, pbs_backup_token | — |
| `/monitoring` | monitoring_users, proxmox_backup | k8s `monitoring` + `homepage` (InfisicalSecret) | grafana_admin_password, openobserve_root_user_pass, unifi_monitoring_password, pbs_api_token, proxmox_token_value | uptimerobot_heartbeat_url, alert_ntfy_topic |
| `/plex` | plex, plex_certificate | plex, homepage | plex_token, plex_smb_pass, plex_cert_pfx_password | — |
| `/plex-services` | — (values pre-exist; `kubernetes/plex-services/` consumes, no generator since P4c) | k8s `plex-services` + `homepage` (InfisicalSecret) | postgres_password, *_db_password (×6), arr_admin_password, *_api_key (×9), libation_master_key | cloudflared_tunnel_token, usenet_*, nzb*_api_key, opensubtitlescom_* |
| `/docker` | — (values pre-exist; `kubernetes/games/` consumes via `InfisicalSecret`, no generator since P4d) | k8s `games` (InfisicalSecret) | valheim_server_password, valheim_supervisor_password | valheim_discord_webhook (optional — absent today, the join-code hook stays inert until it exists) |
| `/authentik` | `kubernetes/authentik/deploy.sh` (`make talos-authentik`, Infisical API, no Ansible role) | k8s `authentik` (InfisicalSecret), k8s `monitoring` (grafana_oidc_client_secret only); the `control` role reads `secrets.authentik.*` at deploy time for Portainer + MeshCentral | secret_key, postgres_password, bootstrap_password, bootstrap_token, grafana/portainer/meshcentral/pdm/pve/pbs `_oidc_client_secret` | — |
| `/authentik-ext` | `kubernetes/authentik/deploy.sh` (external realm) | k8s `authentik-ext` (InfisicalSecret), k8s `jellyfin` (InfisicalSecret, ldap_bind_password → the LDAP plugin config) | secret_key, postgres_password, bootstrap_password, bootstrap_token, ldap_bind_password | — |
| `/jellyfin` | `kubernetes/jellyfin/deploy.sh` (`make talos-jellyfin`, Infisical API, no Ansible role) | k8s `jellyfin` (InfisicalSecret; deploy.sh reads the managed Secret to drive the API) | admin_password | — |
| `/vps` | vps_wireguard | — (no agent) | vps_wg_private_key | maxmind_license_key |
| `/pfsense` | — (manual ref) | — (no agent) | — | cloudflare_dns_api_token, bind_tsig_key_secret |
| `/pbs` | proxmox_backup | — (no agent) | pbs_admin_password, pbs_backup_user_password | — |
| `/infrastructure` | bind9 | k8s `external-dns` (InfisicalSecret, TSIG), k8s `cert-manager` (InfisicalSecret, Cloudflare token) | bind_tsig_key_secret | unifi_admin_password, synology_admin_password, cloudflare_dns_api_token (DNS:edit on both public zones; also read by `plex_certificate` at deploy time) |
| `/homepage` | control (portainer_api_key only), adguard (adguard_*) | k8s `homepage` (InfisicalSecret) | portainer_api_key, adguard_* | unifi_* |
| `/control` | control, pdm | — (no agent) | portainer_admin_password, pdm_root_password | — |
| `/minio` | minio | minio | minio_root_password | — |
| `/github-runner` | — (read by `make talos-arc` into the `arc-runners/github-app` k8s secret; no Ansible role since P3b) | — (no agent) | — | github_app_id, github_app_private_key, github_app_installation_id |
| `/squid` | squid | — (no agent) | squid_ca_private_key, squid_ca_cert_pem | — |
| `/talos` | `kubernetes/talos/talos.sh` (`make talos-secrets`, no Ansible role) | — (no agent) | talos_secrets_yaml, talosconfig (base64) | — |

**Key rules:**
- "Owner Role" is the Ansible role whose tasks write secrets to this folder via `generate_secret.yml` or `infisical_write_secret.yml`.
- "Agent Readers" are VMs whose Infisical Agent templates include `with secret` blocks reading from this folder, or cluster namespaces with an `InfisicalSecret` on it (ADR 0035).
- VMs without agents (vps, pfsense, pbs, infrastructure) consume secrets at Ansible deploy time via the `secrets` fact — not via agent.
- `/homepage` has no owner role that generates secrets — it contains only user-provided external credentials seeded from `bootstrap.sops.yml`.
- **A new folder must also be added to the nested-path loop in `ansible/tasks/infisical_login.yml`** — the `secrets` fact only loads the folders listed there, so an unlisted folder's secrets look absent to `generate_secret.yml` and get REGENERATED on every run (Portainer admin password rotated out from under the live instance, 2026-08-22).
- When adding a new secret, find the correct owner folder first. If no existing folder fits, consider whether the secret truly needs a new folder or should be added to an existing one.

## Ansible Conventions

### Playbook Structure
- Playbooks import `full_pre_tasks.yml` which loads: local overrides → bootstrap SOPS → secrets (Infisical, hard fail if unreachable) → VLAN definitions → VLAN prefix computation → dns_zones.
- Playbooks are role-based — inline tasks only in `docker-config.yml` (lightweight compose-only deploys) and bootstrap.
- Each play targets specific hosts via `hosts:` and runs the appropriate roles.
- `site.yml` imports `infrastructure.yml` then `services.yml`.

### VLAN Prefix Facts
Pre_tasks compute these facts from `network-data/vlans.yaml`:
- `mgmt_vlan_prefix`, `services_vlan_prefix`, `storage_vlan_prefix`, `core_vlan_prefix`
- Cross-VLAN IP translation: `{{ ansible_host | replace(mgmt_vlan_prefix, services_vlan_prefix) }}`
- `internal_network_cidr`: the `/12` covering all VLANs
- **SSH uses vlan10 (management) IPs by default.** `ansible_host` resolves to the management VLAN IP. When running ad-hoc SSH commands, use the vlan10 IP (i.e., `ansible_host` or the address from `vms.yaml`).

### Role Structure
- `tasks/main.yml` — entry point, may include subtask files
- `defaults/main.yml` — default variables (use `| default('')` for secrets that may not be set)
- `templates/` — Jinja2 templates (`.j2` extension)
- `handlers/main.yml` — handlers (restart services, etc.)
- `meta/main.yml` — dependencies (rare)
- No `vars/main.yml` unless truly constant (prefer defaults for overridability)

### Template Conventions
- Docker-compose templates live at `roles/<role>/templates/docker-compose.yml.j2`
- Templates are deployed to `/opt/<service>/` on the target VM
- Use `mode: '0600'` for files containing secrets, `'0644'` for non-sensitive configs
- Use `replace()` for literal string substitutions — do NOT use `regex_replace` with backslash escaping across YAML/Jinja2 layers

### Task Conventions
- Use FQCNs: `ansible.builtin.template`, `community.docker.docker_compose_v2`, etc.
- Use `run_once: true` for tasks that should only execute on one host in a play
- Use `delegate_to: localhost` + `become: false` for local/API operations
- `is succeeded` returns TRUE for skipped tasks — always add `is not skipped` when gating on a registered variable from a conditionally-skipped task
- `docker_compose_v2`: `restarted` is a `state` value, not a separate parameter

### Tags Strategy
Both `infrastructure.yml` and `services.yml` have play-level tags for targeted deploys:

**infrastructure.yml tags:** `phase1`, `phase2`, `phase3`, `dns`, `adguard`, `infisical`, `proxmox-backup`, `unifi`, `apt-cache`, `pxe`, `control`, `pdm`, `monitoring`, `monitoring-users`, `users`

**services.yml tags:** `nvidia-licensing`, `plex`, `minio`, `squid`, `lancache` (homepage, plex-services and the games host moved to the cluster — `make talos-homepage`, `make talos-plex-services`, `make talos-games`)

**How tags work with pre_tasks:**
- All `pre_tasks` blocks have `tags: [always]`, so secrets/VLANs/facts always load regardless of `--tags` filter.
- The `infisical_client` role's "Enable and start infisical-agent" task is tagged `always` — even when filtering by tags within a play, the agent is guaranteed to be running.
- Tags filter at the **play** level: `--tags docker` runs only the docker play (and its pre_tasks).

**Usage via Makefile:** Pass `TAGS=` to any ansible target:
```bash
make ansible plex TAGS=plex              # only the plex play
make ansible-infra TAGS=phase1           # infrastructure phase 1 only
make ansible-services TAGS=plex,homepage  # plex + homepage plays
```

### Variable Sourcing Priority
1. `ansible/inventory/host_vars/<vm>.yml` — per-VM config
2. `ansible/group_vars/all.yml` — global vars (overrides role defaults!)
3. `ansible/roles/<role>/defaults/main.yml` — role defaults
4. `ansible/group_vars/local/all.yml` — local overrides (gitignored)

## Docker Conventions

- **Compose file location:** `/opt/<service>/docker-compose.yml` on the VM (e.g., `/opt/docker/`, `/opt/plex/`, `/opt/minio/`). Each role defines a `*_base_dir` default variable for its path.
- **Restart policy:** `restart: unless-stopped` for all containers
- **Logging:** Docker daemon configured for syslog to OpenObserve (`configure_docker_logging.yml`)
- **NFS volumes:** Created as named Docker volumes with `driver_opts` for NFS. Plex-services auto-recreates volumes if mount options change.
- **NFS tuning:** `nfsvers=4.1,hard,intr,noatime,rsize=1048576,wsize=1048576,nconnect=4` (Synology negotiates down to 128KB rsize/wsize)
- **Restart on change:** Register template results, restart containers only `when: <registered_var> is changed`
- **env_file for Infisical agent secrets:** Path is `/etc/infisical/secrets/<name>.env`, rendered by the Infisical agent from Go templates in `infisical_client/templates/secrets/`

## Terraform Conventions

### Structure
- Single fleet project at `terraform/` with `main.tf`, `vm-configs.tf`, `variables.tf`, `outputs.tf`, `provider.tf`
- **`terraform/hosts/`** is a SEPARATE root (own local state) for the host/cluster plane — host networking (bpg `network_linux_bond/bridge/vlan`), and later cluster options / Ceph pool / SDN / ACME (ADR-0002, WP1). Two-stage apply ordering: `hosts/` (rare) before `terraform/` (routine). Answer-file install → `make node-iso` (bake) → `make node-bootstrap` (token) → `make hosts-apply ENDPOINT=…` → `make proxmox-hosts` (WP2 Ansible cluster plane: mgmt re-home, corosync rings, Ceph with the ADR-0009 public/cluster split, keepalived VIP — needs `inventory/proxmox.yaml` + the WP2 fields in `host-bindings.yaml`). It manages the X710/82599ES bond→`vmbr0`→VLAN 20/40 and never touches the VLAN 30 install link, the ConnectX 25G port (switched Ceph VLAN 21 — [ADR 0014](docs/decisions/0014-ceph-cluster-network-rides-switched-25g-sfp28-ports-not-a-switchless-mesh.md)), or ring1. Baked `answer-*.toml` + `nodes.auto.tfvars` + `network-data/local/host-bindings.yaml` are gitignored (carry MAC/IP/root-hash); templates + `*.example` are tracked. See `terraform/hosts/README.md`.
- `vm-configs.tf` defines all VMs in `local.infrastructure_vms` and `local.services_vms`
- **Storage entries vs backup jobs (boundary amended 2026-08-11, B3):** PVE *storage entries* are cluster-plane and Ansible-owned — bpg has no storage-definition resource, so Ceph storage defs come from the WP2 `proxmox_host` role and the PBS entry from `make backup-finalize` (once per cluster, after PBS provisioning). PVE *backup jobs* (`backup_jobs` var → `backup-jobs.tf`) live in the fleet root because they select fleet VMIDs and change with fleet composition; `backup-finalize` is their one-time prerequisite. Putting jobs in `terraform/hosts/` would couple the rare-apply host root to every fleet change.
- `vars.auto.tfvars` for non-sensitive config only

### Secrets Flow
- Makefile reads `bootstrap.sops.yml` via `sops -d --extract` and exports as `TF_VAR_*` env vars
- Top-level `export` in Makefile — do NOT use `define`/`$(call)` with `$(eval export)`, it doesn't propagate
- `terraform refresh` updates state but NOT outputs — use `terraform apply -refresh-only -auto-approve`
- Targeted applies (`-target`) don't recompute `ansible_inventory_yaml` either — a brand-new guest never lands in `vms.yaml` from the targeted apply alone; `make build` runs a refresh-only apply before `inventory` for exactly this reason (found building apt-cache, 2026-08-11)

### VM Configuration Patterns
- Each VM: `name`, `vm_id`, `vlans` (list), `ip_offset`, `cores`, `memory`, `disk_size`
- `protected = true` for critical VMs (currently: infisical). Override with `var.unprotect`
- `lifecycle.ignore_changes` on cloud-init file IDs to prevent VM recreation on template changes
- Management VLAN is always first interface; `vm_id` → static IP, no `vm_id` → DHCP
- MAC addresses: `52:54:00:{random}:{vlan_id/256}:{vlan_id%256}`
- Use `coalesce()` carefully — not short-circuit. Use `try()` for fallbacks referencing resources that may not exist during `-target` operations

### Inventory Generation
- `make inventory` runs `terraform output -raw ansible_inventory_yaml > ansible/inventory/vms.yaml`
- Always runs `clean-ssh` first to remove stale SSH keys

## Make Targets

| Target | What it does |
|--------|-------------|
| `make apply` | Full deploy: terraform → inventory → ansible-all |
| `make bootstrap` | First-time: terraform (adguard+infisical only) → inventory → ansible-bootstrap |
| `make plan [vm]` | Terraform plan (optionally scoped to one VM) |
| `make build <vm>` | Terraform + inventory + ansible for a single guest or a numbered pair (`make build adguard` builds `adguard1`+`adguard2` via `group-targets`; `scripts/guest-targets.sh` resolves type, HA, and cloud-init targets; resolution failure aborts before terraform runs) |
| `make rebuild <vm>` | Replace guest in one atomic `-replace` apply → clean SSH → ansible (reconciles VM↔LXC conversions + stale HA registrations) |
| `make backup-jobs` | Apply only `proxmox_backup_job.jobs` after editing `backup_jobs` in `vars.auto.tfvars` (re-author as replacements land; never a bare apply while old-shape guests sit outside state) |
| `make sdn-apply` | Apply only the network module (SDN zones + VNETs from `vlans.yaml`) — how a new guest VLAN lands without a bare fleet apply. `REAPPLY=1` re-creates the SDN applier when the cluster shows a VNET `state: new` (the applier only re-runs for changes in the same plan) |
| `make data-volumes` | ADR 0020 holder step: apply the holder VM 900 ALONE, then `playbooks/data-volumes.yml` formats (`mkfs.ext4`, blkid-guarded) and chowns each new volume to the CT-root mapped owner. Run after adding a `data_volumes` entry and BEFORE `make build` of its consumer — never both in one apply |
| `make ansible <vm>` | Run site.yml limited to one host (supports `TAGS=`) |
| `make docker-config <vm>` | Deploy compose+config only, skip user/group/package/API setup |
| `make ansible-all` | Run full site.yml (supports `TAGS=`) |
| `make ansible-infra` | Run infrastructure.yml only (supports `TAGS=`) |
| `make ansible-services` | Run services.yml only (supports `TAGS=`) |
| `make update` | apt upgrade + docker pull on all hosts |
| `make vps-deploy` | 3-phase: terraform (SSH open) → ansible → terraform (SSH closed). Initial provisioning only — phase 2 needs root SSH, which hardening removes |
| `make vps-ansible` | Day-2 VPS deploy over the tunnel (survives the wg restart via async handler) |
| `make vps-close-ssh` | Close the Vultr provisioning SSH rule after a failed vps-deploy |
| `make vps-rebuild` | Destroy + rebuild VPS |
| `make rebuild-infisical` | Destroy + rebuild Infisical VM (handles stale credentials) |
| `make infisical-seed` | Restore Infisical from backup (disaster recovery only) |
| `make infisical-backup` | Export Infisical → SOPS backup |
| `make refresh-identity` | Refresh Infisical machine identities (supports `LIMIT=`, `FORCE=true`) |
| `make clean` | Destroy all (protected VMs preserved). `FORCE=true` to destroy everything |
| `make node-answers` | Bake the PXE answer file for every node in `host-bindings.yaml` (ADR 0026); `make ansible pxe` ships them |
| `make node-arm NODE=<n>` | **Arm a node for PXE install (the wipe confirm).** The answer server refuses (404 → no wipe) until armed; auto-expires after `ARM_MINUTES` (default 30). `make node-disarm NODE=<n>` cancels; `make node-arm-status` lists armed nodes. iPXE also defaults to local-disk boot, so an accidental PXE never auto-installs |
| `make proxmox-hosts` | WP2 cluster plane on the `proxmox` group (`inventory/proxmox.yaml`, first host creates the cluster): resolver+repos → mgmt re-home to `vmbr0.30` + rings + ceph link → cluster create/join (API) → QDevice while 2-node (qnetd host = `cluster.qdevice_addr`, prepped hands-off) → keepalived VIP (`cluster.vip_cidr`) → logging/mounts/firmware. Run after `make hosts-apply` on every node; second run = 0 changed (proven 2026-08-19). `TAGS=mounts` scopes it to the NAS host mounts (`cluster.nas_mounts` in `host-bindings.yaml`, ADR 0017) |
| `make adguard-pause MINUTES=n` | Disable AdGuard filtering on every resolver instance for n minutes (default 10) — POST `/control/protection` with the Infisical `/homepage` admin creds (ADR 0029) |
| `make dns-records` | Push every inventory-derived name into the flat service zone over RFC 2136 (ADR 0040): guests + nodes (A + PTR), VIPs, MetalLB names, mirrored public records; both inventories + `host-bindings.yaml`; second run = 0 changed |
| `make talos-dns` | external-dns (RFC 2136 → BIND VIP, TSIG from Infisical `/infrastructure`): every Ingress host becomes an A record in the service zone |
| `make talos-jellyfin` | P5d (ADR 0040): Jellyfin at `jellyfin.<media domain>` — ceph-rbd config + cache PVCs, the NAS media export as a read-only NFS PV, plain Ingress (no forward-auth: TV apps), LDAP-Auth plugin against `ak-outpost-ldap.authentik-ext.svc` configured through the API from inside the pod (wizard, plugin install, Movies/TV/Music libraries); second run = no restart. Needs `make talos-authentik REALM=external` first |
| `make talos-authentik` | P5c (ADR 0040): both authentik realms (`REALM=internal\|external` for one) — generates the realm's Infisical folder through the API on first run, Postgres + nightly PBS dump, helm chart via `helm_apply`, blueprint applied with `ak apply_blueprint`; internal = `auth.<service domain>` Ingress + `authentik-forward-auth` Middleware + OIDC providers, external = Service only + the LDAP outpost. Precedes `make talos-monitoring` on a fresh cluster (Grafana's client secret) |
| `make talos-plan` / `make talos-build` | Plan / targeted-apply the three Talos control-plane VMs (`terraform/talos.tf`, incl. the worklab alias) and write `kubernetes/talos/.secrets/nodes.json` |
| `make talos-secrets` / `talos-apply` / `talos-bootstrap` | Pull (or generate once) the cluster secrets from Infisical `/talos`; render + `apply-config` every node (maintenance nodes via their DHCP lease, configured nodes in place); bootstrap etcd, fetch kubeconfig, wait Ready |
| `make talos-csi` / `make talos-smoke` | Deploy ceph-csi RBD (`client.csi-rbd` key read from the first cluster node) + the default `ceph-rbd` StorageClass; smoke = PVC Bound + pod write/read |
| `make talos-ingress` / `talos-infisical` / `infisical-smoke` / `talos-homepage` | P4a (ADR 0035): Traefik on offset 65 with the Let's Encrypt wildcard as default cert (ADR 0040 P5b; `ISSUER=letsencrypt-staging` for a rehearsal; needs `talos-infisical` first); Infisical operator + the bootstrap `infisical-universal-auth` secret (smoke = sync one `/shared` key and delete it); homepage Deployment/ConfigMap/InfisicalSecrets/Ingress on `homepage.<service domain>` + `home.<service domain>` |
| `make talos-plex-services` / `plex-services-migrate FROM=<ip>` / `plex-pbs-image` | P4c (ADR 0037): the arr stack (`kubernetes/plex-services/`, NFS media PV, 8 UI hostnames on the ingress); one-off state copy from the old guest; build+push the `proxmox-backup-client` image for the pg-backup CronJob |
| `make talos-games` / `games-migrate FROM=<ip>` | P4d (ADR 0038): Valheim + PlayFab sidecar on MetalLB offset 67 + Kiwix over NFS; player-gated one-off state copy from the old guest |
| `make talos-monitoring` / `monitoring-migrate FROM=<ip>` / `monitoring-users` | P4b (ADR 0036): the monitoring stack (`kubernetes/monitoring/`, syslog LB on offset 66, five UI hostnames rewritten to the ingress); rsync the old guest's `/var/lib/monitoring/*` into the PVCs + repoint Kuma's rows (stack scaled to 0, old compose down); `monitoring@pve` + token → Infisical `/monitoring/proxmox_token_value` |
| `make talos-lb` / `talos-certs` / `talos-trust` / `talos-registry` / `registry-smoke` / `talos-arc` | P3b (ADR 0034): MetalLB L2 pool; cert-manager (DNS-01 self-check pinned to public resolvers) + Infisical PKI project/root + the signed `homelab-ca` intermediate (exports the root CA; ADR 0039) + the `letsencrypt`/`letsencrypt-staging` ClusterIssuers fed by an `InfisicalSecret` on `/infrastructure` (ADR 0040 P5b); re-apply Talos configs so nodes trust the CA; Zot + AdGuard rewrite; pull busybox through Zot from a node; ARC controller + scale sets + reaper (secrets from Infisical `/github-runner` and the hosts-root `ci_api_token`) |
| `make uptime-kuma` | Converge Uptime Kuma's monitors + ntfy channel from `ansible/playbooks/uptime-kuma.yml` via the `goodolclint.uptime_kuma` collection (localhost play, full pre_tasks for `hostvars`/`vlans.yaml`/Infisical); `CHECK=1` = `--check --diff`, the acceptance test is 0 changes. Also the last step of `make monitoring-migrate` |
| `make apt-proxy` | ADR 0021 client half on every node, worklab and guest (`apt_proxy` role, both inventories, `LIMIT=`): `/usr/local/bin/apt-proxy-detect` + `apt.conf.d/01proxy` `Proxy-Auto-Detect` — cache when `hostvars['apt-cache']` answers on 3142, else `DIRECT`; https URIs always DIRECT. Nodes also get it from `proxmox_host` (`make proxmox-hosts` now loads `vms.yaml` when present); an LXC only gets it here |
| `make nut-clients` | Deploy NUT `upsmon` secondaries to the physical hosts (`nut_clients` group in `inventory/proxmox.yaml`; server = pfSense NUT package, `docs/pfsense-nut.md`) |
| `make setup-hooks` | Install pre-commit hooks |
| `make init` | Create venv, install deps, terraform init, galaxy install |

## Change Discipline

1. **Read the role and its templates** before modifying anything
2. **Check `docker-config.yml`** — if the service has a docker-config section, verify your changes work without the full role (docker-config skips package/user/group setup). Secrets no longer need flattening but templates must still render correctly in this lightweight context.
3. **Check the `generate_secret.yml` pattern** when adding new secrets — follow the existing convention exactly
4. **Test idempotency** — running the same playbook twice should produce zero changes on the second run
5. **Never modify Terraform-generated files** — `ansible/inventory/vms.yaml` is generated by `make inventory`
6. **Pre-commit hooks run automatically** — trailing-whitespace and end-of-file-fixer modify staged files; re-stage after hook failures

## Documentation Discipline

### README
- Update `README.md` any time you change: make targets, prerequisites, first-time setup
  steps, supported VMs, or the overall architecture
- Do NOT update README for internal refactors that don't affect how a user operates the repo
- If you're unsure whether a change warrants a README update, err on the side of updating it

### CLAUDE.md
- Update `CLAUDE.md` any time you establish a new convention, discover a new gotcha, or
  resolve one of the Known Inconsistencies
- When a Known Inconsistency is resolved, move it to a `## Resolved` subsection with a
  note on what changed — don't silently delete it
- Add to What Never To Do any time you encounter a mistake that wasted meaningful time

## Git Discipline

### When to commit
- After each logical unit of work is complete and verified — not mid-task
- After any change that touches more than one file
- Before running destructive operations (`make rebuild`, `make clean`, terraform destroy)
- After updating CLAUDE.md or README.md
- Never commit in the middle of a multi-phase task — complete the phase first

### Commit message format
- Use imperative present tense: "Add homepage Infisical agent templates" not "Added..."
- First line: 50 chars max, no period
- If the change warrants explanation, add a blank line then a body
- Reference the affected role/component: "ansible/roles/homepage: add agent env_file support"

### What never to commit
- Any file matching `*.sops.yml` that hasn't been encrypted with `sops --encrypt` first
- `vlans.yaml`, `private_bindings.yaml`, `infisical-backup.yml` (gitignored — if git
  somehow picks them up, stop immediately)
- Generated files: `ansible/inventory/vms.yaml`
- Anything with a raw secret value — pre-commit hooks should catch this but don't rely on them

### Before committing
- Run `make setup-hooks` if hooks aren't installed
- Pre-commit hooks will auto-fix whitespace/EOF — re-stage modified files after hook runs
- If gitleaks fires, do NOT use `--no-verify` to bypass — fix the leak first


## What Never To Do

- **Never hardcode IPs in templates** — derive from `ansible_host`, VLAN prefix replacement, or `hostvars`
- **Never put a specific RFC 1918 address or the internal domain in a tracked file** — the repo is public; IPs/subnets/domains are bindings (gitignored or derived from `network_data.*` facts). The `security_guardrails.sh` pre-commit hook blocks added lines containing them (example files and generic supernets allowlisted) — WP6
- **Never leave a node armed for PXE install, and never assume PXE is wipe-safe without the arm gate** — the answer server only serves an install answer (→ unattended disk wipe) while `answers/armed/<node>` is present and unexpired (`make node-arm`). Default is disarmed: an accidental PXE (boot-order fallthrough, stray BootNext, mis-clicked *Reset to PXE*) gets a 404 and does NOT wipe. `boot.ipxe` also defaults to **local-disk** boot (never the installer) on timeout. Both were added 2026-08-19 after the live-fleet restore made an accidental re-PXE catastrophic. Arm right before an install and let it auto-expire; if you arm and don't install, `make node-disarm`
- **Never flip `tf-node-install`'s native VLAN — worklab's untagged mgmt (`.30.75` on `vmbr0`/`nic0`, Pro-Max p46) rides that shared profile** — AMT's move to the vlan10 OOB plane (ADR 0030) is the separate `tf-node-oob` profile bound per port (`unifi-ports.yaml`), so the MS-01s flip one at a time and worklab/msi stay put. Order per node: re-IP AMT over the old path first (it goes dark), then `unifi-apply` the port; verify from a vlan10 host (control 212) — the operator Mac cannot reach vlan10 by policy, so a failed probe from the workstation proves nothing
- **Never re-enable Secure Boot on a node installed with SB off — it boots systemd-boot, which Proxmox does not SB-sign.** `proxmox-boot-tool` picks the on-disk bootloader at install/init time: SB-active → shim+grub (Proxmox-signed, SB-capable); SB-off → **systemd-boot** (unsigned). The MS-01s installed under SB-off (the iPXE transport needs it), so their ESPs are systemd-boot mode and enabling SB in BIOS makes the firmware reject the loader → the node fails to boot (recovery: disable SB again via AMT/JetKVM). Verified on ms-01b 2026-08-19: host ESP holds only `/EFI/systemd/systemd-bootx64.efi`, while `shim-signed`+`grub-efi-amd64-signed` sit installed-but-unused. This is NOT the PXE/efinet issue (that is transport-only) and NOT a dead end: `proxmox-boot-tool init /dev/<esp> grub` on each ESP (booted SB-off) deploys the signed shim+grub with systemd-boot as fallback, then enable SB in BIOS — no reinstall (Proxmox Host_Bootloader wiki). Prove it on the disposable msi install before touching a live keep-node; until then SB stays OFF fleet-wide
- **Never reconfigure host networking over the interface carrying the PVE API session** — `terraform/hosts/` applies run over the stable VLAN 30 mgmt link and never touch it; a re-apply must never drop the link Terraform is talking over (ADR-0002)
- **Never DHCP an LXC** — static IPs only, so plan-time inventory is deterministic (ADR-0003)
- **Never put secrets in `vars.auto.tfvars`** — pre-commit hook blocks this
- **Never set `vlan_id` on SDN VNETs** — only for physical bridges (`vmbr*`)
- **Never use `regex_replace` for literal string substitution** — use `replace()` instead
- **Never write a source-based `routing-policy` rule with the interface prefix** — it must be `/32`. A `from: <ip>/24` rule matches every host in the subnet, not just this one, so any *forwarded* packet (a reply to a Docker container) from a neighbour on that VLAN is looked up in the per-VLAN table, which has no bridge route, and is sent back out the gateway. Symptom: containers reach the internet fine but hang on every connection to a host on that interface's own subnet
- **The AdGuard config template is `when: not _adguard_config.stat.exists` — "initial only"** — AdGuardHome rewrites its own YAML at runtime, so editing `adguardhome.yaml.j2` affects fresh VMs only. Changing a setting on a running host means editing the live config (stop service → edit → start) or using the AdGuard API (the role converges the upstream list through `/control/dns_config` for exactly this reason); a redeploy of anything else silently no-ops. When editing the live config, DNS rewrites live under `filtering.rewrites` (schema ≥ 23) — keys AdGuardHome doesn't recognize (e.g. a hand-added `dns.rewrites`) are silently discarded on the next config write, so verify the edit survived a service restart. The AdGuard web-UI admin password exists only as a bcrypt hash in that config — it was never seeded into Infisical `/homepage`, so the API route needs the operator to supply it
- **Never write a bare `$` in a docker-compose `environment:` value that must expand inside the container** — compose interpolates `${...}` at parse time on the Ansible host and silently substitutes empty. Escape as `$$` (the Valheim `ON_VALHEIM_LOG_FILTER_CONTAINS_*` hooks carried this on compose; in a Kubernetes manifest only `$(VAR)` is expanded, so `kubernetes/games/app.yaml` uses a bare `$`)
- **Never point a container running as a non-root user at a bare `/etc/infisical/secrets/<file>` via a `*_file` directive without declaring `mode:` on the agent template** — the Infisical agent writes `0600 root` and has no permission option, so `prom/*` images (which run as `nobody`) get `permission denied`. Prometheus reports this at WARN and retries forever, so `remote_write` died silently for months. Declare `mode: '0644'` in `infisical_agent_templates` and re-apply it in the template's `post_command`, since a rotation rewrites the file
- **Never put a literal comment line in an Infisical agent template that renders a *bare value*** — the comment becomes part of the value. Use `{{- /* ... */ -}}` Go-template comments with whitespace trimming (see `openobserve-password.tpl.j2` and `alert-ntfy-url.tpl.j2`)
- **Never enable netconsole without also raising `console_loglevel`** — netconsole is a *console* driver, so it only transmits what passes console-level filtering. PVE hosts ship with `kernel.printk` console level 1–3 (observed 2026-07-30: pve `1`, worklab `3`), meaning only EMERG/ALERT/CRIT are emitted. NVMe controller failures and `EXT4-fs error` are **KERN_ERR (3)** — precisely the messages a disk-death postmortem needs, and precisely the ones the default drops. Set **`kernel.printk = 5 4 1 7`** via an `/etc/sysctl.d/` drop-in alongside the module, or netconsole ships a stream that is silent exactly when it matters. Level 5 is deliberate, not a round number: it passes `err` (3) and `warn` (4) — the NVMe/EXT4 failure classes — while filtering `notice` (5) and `info` (6). Measured on worklab 2026-07-30, one boot emits 51 err + 44 warn against 1561 info + 62 notice, so level 7 floods both the netconsole stream *and the physical console*: a late `notice` such as `sd 1:0:0:1: [sda] Attached SCSI disk` overwrites the getty login prompt, leaving the screen stuck on a boot message until someone presses Enter. **`console_loglevel` is global — netconsole and the VT share one filter**, so quieting the screen necessarily reduces what ships off-box; there is no per-console split. Verify with `<3>`/`<4>`/`<5>`-prefixed `/dev/kmsg` writes (the first two must arrive, the third must not); a bare write is KERN_INFO (6) and tests a stricter threshold than the messages you actually care about
- **Never assume an RFC 3164 sender's clock agrees with the collector's timezone** — RFC 3164 timestamps carry no zone, so syslog-ng interprets them as *receiver-local*. A device running UTC (JetKVM ships with no `/etc/TZ`) lands every message 5 h in the future on a CDT collector, and **the skew is invisible**: normal dashboards and `end_time=now` queries silently exclude future-dated rows, so the stream looks empty rather than wrong. Diagnosed 2026-07-30 after wrongly blaming a firewall. Fix at the source (`/etc/TZ` + `TZ=` on the syslogd process), and when a device's syslog "isn't arriving", widen the query window into the **future** before blaming the network. Corollary for querying: RFC 3164 puts the tag in `program`, not `message` — grep the whole row, not just `message`
- **Never point a device's ordinary syslog at the netconsole port (5515)** — 5515 is deliberately parser-free and dumps straight into the `netconsole` stream with no field extraction. Ordinary syslog belongs on 5514. Conversely, netconsole must never share 5514: `syslog-parser()` does not reject unframed printk, it parses it as RFC 3164 and eats the first token, so `nvme nvme0: Removing after probe failure` silently becomes `nvme0: Removing after probe failure`. UniFi models these as two separate destinations (`unifi_setting.syslog.netconsole_*` vs `.ip`/`.port`) — keep them separate. Pinned by `scripts/test_axosyslog_routing.py`
- **Never expect netconsole to carry a hostname, and never reach for `userdata` to add one** — netconsole's wire format has no hostname field, so the `netconsole` stream's `host` is the *source IP* that axosyslog stamps on arrival. The configfs `userdata` knob looks like the fix and is not: measured on worklab 2026-08-12, with `extended=0` (the default, and what the module-arg target uses) userdata is **silently dropped** — the message arrives byte-identical to one from a target with no userdata at all. Setting `extended=1` does deliver it, but as a continuation line *inside the message body* (`11,1776,1340284657,-;my message\n hostname=worklab`), so `host` is still the IP and a collector-side parser is now required to lift it out — while every line has gained an extended-format prefix that the deliberately parser-free 5515 lane exists to avoid. Names belong at the collector: `use-dns(yes)` on the `s_netconsole` source once PTR zones exist (open item in [docs/ms01-cluster-iac-plan.md](docs/ms01-cluster-iac-plan.md)). PTR also beats the sender's own hostname, since a PVE node cannot be renamed once it has guests — worklab and pve both report `pve`, and the source IP is the only thing telling them apart
- **Never leave syslog-ng's default `max-connections(10)` on a fleet-wide syslog source** — each Docker daemon holds one TCP connection per logging container, so the fleet blows past 10 and every new sender is rejected with ingestion silently stalling
- **Never conclude a stalled `make update` is fixed because a hand-run `apt-get upgrade` succeeded** — `update-all.yml` uses `upgrade: dist`, and plain `upgrade` holds back exactly the packages that cause the stall. On unifi 2026-08-04 the manual run moved 2 cached packages in 1 second while `dist-upgrade` wanted a **new kernel** (6.8.0-137, 10 packages, ~150 MB), so the two runs were never comparable. apt has no minimum-rate abort, so a transfer crawling at ~19 KB/s is indistinguishable from a hang — the play sits silent with no output for as long as it takes. Diagnose from the guest, not the terminal: a `Start-Date` in `/var/log/apt/history.log` means the transaction began, and its **absence** while `AnsiballZ_apt.py`'s sudo session is still open means it is still *downloading*; confirm with the growing partial in `/var/cache/apt/archives/partial/` (size vs. mtime gives the actual rate). Root cause was the `archive.ubuntu.com` IPv4 path, not the guest — see the apt-source open item in [docs/ms01-cluster-iac-plan.md](docs/ms01-cluster-iac-plan.md)
- **Never probe the UniFi controller with credentials from a monitor** — its `/status` gate now passes on port 11443, so authenticating risks locking the admin account. TCP port check only, and do not run the `monitoring_users` role
- **Never use `iif`/`oif` in nftables rules for interfaces created after boot (wg0)** — they resolve interface indexes at ruleset load, so the whole ruleset fails to load when the interface doesn't exist yet, leaving the box unfiltered. Use `iifname`/`oifname`. Cost a full VPS lockout on 2026-07-27
- **Never synchronously restart WireGuard over the tunnel it carries** — the service task dies between `down` and `up` when its own SSH session severs, and the box is unreachable (VPS sshd binds tunnel IPs only). The `vps_wireguard` restart handler is `async: 60 / poll: 0` fire-and-forget; keep it that way. Recovery required a full `make vps-rebuild`
- **Never trust computed tunnel MTU arithmetic — measure with DF-set probes** (procedure in docs/pfsense-wireguard-vps-peer.md). The IPv6 relay path drops WG UDP outers ≥ 1458 even though raw ICMPv6 passes at 1462; the IPv4 path carries full 1500 outers. An IPv6 tunnel endpoint costs 80 bytes of encapsulation, not IPv4's 60 (ADR 0013)
- **Never debug a dead VPS tunnel past the pf state table** — after a VPS rebuild pfSense can keep sending handshake initiations into a stale inbound `if-bound`/`reply-to` UDP state for `:51821` that the old instance created; zero packets leave the WAN, nothing is logged, and pfSense's own keepalives refresh the state forever. `pfctl -k <vps_ip>` brings the tunnel up in seconds; keys/endpoint/MTU were all fine (2026-08-22, cost two `make vps-rebuild`s). Procedure in [docs/pfsense-wireguard-vps-peer.md](docs/pfsense-wireguard-vps-peer.md)
- **Never peer WireGuard against the VPS instance IPv6, and never set narrow AllowedIPs** — the SLAAC address dies on every rebuild (new MAC), and AllowedIPs narrower than the internal supernets makes cryptokey routing silently drop LAN-sourced SSH/pings while breaking VPS-originated telegraf/rsyslog (ADR 0013)
- **Never `source` an Infisical agent env file in a shell script** — values are rendered unquoted for docker's `env_file:` parser (which does no shell expansion), so any `$` in any secret aborts the whole script under `set -u`. `opensubtitlescom_password` containing `$5` killed the nightly postgres PBS backup silently from 2026-03 to 2026-08 (cron exit swallowed by the log redirect). `grep` the specific key instead, and verify periodic backups by SNAPSHOT RECENCY on the datastore, never by the job exiting 0
- **Never assume a VLAN has a router at `.1`, and never let a gateway-less VLAN own a guest's default route** — the storage VLAN has no router (ARP at `.1` is INCOMPLETE, measured 2026-08-12). The module's old "first non-management VLAN wins" rule put a two-legged guest's default route there, which fails *silently and expensively*: no internet → cloud-init's `packages:` install dies → `qemu-guest-agent` never starts → bpg blocks indefinitely waiting for an agent IP, so the guest sits at a login prompt while terraform shows only "Still creating…". VLANs now declare `gateway:` in `vlans.yaml` (explicit `null` = no router, distinct from an absent key) and the module skips gateway-less legs. **That `gateway: null` lives in a gitignored file** — restoring or regenerating `vlans.yaml` without it re-arms the trap (W6, ADR 0017 makes PBS the exposed guest at WP4)
- **Never read a role default through `hostvars[other_host]`** — role defaults/vars are per-host template context, not hostvars; under ansible-core 2.20 the lookup hard-fails (`HostVarsVars has no attribute`) instead of templating lazily. Anything an orchestrating `run_once` task needs from *other* hosts must be `set_fact`'d first (see `proxmox_host/tasks/cluster.yml` "Publish per-node addresses as facts"). Cost a failed Day-1 cluster create 2026-08-19
- **Never pass the master's ring0 IP as `proxmox_cluster` `master_ip`** — the module's re-run idempotency key is compared to `/cluster/status` node `ip` fields, which PVE derives by resolving each nodename via `/etc/hosts` = the **mgmt** IP written by the answer file. ring0 made every second `make proxmox-hosts` fail "Node is already part of a cluster" (the W4 nested harness had mgmt==ring0 and masked it). Rings go in `link0`/`link1`
- **Never tag ring1 on a NIC that sits on an access port** — the WP5 `corosync_ring1` profile is access (native 32); UniFi drops tagged frames matching the native VLAN, so `nic0.32` carried an address and no traffic. Dedicated ring1 NIC = untagged address on the NIC; only a bond-carried ring1 (`ring1_nic: vmbr0`, msi) is a VLAN 32 sub-if (`interfaces.j2` keys off the name)
- **Never shell `pvecm qdevice setup` without pre-trusting the qnetd host key and pre-authorizing the node keys** — it runs `ssh-copy-id` interactively; under Ansible (no tty) an unknown host key hangs the task forever, not fails. `qdevice.yml` now does both on the qnetd host via `delegate_to`
- **Never assume a VM-shaped network task works in an LXC** — `dns_config`'s ADR 0012 drop-in targets `10-netplan-<iface>`, which netplan emits (into `/run`) **only on VMs**; PVE configures a container's link as `eth_vlan<n>.network`, so the drop-in overrides nothing, and the `netplan apply` handler *hard-fails* in a container because it shells out to `udevadm control --reload`. It is now guarded with `when: ansible_virtualization_type != 'lxc'`. **Skipping is not the same as solving:** a container's `/etc/resolv.conf` is a static file PVE writes from `dns { servers = var.dns_servers }`, so it still receives the public bootstrap resolver and nothing strips it — an open WP4 decision, not a closed one
- **Never let a role write DNS config and defer the apply to a handler** — handlers run at end-of-play while every task in between depends on DNS, so one bad resolver latches: the next run writes the *corrected* file, dies at the first network-touching task, and never reaches the handler. Recovery needs a hand-run `netplan apply`. Apply in-role instead (see the next two rules for what that must and must not look like), and gate the apply on a `/run` pending-marker rather than on this run's file changes — gating on the changes alone rebuilds the same latch one level down, because a failed apply leaves the files converged and the next run skips it
- **Never assume `community.docker` modules work on an LXC just because they work on the VM fleet** — the Ubuntu cloud image ships `python3-requests`; the 26.04 LXC template does not, so `docker_container_info`/`docker_compose_v2` fail with "Failed to import the required Python library (requests)" on a fresh CT. Install `python3-docker` in the role (plex_services does) — same shape as the `python3-debian` rule below
- **Never switch a role from `apt_repository` to `deb822_repository` without `install_python_debian: true`** — deb822 needs `python3-debian`. The Ubuntu/Debian *cloud images* ship it; the *LXC templates* do not, so the role works on VMs and fails on containers with "python3-debian is not installed"
- **Never assume `get_url` is offline in steady state — it is not without `checksum:`** — `get_url.py` takes its "file already exists" early exit only under `not force and checksum and not checksum_mismatch`; with no checksum it always reaches `url_get()`, so an unguarded fetch makes the remote host a hard per-run dependency of every play that role appears in. Keep a `stat` guard (`when: not <key>.stat.exists or <key>.stat.size == 0`) rather than pinning a checksum for vendor keys, which rotate. The `size == 0` arm also heals zero-byte files left by any earlier `wget -O`. Same trap for `gpg --dearmor -o`: pair the size guard with **removing `creates:`**, because `creates:` skips whenever the path exists — zero-byte included — and silently defeats the guard
- **Never use `meta: flush_handlers` to sequence one role's apply** — it flushes EVERY pending handler, including other roles'. `dns_config` is not first in every play (`infisical_client` precedes it in seven), so flushing there fires `restart infisical-agent` before the service role has written its compose file, and the agent's `post_command` restarts hit a nonexistent path. Apply in-role with a direct command gated on a registered change; the barrier then belongs to the role instead of to each caller's role ordering
- **Never pass `deb822_repository` a URL for `signed_by`** — given a URL it re-fetches on EVERY run (`write_signed_by_key` early-exits only for `os.path.isfile`) and raises an unhandled `RuntimeError` when the fetch fails, so one upstream blip aborts the role on every host it touches. Fetch the key with `get_url` to a path and pass the path; steady state then does no network at all. Same reason `shell: wget -O <dest>` is banned for keys — wget creates the destination before the transfer succeeds, so a failure leaves a **zero-byte keyring** that any `stat.exists` guard then treats as present forever, latching a broken repo with no self-heal. `get_url` is atomic; use it
- **Never let a role change an apt source without refreshing the cache in the same role** — `update_cache` riding an install task that is gated on the package being absent means the swap is never validated on hosts that already have it. The role exits green and the malformed source surfaces later at an unrelated apt call on a different play, which is the hardest class of failure to attribute
- **Never use `ansible.builtin.apt_repository` against a Debian 13 guest** — trixie removed `apt-key` and the genericcloud image ships no `gpg`, so the module aborts at startup *before* it reads the repo line, even though `signed-by=` means no key import is needed. Ubuntu noble ships gpg, which is why this never surfaced on the live fleet. Use `ansible.builtin.deb822_repository` (ansible-core builtin — no new dependency, and don't install gpg just to keep a deprecated path alive). Note deb822 writes `.sources`, so a host carrying an old `.list` for the same repo ends up with both and apt warns about duplicates
- **Never model an OS-dependent value as a global scalar when guests roll one at a time** — `make rebuild <guest>` means the fleet is MIXED for the whole image roll, so a single fleet-wide `pbs_client_repo_suite` is wrong in both directions at once: `bookworm` breaks the first resolute guest, `trixie` breaks every host still on noble. Both suites are now DERIVED from the guest's own `ansible_distribution` facts. Deriving a capability from a runtime fact is not the version-check migration logic the greenfield rule bans — the banned thing is an upgrade path, not "this OS needs that repo"
- **Never turn `pbs_repo_suite` / `pbs_client_repo_suite` back into a fixed value** — they DERIVE from each guest's own `ansible_distribution` facts, and that is load-bearing, not tidiness. The suite tracks the GUEST OS, not the PBS version you want, and the binding constraint is libfuse: the bookworm client needs `libfuse3-3`, the trixie client `libfuse3-4`. Ubuntu noble has only the former, Ubuntu 26.04 (resolute) only the latter, so each suite is uninstallable on the other's guest. Since `make rebuild <guest>` rolls guests one at a time, the fleet is MIXED for the whole image roll and any single value is wrong for half of it — and `pbs_client` runs on **every** fleet guest, so getting it wrong breaks in-guest backups fleet-wide, not just on PBS
- **Never define an SDN VNET for a VLAN the host itself uses on the same bridge** — a VLAN zone on `vmbr0` renders each VNET as `iface <Vnet>` / `bridge_ports vmbr0.<tag>`, so VNETs for VLAN 30 (mgmt) and 20 (Ceph public/NAS) **enslave the host's own `vmbr0.30`/`vmbr0.20` and strip their addresses** on `ifreload`. Hit 2026-08-20 applying the first cutover SDN: ms-01b lost mgmt + Ceph public, its mon dropped out and 3 OSDs went down mid-apply (the other nodes survived only because the API died before their reload). `vlans.yaml` now carries `sdn_zone: null` for storage/infrastructure and the network module never renders a VNET without a zone; PBS's vlan20 leg is a tagged `vmbr0` port. Before any SDN apply on a new bridge layout, diff the rendered `/etc/network/interfaces.d/sdn` ports against `ip -br addr` on a node
- **Never hand `pveceph osd create` a partition whose GPT type is still ZFS (`BF01`)** — PVE's Diskmanage flags any ZFS-typed partition as "in use" from the type GUID alone, so a wiped, zapped, pool-destroyed `-part4` still fails with `device is already in use`. `sgdisk -t 4:8300 <disk>` + `partprobe` first (metadata only). Hit retiring the ADR 0027 bridge pool 2026-08-20; the role's wipefs guard cannot catch it
- **Never `terraform destroy -target` a per-instance resource that other guests share a resource block with** — `-target` on destroy takes the target AND every dependent, and dependencies are tracked per *resource*, not per *instance*: targeting `proxmox_virtual_environment_file.user_data["adguard"]` made every `vms[*]` instance a dependent, so the destroy took **homepage 211** and tried infisical 205 (saved only by `protected = true`, but bpg had already stopped it). Cost a homepage rebuild + an Infisical outage 2026-08-21. Retire a scratch guest with `terraform state rm` + `qm destroy` by hand, or `make rebuild`/`make clean`, never a targeted destroy of a shared-block instance. Always read the `will be destroyed` list before `-auto-approve` on any destroy
- **Never hand a freshly formatted data volume to a CT with `lost+found` still on it** — mkfs.ext4 creates it host-root owned, which is unmapped (nobody) inside an unprivileged CT, so any package postinst that `chown -R`s the mount (Plex does) fails, and the half-configured package then needs a purge because its postinst depends on a preinst-written `/tmp` log. `playbooks/data-volumes.yml` removes it after the chown; do not re-add it
- **Never start a VIP's keepalived while the guest it replaces still holds the address** — keepalived does no conflict detection and claimed `.40.53` beside the running restored 102 for a minute (2026-08-21). The `adguard` role has `adguard_vip_state: stopped` for exactly this: stage installed+configured, stop the old copy, then `systemctl start keepalived` on every instance **from the workstation** (the nodes have no SSH key into the CTs — a nested `ssh root@ct` from a node fails `Permission denied` and leaves DNS down while you notice; cost ~80 s of fleet-wide DNS 2026-08-21)
- **Never trust `mount | grep cephfs` as proof cephfs is usable on a node** — a blocklisted kernel client (`dmesg`: `mds0 session blocklisted`, left over from the 2026-08-20 SDN incident on ms-01b) keeps the mount entry and returns `Permission denied` on every access; PVE reports it as "directory does not exist or is unreachable" and every `vzcreate` on that node fails. `umount -f /mnt/pve/cephfs` and let pvestatd remount (~10 s). Check `pvesm status` on EVERY node after any host-network incident
- **Never put a BIND primary's zone files under `/etc/bind`** — Ubuntu's AppArmor profile grants named `/etc/bind/** r` only, so every RFC 2136 update fails with `journal open failed` (`.jnl: create: permission denied`) while queries keep answering; the old 109 carried zero DDNS records for that reason. Dynamic zones belong in `/var/lib/bind` (`rw` in the profile). Found 2026-08-21 building the dns pair; chowning `/etc/bind` to `bind` does nothing against AppArmor
- **Never rsync out of a Docker *named-volume* mountpoint (`/var/lib/docker/volumes/<v>/_data`) after `docker compose down`** — for NFS-backed volumes docker unmounts the export when the last container goes, so the path is an EMPTY directory and `rsync --delete` from it wipes the destination. The "final delta" pass of the docker→204 replace did exactly that (2026-08-21): emptied 204's Valheim trees and the server generated a fresh world before it was caught. Source the delta from the export itself (the node's `/mnt/nas/...` bind) or from the still-running container; and a 0-files-transferred delta right after a shutdown is a red flag, not a clean result
- **Never expect an unprivileged CT's root to `chown` on an NFS bind mount** — CT root is host uid 100000, an ordinary uid to the NFS server, so any image entrypoint that "fixes volume ownership" as root before dropping privileges (`goodolclint/dogecoin-node` does) dies with `Permission denied`. `chown -R 100000+<uid>` the tree from the node once, and run the container as that uid (`user: "999:999"`) so the root branch is skipped
- **Never remove or reorder an LXC `mount_point` expecting an in-place update** — bpg recreates the container (plan shows `1 to add, 1 to destroy` inside one `make build`), so the rootfs and anything loaded only there (a `docker load`ed local image) is gone; the holder data volume survives because it is attach-only. Read the plan line before `make build` on a CT whose binds changed (cost the valheim fork image 2026-08-22)
- **Never let a Proxmox appliance package pull its `*-meta` Recommends onto a cloud-image guest** — `proxmox-datacenter-manager` *Recommends* `proxmox-datacenter-manager-meta`, which drags in the Proxmox kernel and `grub-pc`; `grub-pc`'s postinst has no install device on a cloud image and dpkg fails mid-transaction (cost two `make rebuild pdm` 2026-08-22). Install the real packages with `install_recommends: false`, and delete the `*-enterprise.sources` file the package drops (401 without a subscription, kills every later `apt update`) — the `pdm` and `proxmox_backup` roles both do this
- **Never assume a PVE backup job survives `make rebuild` of its only member** — PVE deletes a job when its last VMID is destroyed (`pbs-self` vanished with the PBS re-home, 2026-08-23), and bpg then fails every refresh with `Unable to Read Backup Job`, which breaks `make inventory`. Recovery: `terraform state rm 'proxmox_backup_job.jobs["<job>"]'` then `make backup-jobs`. Rebuilding PBS therefore always ends with `make backup-jobs`
- **Never leave a stopped/retired guest in `vm-configs.tf` when running a multi-host play** — an unreachable host makes its play end with "NO MORE HOSTS LEFT", and Ansible then aborts the *entire* playbook: the 2026-08-23 fleet `pbs_client` pass silently skipped every services play because the retired nvidia-licensing VM sat before them. Retired guests come out of `vm-configs.tf` the day they stop; `make inventory` then drops them
- **Never run a per-minute `:latest` pod against Zot without `imagePullPolicy: IfNotPresent`** — every start re-checks the tag upstream, Zot's on-demand sync forwards each check to Docker Hub anonymously from the house's one WAN address, and the 100-pulls-per-6-h quota died within minutes of the first CronJob (2026-08-23: every `docker.io` pull through Zot then failed `not found` — including the migration pod). Prefer `public.ecr.aws/docker/library/*` mirrors for throwaway images; Zot proxies that registry too
- **Never name a Kubernetes Service after a container that reads `<NAME>_PORT` from its environment** — the pod gets the service-link env `SPEEDTEST_PORT=tcp://10.x:9798`, which the speedtest exporter read as its listen port and crash-looped. `enableServiceLinks: false` on the pod, or rename the Service
- **Never drive one role's task-tag through `infrastructure.yml --tags <task-tag>`** — `pre_tasks` are a dynamic include tagged `always`, and that tag does not reach the included tasks; only a *play* tag (which every task in the play inherits) makes the SOPS/Infisical/VLAN loading run. With `--tags proxmox-user` the phase-3 play created the PVE token and then died at the Infisical write with `'bootstrap_config' is undefined` (censored by `no_log`, four runs to see it). `make monitoring-users` uses the play tag and `--skip-tags unifi-user`
- **Never derive `openobserve_listen_host` from `hostvars['openobserve']`** — the guest is gone (ADR 0036); the address is the services prefix + `monitoring_syslog_offset` (66) and every sender (`rsyslog_client`, Docker syslog driver, nodes' `host-bindings.yaml`, pfSense, UniFi, Kuma) must point at that LB
- **Never let `helm_apply` meet a chart that emits objects in another namespace** — `lib.sh` passes `-n <ns>` to `kubectl apply`, which rejects any object whose `metadata.namespace` differs (the cert-manager chart's leader-election Role/RoleBinding default to `kube-system`; `make talos-certs` failed on the first run after P4a added the flag). Move the objects with a chart value (`global.leaderElection.namespace`) rather than dropping `-n`, which other charts need
- **Never run a secrets-loading play under `--check` without `check_mode: false` on the Infisical reads** — `infisical.vault.read_secrets` short-circuits in check mode and returns an empty list, so `secrets` collapsed to nothing and the Kuma role failed on `secrets.monitoring` (2026-08-24). `infisical_login.yml` now pins its four read-only tasks to `check_mode: false`; keep that when touching the loader
- **Never give an optional Uptime Kuma monitor parameter a module default** — `goodolclint.uptime_kuma` 0.2.0 defaulted `dns_resolve_server`/`dns_resolve_type`/`json_path_operator`, so every UI-created monitor (null in the DB) reported `changed` on every run and check mode could never reach 0; fixed in 0.2.1 (creation still gets Kuma's own defaults from the client's `MONITOR_DEFAULTS`). The acceptance test for any collection bump is `make uptime-kuma CHECK=1` = 0 changes
- **Never assume the Infisical VM's own play loaded runtime secrets** — `load_secrets.yml` sets `secrets: {}` for an Infisical-only play, now only while `/api/status` is unreachable (bootstrap/rebuild). Before 2026-08-24 it skipped unconditionally, so `pbs_client` on 205 baked empty PBS credentials and the vault's nightly Postgres dump (the CA's DR path, ADR 0039) had never succeeded — the kind of silent gap only snapshot recency on PBS exposes
- **Never probe a name in the flat service zone before its record exists and then trust the next answer** — AdGuard caches the NXDOMAIN for the zone's negative TTL (`cache_ttl_max: 0` = no cap), and macOS's resolver does the same on the client, so a name created seconds later stays "missing" on that path. The zone shell now carries a 300 s negative TTL (`db.zone.j2`, the live zone's SOA was updated by hand 2026-08-25 — the initial-only template does not touch existing files), and AdGuard's `POST /control/cache_clear` on both instances is the fix for a stuck client; the Mac needs `dscacheutil -flushcache`
- **Never put a redirect in front of the Kuma playbook's `uptime_kuma_api_url`** — python-socketio fails the handshake on a 302 with `scheme https is invalid`; it is `uptime-kuma.{{ service_domain }}`, straight to the app. Kuma's own HTTP monitors follow redirects fine
- **Never treat a 302-to-self from authentik's flow executor API as a failure** — `POST /api/v3/flows/executor/<slug>/` answers a stage with a redirect to its own URL, which means "accepted, GET the next stage" (stages with nothing to show — MFA validation with no devices, user-login — redirect the same way, several in a row). The scripted Grafana OIDC proof for P5c loops GETs until JSON; the `query=` parameter is the flow URL's own query string, and anonymous POSTs need no CSRF token
- **Never leave the embedded outpost's `config.authentik_host` unset, and never leave an OAuth2 provider's `grant_types` implicit in a blueprint** — with 2026.x the first makes every forward-auth 302 point at `http://localhost/…`, the second creates the provider with `grant_types: []` and every authorize request fails "Invalid grant_type". Both are silent in `ak apply_blueprint`'s output; `kubernetes/authentik/blueprint-internal.yaml` sets both. A running embedded outpost also needs a server restart to pick up a changed `authentik_host`
- **A new Ingress hostname for SABnzbd must also land in its `host_whitelist`** — SAB rejects any Host header outside it (`Hostname verification failed`, 403). The whitelist lives only in SAB's live config: `api?mode=set_config&section=misc&keyword=host_whitelist` from inside the pod (`localhost` bypasses the check; the ingress path cannot fix itself). Done for `sabnzbd.<service domain>` 2026-08-25
- **Never assume `make ansible-all` reached `services.yml`** — `site.yml` runs with `vms.yaml` only, so `groups['proxmox']` is undefined and `proxmox_host` (gitignored `all.yml`) falls back to a literal; when that literal was the pre-cutover pve address, Phase 3 died UNREACHABLE and Ansible aborted before every services play, silently, on every fleet pass from cutover to 2026-08-25. Read the recap for the `plex` line, not just the exit code; the fallback is now the cluster VIP, and `ansible-all`/`ansible-infra` carry `--skip-tags unifi-user` like `make monitoring-users` (the UniFi login half is the lockout risk above and was the next thing to abort the run)
- **Never leave a Postgres-backed arr Deployment without a liveness probe** — sonarr/radarr/lidarr/prowlarr treat a refused DB connection at startup as "non-recoverable failure, waiting for user intervention": the process stays alive, readiness never passes, nothing restarts it (2026-08-25: 20 min of 503s after a redeploy raced the postgres restart). `kubernetes/plex-services/app.yaml` carries `livenessProbe` on `/ping` for the four
- **Never trust a Kuma `login` timeout before checking the server's version** — Uptime Kuma 2.5.3 answers a password login in ~3.5 s; the collection's retried requests used a 3 s per-attempt timeout, so every attempt timed out while Kuma logged one successful login per retry (and each retry burns the 20/min password limit). Fixed on the collection's main (PR #12, 10 s); `ansible/requirements.yml` pins that main SHA until the operator tags v0.2.2 — bump the pin then, not before
- **Never change `domain_suffix` in `vlans.yaml` outside a deliberate VM roll** — since P5e it names no DNS zone; its one consumer is the VM fleet's cloud-init `fqdn`, and a changed user-data snippet shows every VM (infisical, PBS, unifi, pdm, pxe, apt-cache) as `must be replaced` (ADR 0016). Flip it to the service domain in the same window as the next fleet-wide image roll
- **Never use `echo -n`** in secret generation commands — use `printf '%s'`
- **Never use `is succeeded` alone** to gate on a registered variable — add `is not skipped`
- **Never use `--start-at-task` with `services.yml --limit plex-services`** — breaks role dependencies
- **Never use `git add -i` or `git rebase -i`** — interactive mode not supported
- **Never commit `vlans.yaml`, `private_bindings.yaml`, or `infisical-backup.yml`** — these are gitignored
- **Never add migration/upgrade logic** — roles must work on fresh VMs
- **Never skip pre-commit hooks** with `--no-verify`
- **Never reference `bootstrap.*` secrets in templates** outside the bootstrap/infisical roles
- **Never use `define`/`$(eval export)` in the Makefile for secret propagation** — use top-level `export`
- **Never bake secrets into docker-compose templates** via `{{ secrets.xxx }}` — use Infisical agent `env_file` exclusively
- **Never write to Infisical root `/`** — all secrets live in named subfolders. Root must remain empty.
- **Never reference `secrets.sops.yml` in operational context** — it is a DR artifact only, produced by `make infisical-backup`
- **Never write copies of secrets to consumer folders** — consumers read from the source folder via multi-folder agent templates. Secrets belong to the VM/role that generates them.

## Known Inconsistencies (Standardization Candidates)

1. ~~Compose file base paths~~ — moved to Resolved #6.

2. ~~NFS mount options~~ — moved to Resolved #7.

3. ~~`timezone` duplication~~ — moved to Resolved #5.

4. ~~AdGuard DNS rewrites hardcode VM names~~ — moved to Resolved #8.

5. ~~PBS storage backend~~ — moved to Resolved #9.

6. ~~One Ansible-rendered .env file still bakes a secret directly~~ — moved to Resolved #10.

### Resolved

1. **Secret delivery is now unified.** All containers use Infisical agent `env_file` paths — no more `{{ secrets.xxx }}` baked into docker-compose templates. (Resolved by secrets refactor Phases 4-6.)

2. **`docker-config.yml` no longer flattens nested secrets.** Compose templates use `env_file:` instead of `{{ secrets.xxx }}`, so the `combine()` flatten pre_tasks have been removed. A monitoring variable alias block remains in docker-config.yml (setting bare variables like `openobserve_root_user_pass` from `secrets.monitoring.*`) because `openobserve.env.j2` references those bare names — see Known Inconsistency #6. (Resolved by secrets refactor Phase 5; dead flatten blocks removed in architecture cleanup Phase A; the alias block went with the monitoring role in P4b.)

10. **`openobserve.env.j2` is gone.** The monitoring stack moved to the cluster (ADR 0036); OpenObserve's password reaches the pod only through the `InfisicalSecret`. No Ansible-rendered file bakes a secret any more.

3. **Homepage role scope reduced.** API key extraction and Infisical secret seeding moved to plex-services role. Homepage now focuses on template deployment, Caddy, and Portainer. (Resolved by secrets refactor Phase 2.)

4. **Monitoring role secret delivery unified.** Both OpenObserve and Grafana/exporters now use per-container Infisical agent env_files exclusively. (Resolved by secrets refactor Phases 5-6.)

5. **`timezone` centralized in `group_vars/all.yml`.** Removed duplicate definitions from `docker`, `plex_services`, `homepage`, `monitoring` role defaults and `host_vars/plex-services.yml`. All roles reference the single `{{ timezone }}` variable. (Resolved in architecture cleanup Phase B.)

6. **Compose file base paths unified.** All services now use `/opt/<service>/docker-compose.yml` — the docker legacy VM moved from flat `/opt/docker-compose.yml` to `/opt/docker/docker-compose.yml`. Each role defines a `*_base_dir` default variable. A shared `tasks/deploy_compose.yml` task provides a reusable deploy+restart pattern. Requires `make rebuild docker` for existing docker VMs. (Resolved in architecture cleanup Phase E+F.)

7. **NFS mount options unified.** All roles now use the centralized `{{ nfs_mount_options }}` variable from `group_vars/all.yml`. Docker VM volumes replaced hardcoded `nfsvers=4,...,timeo=600` with `{{ nfs_mount_options }}` (gains NFSv4.1, noatime, nconnect=4). Plex and PBS system-level mounts replaced `opts: "defaults"` with `{{ nfs_mount_options }}`. The plex_services volume auto-recreate logic remains unique to that role. The `*_nfs_src: "host:/path"` variable pattern and `.split(':')` calls were audited but intentionally left as-is — the source variables are centralized in `all.yml`, the split pattern works consistently (docker pre-splits in task vars, plex-services splits inline in template), and replacing it with separate `nfs_server` + `*_nfs_path` variables would double the NFS variable count for no functional gain. (Resolved in architecture cleanup Phase I.)

8. **AdGuard DNS rewrites parameterized.** Hardcoded VM names replaced with three configurable variables in `adguard/defaults/main.yml`: `adguard_rewrite_vms` (list of inventory hostnames), `adguard_service_aliases` (domain→target mappings), `adguard_cross_vlan_rewrites` (hostname+zone+prefix translation). Adding a new VM only requires appending to the list — no template changes. Also fixed latent bug where `hostvars['proxmox_backup']` (underscores) silently skipped the PBS rewrite because the inventory hostname is `proxmox-backup` (hyphens). (Resolved in architecture cleanup Phase J.)

9. **PBS storage backend cleaned up.** Both iSCSI and NFS backends were already functional via `pbs_storage_backend` conditional — the issue was cosmetic. Removed misleading "legacy" label from NFS config comment, replaced hardcoded `/mnt/backups` paths with `{{ pbs_nfs_mount_path }}`, changed mount source from `{{ proxmox_backup_nfs_src }}` to `{{ pbs_nfs_source }}` (the role's own default), and made datastore comment backend-agnostic. (Resolved in architecture cleanup Phase K.)
