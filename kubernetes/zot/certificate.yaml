apiVersion: cert-manager.io/v1
kind: Certificate
metadata: {name: zot-tls, namespace: zot}
spec:
  dnsNames: ["registry.${DOMAIN}"]
  secretName: zot-tls
  issuerRef: {name: homelab-ca, kind: ClusterIssuer}
