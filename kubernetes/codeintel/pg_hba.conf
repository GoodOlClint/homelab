# ADR 0055: TLS only off the pod, each role from its one consumer.
local    all        all                          trust
host     all        all        127.0.0.1/32      scram-sha-256
hostssl  codeintel  ci_writer  ${CODE_INTEL}/32  scram-sha-256
hostssl  codeintel  ci_reader  ${MCP}/32         scram-sha-256
hostssl  all        postgres   ${POD_CIDR}       scram-sha-256
