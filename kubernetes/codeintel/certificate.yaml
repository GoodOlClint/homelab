apiVersion: cert-manager.io/v1
kind: Certificate
metadata: {name: postgres-tls, namespace: codeintel}
spec:
  dnsNames: ["codeintel.${DOMAIN}"]
  secretName: postgres-tls
  issuerRef: {name: homelab-ca, kind: ClusterIssuer}
