apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata: {name: infisical, namespace: flux-system}
spec:
  interval: 10m
  path: ./kubernetes/infisical
  prune: false
  sourceRef: {kind: GitRepository, name: homelab}
  serviceAccountName: flux-infisical
  postBuild:
    substituteFrom: [{kind: ConfigMap, name: cluster-bindings}]
