apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata: {name: headlamp, namespace: flux-system}
spec:
  interval: 10m
  path: ./kubernetes/headlamp
  prune: false
  sourceRef: {kind: GitRepository, name: homelab}
  serviceAccountName: flux-headlamp
  postBuild:
    substituteFrom: [{kind: ConfigMap, name: cluster-bindings}]
