apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata: {name: metrics, namespace: flux-system}
spec:
  interval: 10m
  path: ./kubernetes/metrics
  prune: false
  sourceRef: {kind: GitRepository, name: homelab}
  serviceAccountName: flux-metrics
  postBuild:
    substituteFrom: [{kind: ConfigMap, name: cluster-bindings}]
