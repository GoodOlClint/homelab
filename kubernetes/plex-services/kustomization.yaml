apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [pv.yaml, pvc.yaml, secrets.yaml, app.yaml]
configMapGenerator:
  - name: recyclarr-config
    namespace: plex-services
    files: [config/recyclarr.yml]
