apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared by every tree with an in-tree Postgres (ADR 0037/0048): a tree includes `../pg-backup` and
# supplies NS PG_USER PG_SECRET BACKUP_ID SCHEDULE through its Flux Kustomization's postBuild.substitute.
resources: [cronjob.yaml]
